{
  "selected_gaps": [
    {
      "gap": "No retrieved leaf preserves the pairwise geometry of imagined action trajectories while fitting a packed weight codebook, so a low vector-reconstruction error need not preserve the planner's relative candidate structure — stakes: a deployment engineer can lose action choices at the same nominal bytes, while researchers can mistake pointwise fidelity for decision fidelity and draw non-transferring conclusions.",
      "chosen_pattern_id": "architectural_operator_substitution",
      "anchor_alternates": [
        "structural_prior_encoding",
        "targeted_self_supervised_objective"
      ],
      "companion_pattern": "structural_prior_encoding",
      "companion_rationale": "The operator-substitution move supplies a packed additive/product VQ fitting procedure; structural-prior encoding then makes a goal-anchored trajectory relation invariant an explicit codeword/index constraint. The intermediate object is a block-local codebook/index assignment evaluated through the frozen downstream rollout.",
      "selection_rationale": "This is the construction gap that most directly closes the bottleneck and matches the requested method contribution: it changes what the VQ fitting operator preserves without changing the runtime representation. The companion encodes a concrete invariant in the substituted operator, while fixed bytes prevent the work from becoming another sensitivity-based rate allocator.",
      "deliverable_coverage": "method — anchor gap → architectural_operator_substitution yields the runnable packed VQ fitting procedure, with structural_prior_encoding supplying the goal-anchored relational constraint"
    }
  ],
  "coherence_thread_type": "n_a",
  "deliverable_coverage": "method — anchor gap → architectural_operator_substitution yields the runnable packed VQ fitting procedure, with structural_prior_encoding supplying the goal-anchored relational constraint",
  "user_direction_disposition": {
    "verdict": "adopted",
    "user_wanted": "yields real model compression or a clearly separated action-tokenization contribution",
    "chosen": "real weight compression using a fixed packed additive/product VQ layout; action tokenization is retained only as a clearly separated adjacent baseline"
  },
  "composition_note": "",
  "pattern_saturation": {
    "architectural_operator_substitution": {
      "share": 0.71,
      "saturation_band": "saturated",
      "selection_note": "saturated lane — differentiation_from_lit must substantively defend the relational, goal-anchored codebook objective"
    },
    "structural_prior_encoding": {
      "share": 0.18,
      "saturation_band": "mid_frequency",
      "selection_note": ""
    }
  },
  "deferred_gaps": [
    {
      "gap": "RSAVQ's language-loss curvature, QuantWAMs' joint Fisher/layer score, and QuantWM's module sensitivity do not establish whether a relational trajectory invariant is necessary for action-conditioned planning rather than merely correlated with it — stakes: low-bit WMs/WAMs may waste memory on proxies that do not protect closed-loop behavior, and the field lacks a principled test of which quantization objective transfers.",
      "reason": "deferred — the anchor construction can test this necessity directly; a second diagnostic leg fails the removal test."
    },
    {
      "gap": "All current leaves inherit the assumption that a pointwise reconstruction or scalar task loss is an adequate surrogate for the planner's comparison relation; no leaf tests this assumption against held-out initial states and candidate pairs — stakes: this can hide failures that only appear when two plausible actions are close, blocking trustworthy compression claims even when average MSE improves.",
      "reason": "deferred — this is the framing assumption audited by the anchor's ablation, not a separate module."
    },
    {
      "gap": "Action-tokenizer leaves such as VQ-VLA discretize executable action representations, but the retrieved corpus does not test whether their gains can be combined with or distinguished from actual backbone-weight byte compression — stakes: users may report token vocabulary gains as model compression, while fair deployment accounting remains unresolved.",
      "reason": "deferred — action tokenization is an adjacent baseline and would make a second contribution; it is out of scope for this weight-compression candidate."
    }
  ],
  "direct_collision_constraints": [
    "ordinary RKD-style VQ (CVPR 2019): same paired rollout representations, same frozen goal anchor, same bytes and same codebook map; if mathematically equivalent, classify as domain adaptation rather than new optimization",
    "QATMA/TPSD (CR-QAT label) (arxiv:2603.05964): pairwise similarity matrices plus quantization-aware training; search and matched implementation must be completed before any novelty language",
    "goal-score-only/diagonal-only: the original DINO-WM objective is called directly, so off-diagonal relation must show independent held-out planner benefit"
  ],
  "selection_rationale": "This is the construction gap that most directly closes the bottleneck and matches the requested method contribution: it changes what the fixed-byte VQ fitting operator preserves without changing runtime representation. Because ordinary RKD (CVPR 2019) and QATMA/TPSD (CR-QAT label) (arxiv:2603.05964) already make pairwise relations part of distillation/quantization, the candidate is only a narrow conditional weight-VQ/planner adaptation; matched controls must establish whether shared codeword coupling adds anything."
}

