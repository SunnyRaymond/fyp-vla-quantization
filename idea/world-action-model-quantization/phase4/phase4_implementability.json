{
  "underspecified_points": [
    {
      "step_id": "S1",
      "hole": "The manifest does not state an immutable identity for the Optional idm checkpoint or the exact comparison rule for Fast-WAM and idm artifacts.",
      "fill": "Authors must supply, without substituting a different artifact, the model family, artifact path or URI, filename, repository revision, checkpoint or training-step identifier, dtype, and verification digest for each applicable Fast-WAM and Optional idm checkpoint; define exact-match comparison and halt before branching when any required identity is absent or mismatched.",
      "severity": "open"
    },
    {
      "step_id": "S1",
      "hole": "The replay manifest has no declared CPU thread/process limits or accelerator-memory identity, so the 80GB-class versus A100-40GB execution boundary is not reproducible.",
      "fill": "Authors must choose and record the CPU thread/process limits, accelerator model, visible memory class, and memory-allocation policy as fixed environment fields, explicitly declaring the 80GB-class or A100-40GB boundary; hold those fields constant across paired branches.",
      "severity": "open"
    },
    {
      "step_id": "S2",
      "hole": "The chosen low-bit operator is not identified for weights or activations, including bit width, quantizer parameters, granularity, calibration scope, and whether the branch uses fake quantization or a real low-bit kernel.",
      "fill": "Authors must choose and register the W/A operator contract: bit width b_lo, signedness, rounding, clipping, scale/zero-point or equivalent, per-tensor/channel/group granularity, activation observer data, and execution mode. Mark fake quantization as graph-level emulation; if a real kernel is claimed, record its backend/version and verify that the intervened path actually executes it. Do not treat the two modes as interchangeable.",
      "severity": "open"
    },
    {
      "step_id": "S3",
      "hole": "The candidate fixes the d_a and d_o denominators, normalized floors, default relative weights, and episode-mean site aggregation, but the valid-element mask and observation preprocessing for the pixel term remain unspecified.",
      "fill": "Use d_a(j,r)=MSE(full action chunks)/max(sigma_a^2,epsilon_a^2), with sigma_a^2 the population variance over all scalar coordinates of FP16 full action chunks in D_cal; use d_o(j,r)=mean per-pixel absolute next-observation difference/max(sigma_o,epsilon_o), with sigma_o the calibration mean per-pixel standard deviation. Set epsilon_a=epsilon_o=1e-3 in normalized units, lambda_a=lambda_o=1 by default, preregister the lambda_a/lambda_o sweep {0.5,1,2}, and define C_r as the episode mean with bootstrap intervals over complete episodes. Authors must still record the valid-element mask and observation preprocessing before held-out evaluation.",
      "severity": "open"
    },
    {
      "step_id": "S3",
      "hole": "The fate of terminal, invalid, or non-renderable first-action transitions is unspecified.",
      "fill": "Authors must choose an inclusion policy for terminal, simulator-error, missing-observation, and variable-length action-chunk rows, and record the reason for every excluded row; the policy must be identical for both branches and all rankings.",
      "severity": "open"
    },
    {
      "step_id": "S4",
      "hole": "The numeric high-precision-site count and its matched-baseline manifest entry are not supplied. The method fixes B_site as the count and fixes B equivalent to B_site, but the run cannot proceed without the numeric count.",
      "fill": "Record B_site as the nonnegative integer high-precision-site count from the matched local-fidelity baseline in the preregistered run manifest, set B equal to B_site everywhere in this method, halt if the entry is missing or B_site>|R|, and resolve ties by registry order. The numeric B_site remains an author-supplied manifest input; do not substitute bytes or latency.",
      "severity": "open"
    },
    {
      "step_id": "S4",
      "hole": "A site-count schedule has no mapping to packed bytes or measured latency, although sites may have different shapes and execution paths.",
      "fill": "Authors must decide whether any real-bytes or latency result is in scope; if yes, add a separate packing and timing protocol covering dtype packing, kernel/backend, warm-up, synchronization, and aggregation, and otherwise label those outcomes out of scope. Site count alone must never be reported as real bytes or latency.",
      "severity": "open"
    },
    {
      "step_id": "S5",
      "hole": "The held-out RoboTwin 2.0/LIBERO split and contact-sensitive subset are named but not enumerated or labeled.",
      "fill": "Authors must list exact task and episode IDs, initial-state IDs, seeds, split membership, and the contact-transition predicate from logged simulator events; define task failure and success aggregation before inspecting results.",
      "severity": "open"
    },
    {
      "step_id": "S5",
      "hole": "The candidate fixes the minimal Fast-WAM mode, sigma shift, and infer_action path, but the exact entrypoint revision/arguments, action horizon, termination rule, and evaluation window still require a manifest entry.",
      "fill": "Record the exact idm and infer_action entrypoint revisions and arguments, action horizon, reset and termination rules, and whether next-observation deviation is first-transition-only or a later/full-episode summary; keep later summaries separate from C_r. Preserve the fixed minimal path mode=idm, sigma_shift_FW=1.0, and inference_path_FW=infer_action, and halt when the required identity is absent or mismatched.",
      "severity": "open"
    },
    {
      "step_id": "S5",
      "hole": "The permutation test and the oracle simulator-state branch have no reproducible randomization, replacement point, or acceptance definition.",
      "fill": "Authors must specify the permutation seed and number or complete list of permutations, preserve the score multiset and all other schedule rules, and define exactly which simulator-state quantity the oracle supplies and what comparison it controls; record the oracle branch as a labeled control, not as a replacement for the real association.",
      "severity": "open"
    }
  ],
  "enriched_steps": [
    {
      "step_id": "S1",
      "what_changes": "Create one immutable calibration record for each state before any paired measurement. Store state_id, task and episode identifiers, the starting observation and preprocessing record, simulator snapshot, controller state, ordered history, all random-number-generator streams, cache state, model/checkpoint identity, site-registry revision, and environment identity. Serialize one common baseline snapshot, then clone it into the FP16 reference branch and each candidate branch; restore each branch in a fixed order: model/checkpoint state, simulator state, controller state, history buffers, RNG streams, and cache contents plus keys and eviction metadata. The controller record must include policy hidden state, timestep, queued action chunk, and mode; the history record must preserve ordered observations, actions, rewards, and termination flags; the cache record must preserve scope and invalidation state. After restoration, replay the same baseline input and require the reference branch to reproduce the recorded observation and action output before measuring a site. The manifest must contain the exact Fast-WAM checkpoint identity and, when the Optional idm path is used, the exact Optional idm checkpoint identity and configuration. 【author decision: supply the immutable checkpoint identities and the exact-match fields for Fast-WAM and Optional idm; missing or mismatched identity must halt with no fallback artifact.】 The environment record must include CPU thread/process limits and the accelerator model and visible-memory class, explicitly distinguishing an 80GB-class run from an A100-40GB run. 【author decision: fix those execution-environment fields and the 80GB-class versus A100-40GB boundary in the manifest, including the memory-allocation policy.】 If any required identity or restoration check fails, halt before paired branch creation.",
      "what_to_do_en": "Create one immutable calibration record for each state before any paired measurement. Store the state identifier, task and episode identifiers, starting observation and preprocessing record, simulator snapshot, controller state, ordered history, all random-number-generator (RNG) streams, cache state, model/checkpoint identity, site-registry revision, and environment identity. Serialize one common baseline snapshot, then clone it into the 16-bit floating-point (FP16) reference branch and one candidate branch per site; restore each branch in a fixed order: model/checkpoint state, simulator state, controller state, history buffers, RNG streams, and cache contents with keys and invalidation metadata. The controller record must include policy hidden state, timestep, queued action chunk, and mode; the history record must preserve ordered observations, actions, rewards, and termination flags; the cache record must preserve scope and invalidation state. After restoration, replay the same baseline input and require the reference branch to reproduce the recorded observation and action output before measuring a site. Store the exact Fast-WAM checkpoint identity and, when Optional idm is used, the exact Optional idm checkpoint identity and configuration in the manifest. 【author decision: supply the immutable Fast-WAM and Optional idm checkpoint identities and exact-match fields; a missing or mismatched identity must halt with no fallback artifact.】 Store the central processing unit (CPU) thread/process limits and accelerator model and visible-memory class as environment fields, explicitly distinguishing an 80GB-class run from an A100-40GB run. 【author decision: fix those environment fields and the 80GB-class versus A100-40GB boundary in the manifest, including the memory-allocation policy.】 If any required identity or restoration check fails, halt before creating paired branches.",
      "what_to_do_zh": "先冻结 manifest。 在任何 paired measurement 之前，为每个 state 建立不可变 calibration record，记录 state、checkpoint/configuration identities、controller/RNG/history/cache restoration contract 与 environment identity。 manifest 中必须提供 Fast-WAM 与 Optional IDM 的 exact checkpoint identities 和 exact-match fields；缺失或不匹配时 halt。【作者需决定：补齐不可变 checkpoint identities、revision/digest 与 exact-match fields；不得 fallback 到其他 artifact。】 environment record 还必须固定 CPU thread/process limits、accelerator model、visible-memory class 与 memory-allocation policy。【作者需决定：在 manifest 中补齐这些 execution-environment fields，并明确 A100-40GB 与 80GB-class 边界。】 任何必需 identity 或 restoration check 缺失或不匹配，都必须在建立 paired branches 之前 halt。"
    },
    {
      "step_id": "S2",
      "what_changes": "Build a versioned site registry with stable site_id, module path or graph edge, tensor role (weight or activation), shape, dtype, capture point, and quantization granularity. For each candidate record, restore the S1 baseline, apply a mask containing exactly one site_id, quantize the selected weight tensor or intercept the selected activation at its registered graph edge, and leave every other target site at FP16. Record the operator parameters and verify that the mask changed only the selected site. The low-bit operator must be declared as either fake quantization or a real low-bit execution kernel, and the declared path must be used unchanged in calibration and held-out evaluation. 【author decision: choose and register b_lo, signedness, rounding, clipping, scale/zero-point, granularity, activation calibration data, and fake-quantization versus real-kernel mode; record backend/version and runtime verification when a real kernel is claimed.】 This is a single-site FP16-background intervention, not a joint-low-bit marginal or a joint-optimal schedule; QuantWM and QuantWAMs remain boundaries for their broader quantization scopes.",
      "what_to_do_en": "Build a versioned site registry for weight/activation (W/A) sites with a stable site_id, module path or graph edge, tensor shape, data type, capture point, and quantization granularity. For each candidate, restore the S1 baseline, apply a mask containing exactly one site_id, quantize the selected weight tensor or intercept the selected activation at its registered graph edge, and leave every other target site at 16-bit floating-point (FP16). Record the operator parameters and verify that the mask changed only the selected site. Declare the low-bit operator as either fake quantization or a real low-bit execution kernel, and use that declared path unchanged in calibration and held-out evaluation. 【author decision: choose and register b_lo, signedness, rounding, clipping, scale/zero-point, granularity, activation calibration data, and fake-quantization versus real-kernel mode; record backend/version and runtime verification when a real kernel is claimed.】 This is a single-site FP16-background intervention, not a joint-low-bit marginal or joint-optimal schedule; QuantWM and QuantWAMs remain prior-art boundaries for their broader quantization scopes.",
      "what_to_do_zh": "建立一个 versioned site registry，给 weight/activation (W/A) site 配置稳定的 site_id、module path 或 graph edge、tensor shape、dtype、capture point 与 quantization granularity。 对每个 candidate，先恢复 S1 baseline，再应用只包含一个 site_id 的 mask；在注册的 graph edge 上量化选定的 weight tensor 或截取选定的 activation，其余 target sites 全部保持 16-bit floating-point (FP16)。 记录 operator parameters，并验证 mask 只改变了被选中的 site。 必须把 low-bit operator 声明为 fake quantization（图级仿真）或真实的 low-bit execution kernel，并在 calibration 与 held-out evaluation 中保持同一条已声明路径。 【作者需决定：选择并登记 b_lo、signedness、rounding、clipping、scale/zero-point、granularity、activation calibration data，以及 fake-quantization 与 real-kernel mode；若声称使用真实 kernel，还要记录 backend/version 并做 runtime verification。】 这只是 single-site、FP16-background 的 intervention，不是 joint-low-bit marginal 或 joint-optimal schedule；QuantWM 与 QuantWAMs 的更广 quantization scope 仍属于 prior-art boundary。"
    },
    {
      "step_id": "S3",
      "what_changes": "From each restored reference/candidate pair, run inference once and capture the complete action chunks before executing any action. Execute only the first action from the restored state and capture the immediate next observation. Define d_a(j,r) as the full-action-chunk MSE divided by max(sigma_a^2, epsilon_a^2), with sigma_a^2 the population variance over all scalar coordinates of FP16 full action chunks in D_cal. Define d_o(j,r) as the mean per-pixel absolute next-observation difference divided by max(sigma_o, epsilon_o), with sigma_o the calibration mean per-pixel standard deviation. Set epsilon_a=epsilon_o=1e-3 in normalized units, lambda_a=lambda_o=1 by default, preregister the lambda_a/lambda_o sweep {0.5,1,2}, and aggregate C_r as the episode mean with bootstrap intervals over complete episodes. Define the open-loop local surrogate L_jr as the mean squared difference over all elements of the paired hook outputs and L_r as its episode mean. The remaining open fields are the valid-element mask and observation preprocessing for the pixel term.【author decision: record both before held-out evaluation and apply them identically to both branches.】 Terminal, invalid, simulator-error, missing-observation, and variable-length rows also require one shared inclusion policy.【author decision: state whether each row is retained, masked, or excluded and record every exclusion reason.】",
      "what_to_do_en": "Run the reference and one-site branch from matched restored states. Save each complete action chunk before any action is executed, including its time length, action ordering, valid-step mask, and data type. Restore the state separately for each branch, execute only the first action, and save the immediate next observation and terminal or simulator-error flags. Define d_a(j,r) as the full-action-chunk mean-squared error divided by max(sigma_a^2, epsilon_a^2), where sigma_a^2 is the population variance over all scalar coordinates of FP16 full action chunks in D_cal. Define d_o(j,r) as the mean per-pixel absolute next-observation difference divided by max(sigma_o, epsilon_o), where sigma_o is the calibration mean per-pixel standard deviation. Use epsilon_a=epsilon_o=1e-3 in normalized units and lambda_a=lambda_o=1 by default, preregister a lambda_a/lambda_o sweep over {0.5,1,2}, and set C_r to the episode mean of lambda_a d_a+lambda_o d_o with bootstrap intervals over complete episodes. Define the open-loop local surrogate L_jr as the mean squared difference over all elements of the paired hook outputs and L_r as the episode mean. The remaining open fields are the valid-element mask, observation preprocessing, and one identical inclusion policy for terminal, invalid, simulator-error, missing-observation, and variable-length rows. C_r remains a one-control-transition proxy, not task loss, a later rollout/replan marginal, or a jointly-low-bit marginal. The remaining open fields are the valid-element mask and observation preprocessing for the pixel term.【author decision: record both before held-out evaluation and apply them identically to both branches.】 Terminal, invalid, simulator-error, missing-observation, and variable-length rows also require one shared inclusion policy.【author decision: state whether each row is retained, masked, or excluded and record every exclusion reason.】",
      "what_to_do_zh": "从匹配的 restored states 分别运行 reference branch 与 single-site branch。执行任何 action 之前保存完整 action chunks，包括 time length、action ordering、valid-step mask 与 dtype；每个 branch 分别恢复同一 state，只执行第一个 action，并保存 immediate next observation 以及 terminal 或 simulator-error flags。定义 d_a(j,r)：完整 action chunk 的 MSE 除以 max(sigma_a^2, epsilon_a^2)，其中 sigma_a^2 是 D_cal 中 FP16 full action chunks 所有 scalar coordinates 的 population variance；定义 d_o(j,r)：next observation 的 mean per-pixel absolute difference 除以 max(sigma_o, epsilon_o)，其中 sigma_o 是 calibration 中 per-pixel standard deviations 的 mean。使用 normalized units 中的 epsilon_a=epsilon_o=1e-3，默认 lambda_a=lambda_o=1，预注册 lambda_a/lambda_o sweep {0.5,1,2}，并将 C_r 定义为 lambda_a d_a+lambda_o d_o 的 episode mean，在完整 episode 上计算 bootstrap intervals。定义 open-loop local surrogate L_jr 为 paired hook outputs 所有 elements 的 MSE，L_r 为其 episode mean。仍需明确 valid-element mask、observation preprocessing，以及对 terminal、invalid、simulator-error、missing-observation 与 variable-length rows 一致适用的 inclusion policy。C_r 仍是 one-control-transition proxy，不是 task loss、后续 rollout/replan marginal 或 jointly-low-bit marginal。 pixel term 的 valid-element mask 与 observation preprocessing 仍需补齐。【作者需决定：在 held-out evaluation 前记录两者，并对两个 branch 一致执行。】terminal、invalid、simulator-error、missing-observation 与 variable-length rows 也需要同一套 inclusion policy。【作者需决定：说明每类 row 是 retained、masked 还是 excluded，并记录每个 exclusion reason。】"
    },
    {
      "step_id": "S4",
      "what_changes": "Construct two deterministic protection schedules from the same site registry: one sorted by C_r and one by local discrepancy. Define B_site, with B equivalent to B_site everywhere in this method, as the nonnegative integer high-precision-site count recorded by the matched local-fidelity baseline in the preregistered run manifest. Protect exactly the top-B_site sites, leave the rest at b_lo, resolve ties by registry order, and keep the primary comparison as a site-count budget. Record persistent weight bytes, peak live activation workspace, scale/copy overhead, and measured latency in a separate deployment-cost ledger. The numeric B_site manifest entry remains open.【author decision: supply the nonnegative integer baseline site count and halt if it is missing or exceeds the registry size.】 Deployment bytes, activation workspace, scale/copy overhead, and latency remain a separate measurement protocol.【author decision: either provide that packing and timing protocol or mark those outcomes out of scope.】",
      "what_to_do_en": "Construct two deterministic protection schedules from the same site registry: one sorted by C_r and one by local discrepancy. Define B_site, with B equivalent to B_site everywhere in this method, as the nonnegative integer high-precision-site count recorded by the matched local-fidelity baseline in the preregistered run manifest. Sort descending and break ties by registry order, protect exactly the top-B_site sites at FP16, and leave all other target sites at b_lo. The numeric B_site and its manifest entry remain open inputs; halt if the entry is missing or B_site exceeds the registered site count. Record persistent weight bytes including copies and scales, peak live activation workspace under the real kernel, scale/copy overhead, and measured latency separately; these quantities are not substituted for the site-count budget. The numeric B_site manifest entry remains open.【author decision: supply the nonnegative integer baseline site count and halt if it is missing or exceeds the registry size.】 Deployment bytes, activation workspace, scale/copy overhead, and latency remain a separate measurement protocol.【author decision: either provide that packing and timing protocol or mark those outcomes out of scope.】",
      "what_to_do_zh": "使用同一个 site registry 建立两份 deterministic protection schedule：一份按 C_r 排序，另一份按 local discrepancy 排序。将 B_site 定义为 matched local-fidelity baseline 在 preregistered run manifest 中记录的 nonnegative integer high-precision-site count，并在本方法中固定 B 等价于 B_site。按 score 降序排列，ties 按 registry order 处理；把恰好 top-B_site sites 保持为 FP16，其余 target sites 保持为 b_lo。B_site 的数值和 manifest entry 仍是 open inputs；如果 entry 缺失或 B_site 超过 registry 中的 site 数量，就 halt。persistent weight bytes（包括 copies 与 scales）、real kernel 下的 peak live activation workspace、scale/copy overhead 与 measured latency 必须另外记录，不能替代 site-count budget。 numeric B_site manifest entry 仍是 open。【作者需决定：提供 nonnegative integer baseline site count；缺失或超过 registry size 时 halt。】deployment bytes、activation workspace、scale/copy overhead 与 latency 仍需独立 measurement protocol。【作者需决定：提供 packing/timing protocol，或明确把这些 outcome 标为 out of scope。】"
    },
    {
      "step_id": "S5",
      "what_changes": "Use the immutable S1 manifest to run idm with sigma_shift_FW=1.0 and infer_action on the named held-out RoboTwin 2.0/LIBERO manipulation split. Hold the declared b_lo, B, B_site, preprocessing, reset protocol, controller restoration, and branch order fixed across the two schedules. Enumerate the held-out task IDs, episode and initial-state IDs, seeds, split membership, contact-transition predicate from simulator events, and task success/failure rule before inspecting results. 【author decision: supply the exact split and contact/failure definitions, including whether a contact transition is required before the first action or may occur after it.】 For each paired episode, restore the same initial state for both schedules, run the declared evaluation horizon, and record task outcome and the next-observation deviation with its aggregation window. 【author decision: pin the idm and infer_action entrypoint revisions and arguments, action horizon, termination rule, and whether next-observation deviation is first-transition-only or later-transition/full-episode; keep any latter summary separate from C_r.】 Build the permutation control by reassigning the recorded C_r values across the same site IDs while preserving the score multiset, B, B_site, tie-breaking, and all other schedule rules. Use the specified oracle simulator-state branch only as a labeled positive control. 【author decision: specify the permutation seed and replicate/list, the oracle state source, the branch insertion point, and the comparison it controls; do not use it as a substitute for the real association.】 Interpret the result narrowly: C_r remains a one-control-transition ranking proxy, while task failures and any full-horizon summaries are separate downstream outcomes rather than task loss or a full-horizon/replan marginal. QuantWM and QuantWAMs retain their broader WAM quantization boundaries; VAML, Model Advantage / Value-Aware Model Learning, Deep Task-Based Quantization, and SQIL retain prior-art ownership of broad consequence-, value-, task-, or critical-state-protection principles.",
      "what_to_do_en": "Use the immutable S1 manifest to run the idm entrypoint with sigma_shift_FW=1.0 and infer_action on the named held-out RoboTwin 2.0/LIBERO manipulation split. Use the same declared b_lo, B, B_site, preprocessing, reset procedure, controller restoration, and branch order for the two schedules. List the held-out task identifiers, episode and initial-state identifiers, seeds, split membership, simulator-event contact predicate, and task success/failure rule before inspecting results. 【author decision: provide the exact split and contact/failure definitions, including whether contact must occur before the first action or may occur after it.】 For each paired episode, restore the same initial state for both schedules, run the declared evaluation horizon, and record task outcome and next-observation deviation with its aggregation window. 【author decision: pin the idm and infer_action entrypoint revisions and arguments, action horizon, termination rule, and whether next-observation deviation is first-transition-only or later-transition/full-episode; keep any later summary separate from C_r.】 Create the permutation control by reassigning the recorded C_r values across the same site identifiers while preserving the score multiset, B, B_site, tie-breaking, and every other schedule rule. Run the specified oracle simulator-state branch only as a labeled positive-control branch. 【author decision: specify the permutation seed and replicate/list, oracle state source, branch insertion point, and controlled comparison; do not use the oracle as a substitute for the real association.】 Keep the interpretation bounded: C_r is a one-control-transition ranking proxy, while task failures and full-horizon summaries are separate downstream outcomes, not task loss or a full-horizon/replan marginal. QuantWM and QuantWAMs retain their broader world action model (WAM) quantization boundaries; VAML, Model Advantage / Value-Aware Model Learning, Deep Task-Based Quantization, and SQIL retain prior-art ownership of broad consequence-, value-, task-, or critical-state-protection principles.",
      "what_to_do_zh": "使用不可变的 S1 manifest，在命名的 held-out RoboTwin 2.0/LIBERO manipulation split 上运行带有 sigma_shift_FW=1.0 的 idm entrypoint 与 infer_action。 对两份 schedule 使用相同的声明性 b_lo、B、B_site、preprocessing、reset procedure、controller restoration 与 branch order。 在查看结果之前，列出 held-out task identifiers、episode 与 initial-state identifiers、seeds、split membership、来自 simulator events 的 contact predicate，以及 task success/failure rule。 【作者需决定：提供 exact split 与 contact/failure definitions，包括 contact 是否必须在第一个 action 之前发生，还是允许在之后发生。】 对每个 paired episode，为两份 schedule 恢复同一个 initial state，运行声明的 evaluation horizon，并记录 task outcome 与带有 aggregation window 的 next-observation deviation。 【作者需决定：固定 idm 与 infer_action entrypoint revisions 和 arguments、action horizon、termination rule，并决定 next-observation deviation 只看 first transition，还是覆盖 later transition/full episode；后者必须与 C_r 分开汇总。】 建立 permutation control：在相同 site identifiers 之间重新分配已记录的 C_r values，同时保持 score multiset、B、B_site、tie-breaking 与其他 schedule rules 不变。 指定的 oracle simulator-state branch 只能作为标注清楚的 positive-control branch 运行。 【作者需决定：指定 permutation seed 与 replicate/list、oracle state source、branch insertion point 以及它所控制的 comparison；不能用 oracle 替代真实 association。】 保持解释边界：C_r 仍是 one-control-transition ranking proxy；task failures 与 full-horizon summaries 是独立的 downstream outcomes，不是 task loss，也不是 full-horizon/replan marginal。 QuantWM 与 QuantWAMs 继续保留其更广的 world action model (WAM) quantization boundaries；VAML、Model Advantage / Value-Aware Model Learning、Deep Task-Based Quantization 与 SQIL 继续代表 broad consequence、value-aware、task-based 或 critical-state-protection principles 的 prior-art boundary。"
    }
  ]
}
