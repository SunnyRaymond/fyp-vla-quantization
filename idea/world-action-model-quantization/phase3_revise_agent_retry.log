Reading additional input from stdin...
2026-09-08T13:32:38.700161Z  WARN codex_skills::interface: ignoring interface.icon_small: icon path with '..' must resolve under plugin assets/
2026-09-08T13:32:38.700188Z  WARN codex_skills::interface: ignoring interface.icon_large: icon path with '..' must resolve under plugin assets/
2026-09-08T13:32:38.709550Z  WARN codex_core::shell_snapshot: Failed to create shell snapshot for powershell: Shell snapshot not supported yet for PowerShell
OpenAI Codex v0.153.4
--------
workdir: D:\Downloads\Final Year Project
model: gpt-5.6-luna
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR]
reasoning effort: max
reasoning summaries: none
session id: 01a08138-7f89-75f2-94b7-c1c965803a89
--------
user
You are a fresh isolated sub-agent for ResearchStudio-Idea Phase 3.3 revision. Read revise.txt first, then read the candidate, selection, and revision brief paths below. Also read the actual host review at D:\Downloads\Final Year Project\ideaspark_run\INITIAL_CANDIDATE_REVIEW.md. Apply only the critique revision targets; do not echo the candidate and do not redesign the mechanism or modify guarded fields. Write a patch-only JSON with applied_revisions[] directly to the exact output path. Preserve the candidate as a coupled future-video/action WAM numerical weight/activation single-site FP16-background, one-control-transition proxy; do not silently upgrade C_r into task loss, full-horizon/replan marginal gain, joint-low-bit marginal, or bytes/latency objective. Explicitly include claim-bounded comparisons to QuantWM, QuantWAMs, VAML, Model Advantage / Value-Aware Model Learning, Deep Task-Based Quantization, and SQIL, making clear broad downstream-consequence targeting and critical-state protection are prior art. Add executable controller/RNG/history/cache save-clone-restore/equality-check semantics without changing the first-action environment estimand. Keep the exact Optional IDM checkpoint identity as a required pre-run manifest/halt condition and retain the 80GB-class/A100-40GB measurement boundary. Do not browse or invent identity, measurements, or citations. Use direct file writes, then return at most 250 words with output path and completion signal.

SYSTEM PROMPT:
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\references\system-prompts\revise.txt

INPUTS:
D:\Downloads\Final Year Project\ideaspark_run\world-action-model-quantization\phase2_coherence\refined_candidate.json
D:\Downloads\Final Year Project\ideaspark_run\world-action-model-quantization\phase2_select\phase2_select_output.json
D:\Downloads\Final Year Project\ideaspark_run\world-action-model-quantization\phase3_revise\revise_brief.json
D:\Downloads\Final Year Project\ideaspark_run\world-action-model-quantization\phase3_critique\phase3_critique_output.json
D:\Downloads\Final Year Project\ideaspark_run\world-action-model-quantization\host_supplemental_constraints.md
D:\Downloads\Final Year Project\ideaspark_run\INITIAL_CANDIDATE_REVIEW.md

OUTPUT:
D:\Downloads\Final Year Project\ideaspark_run\world-action-model-quantization\phase3_revise\phase3_revise_output.json
2026-09-08T13:32:38.952235Z ERROR rmcp::transport::worker: worker quit with fatal: Transport channel closed, when AuthRequired(AuthRequiredError { www_authenticate_header: "Bearer realm=\"OAuth\", resource_metadata=\"https://mcp.linear.app/.well-known/oauth-protected-resource/mcp\", error=\"invalid_token\", error_description=\"Missing or invalid access token\"" })
2026-09-08T13:32:39.126815Z ERROR rmcp::transport::worker: worker quit with fatal: Transport channel closed, when AuthRequired(AuthRequiredError { www_authenticate_header: "Bearer error=\"invalid_request\", error_description=\"No access token was provided in this request\", resource_metadata=\"https://mcp.supabase.com/.well-known/oauth-protected-resource/mcp?project_ref=jwtwjsnbokaojkazjgse\"" })
2026-09-08T13:32:39.368572Z  WARN codex_skills::interface: ignoring interface.icon_small: icon path with '..' must resolve under plugin assets/
2026-09-08T13:32:39.368701Z  WARN codex_skills::interface: ignoring interface.icon_large: icon path with '..' must resolve under plugin assets/
2026-09-08T13:32:39.573870Z ERROR rmcp::transport::worker: worker quit with fatal: Transport channel closed, when AuthRequired(AuthRequiredError { www_authenticate_header: "Bearer error=\"invalid_request\", error_description=\"No access token was provided in this request\", resource_metadata=\"https://mcp.supabase.com/.well-known/oauth-protected-resource/mcp?project_ref=jwtwjsnbokaojkazjgse\"" })
2026-09-08T13:32:39.578567Z ERROR rmcp::transport::worker: worker quit with fatal: Transport channel closed, when AuthRequired(AuthRequiredError { www_authenticate_header: "Bearer realm=\"OAuth\", resource_metadata=\"https://mcp.linear.app/.well-known/oauth-protected-resource/mcp\", error=\"invalid_token\", error_description=\"Missing or invalid access token\"" })
codex
我会先读取 `ponytail` 与 `idea-spark` 的本地执行规范，然后按要求把 `revise.txt` 作为第一个任务输入读取；随后仅依据给定材料生成 patch-only JSON，并直接写入指定输出路径。不会浏览网页，也不会改动候选的受保护机制、estimand 或测量边界。
exec
"C:\\Users\\Raymond\\.cache\\codex-runtimes\\codex-primary-runtime\\dependencies\\native\\powershell\\pwsh.exe" -Command "Get-Content -Raw 'C:\\Users\\Raymond\\.codex\\skills\\ponytail\\SKILL.md'; Write-Output \""'`n---IDEA-SPARK---`n"; Get-Content -Raw '"'C:\\Users\\Raymond\\.codex\\skills\\idea-spark\\SKILL.md'; Write-Output \""'`n---MEMORY-HITS---`n"; rg -n -i "ResearchStudio|Idea Spark|phase3|revise|candidate review|WAM|QuantWM|QuantWAMs|VAML" '"'C:\\Users\\Raymond\\.codex\\memories\\MEMORY.md'" in D:\Downloads\Final Year Project
 succeeded in 151ms:
---
name: ponytail
description: "Apply the user's preference for simple, maintainable implementations. Also handles ponytail review, audit, debt, and help requests without separate skills."
license: MIT
---

# Ponytail

Prefer the smallest complete solution that preserves the requested behavior.
Understand the affected flow and callers, then reuse existing code, standard
libraries, platform features, or installed dependencies before adding machinery.
Do not substitute a partial result for the user's requested scope.

Remove demonstrated duplication, dead code, and speculative abstractions.
Do not use line count alone to judge simplicity. Preserve security, accessibility,
data integrity, useful error handling, and necessary validation. Fix root causes;
verify the affected behavior with checks proportional to the change.

Follow the user's language and requested explanation depth. Do not impose a
persona, fixed answer length, automatic mode escalation, or unrelated persistence.
If the user says stop ponytail or normal mode, stop applying this preference.

## Optional requests

- Review: inspect the requested diff for unnecessary complexity. Give concrete
  locations, evidence, and simpler replacements; do not apply edits unless asked.
- Audit: apply the same review across the requested repository or installation.
- Debt: use `rg` to find actual `ponytail:` comments, excluding generated files.
  Report the recorded limitation and revisit trigger; do not invent missing ones.
- Help: explain these options briefly. Legacy ponytail-review, ponytail-audit,
  ponytail-debt, ponytail-help, and ponytail-gain phrases are understood here;
  separate skill files are no longer required.
- Gain: report only observed before/after measurements from a valid baseline.
  Do not reuse other models' historical benchmark claims as GPT-6 measurements.

Use a `ponytail:` comment only for a real deferred limitation worth tracking.
No benchmark, debt ledger, extra test suite, or new artifact is required merely
because this skill is active.


---IDEA-SPARK---

---
name: idea-spark
description: "Run or resume the corpus-driven Idea Spark pipeline: pattern-card retrieval, staged research-idea generation, falsification audits, and persistent run artifacts. Use when this structured pipeline is wanted; ordinary brainstorming, paper summaries, and standalone novelty checks do not require it."
---

# Idea Spark Skill

Local routing: this is the full staged pipeline, not a prerequisite for ordinary
research reasoning. Its historical corpus supplies patterns, not evidence of
current novelty. Keep current primary-source retrieval and all run-state gates
when this pipeline is selected. Preserve the current model unless the user
requests another; no external model transport is implied by selecting this skill.


Convert an under-specified research direction into ONE reviewer-defensible Oral-level research proposal — grounded in 1947 ICLR/ICML/NeurIPS papers (2021-2025) — via a 5-phase workflow: retrieve recent literature, diagnose the bottleneck, select + generate a candidate using corpus-derived ideation pattern cards, run it through a quality gauntlet, expand into an idea card.

This file is the operational runbook. Design rationale (the 7 design principles, why each contract is shaped this way, removed-check history) lives in [references/design-notes.md](references/design-notes.md) — read it only when modifying or evaluating the skill, never needed to run it. When MODIFYING the skill, also replay the cross-shape regression set in [references/regression-directions.md](references/regression-directions.md) (deterministic subset: `python3 "$SKILL_DIR/scripts/regression_check.py" <run_dir>`; routing/merger branch fixtures — including every guard/retry branch that real runs rarely exercise: `python3 "$SKILL_DIR/scripts/selftest_routing.py"`; unit fixtures for the helpers every phase sits on — the multi-query round-robin and the tolerant LLM-JSON loader: `python3 "$SKILL_DIR/scripts/selftest_units.py"`). First-time installation lives in [references/setup.md](references/setup.md).

## When to use

- "Give me a research idea in {area} I could pursue." / "What's the most impactful next step in this direction?"
- "Help me sharpen this vague direction into an Oral-level proposal."
- "What's the bottleneck of this problem?" / "Run a novelty audit on this idea."

## When NOT to use

- Code review, debugging, refactoring. Summarizing one paper. Cross-decade survey writing.
- Free-association brainstorming with no research context. Engineering integration tasks ("ship this feature in our system").
- Pure benchmark / dataset construction work — the 15-pattern vocabulary handles benchmark *audit* (controlled_diagnostic_design) but not benchmark *construction*.

## Setup (first use only)

Follow [references/setup.md](references/setup.md). Quick version — set two shell variables, install deps, verify:

```bash
SKILL_DIR=<path to this folder>                  # e.g. ~/.claude/skills/idea-spark (Claude Code), ~/.codex/skills/idea_spark (Codex CLI), or any clone location
RUN_DIR="$PWD/ideaspark_run/<topic-slug>"   # convention below; any absolute dir works. No mkdir needed — every phase mkdir -p's its own --out, and `next` treats a missing dir as a fresh run
python3 -m pip install feedparser openreview-py beautifulsoup4 pymupdf
python3 "$SKILL_DIR/scripts/run.py" check_connectors   # from the SAME shell you'll run phases from
```

Credentials go in `.env` (OpenReview user/pass + Semantic Scholar key — see setup.md); the orchestrator auto-loads it. Optional: `xelatex`/`tectonic` for PDF cards.

---

## How to run: the `next` loop

The canonical way to drive a run is the **run-state navigator**:

```bash
python3 "$SKILL_DIR/scripts/run.py" next --dir "$RUN_DIR" --query "<user's research question>"
```

**Run-dir convention (one run = one directory, named by the host BEFORE the first command):** `$PWD/ideaspark_run/<topic-slug>` — a short kebab-case slug distilled from the user's direction (e.g. `ideaspark_run/diffusion-watermark`); if the slug is taken, append `_2`, not a timestamp. NEVER reuse a directory that already contains a `phase0/` — every phase writes into `$RUN_DIR` and would clobber the prior run. The skill itself never names the dir (any absolute path works); this convention exists so runs from different harnesses land in one predictable place instead of each agent improvising.

`next` inspects the artifacts already on disk and prints EXACTLY one next step — either a Bash command to run verbatim, or an LLM sub-agent spec (system-prompt path + input file paths + output path + the routing signal to report back). It is read-only and idempotent (safe to re-run anytime, including to resume an interrupted run) — including on a run dir that does not exist yet, which it reports as a fresh run and answers with the Phase 0 step rather than an error. You never need to create the directory before calling it. The host loop is:

1. Run `next`.
2. Do what it says (`bash` → run the command; `llm_subagent` → execute in an ISOLATED context per the Context discipline rules below).
3. Run `next` again. Repeat until it reports a terminal state (`DONE`, `do_not_generate`, or `phase_3_failed`).

**Consume every emit WHOLE — never grep/filter/truncate the block.** INPUT lists span unlabeled continuation lines; a label-keyed grep (`grep -E "STEP|INPUT|..."`) silently drops them. This exact failure occurred in a live run: the navigator listed the coherence gate's blocking findings as an audit input, the host's grep dropped the line, and the audit issued a false `advance` without ever seeing the executed evidence. If the emit must be captured into a bounded tool result, `head -c 4000` the WHOLE block — never filter by line labels.

`next` encodes the full phase graph — the mandatory full-text gate, the citation gate, the abandon-retry branch, the falsification re-audit branch, and the correct Phase 4 flags per path — so you do not need to memorize the reference tables below; they exist for deviation and debugging.

If your host exposes a task/todo tool (e.g., TodoWrite), seed it with this checklist and tick phases as `next` moves past them:

```
- [ ] Phase 0: Literature grounding → lit_table.md, then Phase 0+ full-text fetch (MANDATORY — Phase 1 hard-gates on it)
- [ ] Phase 1: Bottleneck identification → phase1_output.json (routing: proceed | do_not_generate)
- [ ] Phase 2: Gap×pattern selection + candidate generation (ONE isolated context, TWO output files) → citation gate → coherence gate (dry-run trace, fresh context)
- [ ] Phase 3: Collision retrieval (signature@10mo + alias@48mo — launchable in parallel with 2.3) → audit (5 checks) → [revise → merge → re-audit if falsification rewritten] | [abandon → information-gain retry: regenerate while each failure yields NEW binding lessons, ≤3 candidate cycles; repeated subsumption lesson → 1 bottleneck re-diagnosis + 1 attempt; no new information or cap → phase_3_failed]
- [ ] Phase 4: skeleton → fill (technical) → assemble partial → derive (plain, fast-tier) → assemble final + method view → implementability audit → validate → render → return 3 cards inline
```

Three outcomes per run: the rendered idea-card markdown returned inline (LaTeX + per-phase JSON left under `$RUN_DIR`), a `do_not_generate.md` (Phase 1 OOD), or a `phase_3_failed.md` (audit abandons twice). **Never ask the user mid-flow** — missing intake fields are inferred; revision, falsification re-audit, and the single internal retry all run without user re-invocation.

### Invocation contract

**No `cd` is required.** `scripts/run.py` self-locates its skill root, so every orchestrator command can be invoked from ANY working directory by absolute script path: `python3 "$SKILL_DIR/scripts/run.py" <subcommand> --out "$RUN_DIR/<phase>/" ...`. The legacy form `cd "$SKILL_DIR" && python3 -m scripts.run <subcommand> ...` works identically. Do NOT use relative script or `--out` paths — CWD is not stable across host-LLM Bash invocations, and the orchestrator rejects a relative `--out` outright.

**Exit codes 10 and 11 are NOT errors — they are sentinel handshakes.** When the orchestrator can't call an LLM itself (no `NOVELTY_LLM_CLASSIFY_FAST_CMD`), it writes a sentinel JSON describing what the host LLM should do, then exits rc=10 (intent / pattern-summary) or rc=11 (signature_terms). Read the sentinel (`$RUN_DIR/<phase>/.<step>_pending`), read the file at its `rubric_file` field (absolute path), produce the expected output, re-invoke per its `re_invocation` field. Do not stop on these codes. (The default Phase 0 flow below avoids the rc=10 intent sentinel entirely by passing `--queries` up front.)

### Context discipline (read BEFORE running any LLM-driven phase)

A full run accumulates ~180-250k tokens of intermediate state. If the host LLM carries that in its own conversation context across phases, the Phase 1 / 2.2 / 4.fill calls routinely hit the backend request timeout (`[API Error · Request timed out · Retrying...]`) and the retry times out again. Apply ALL three rules on every run:

**Rule 1 — Run every LLM-driven phase in an ISOLATED context.** Phases 1 / 2 (2.1+2.2) / 2.3 / 3.2 / 3.3 / 4.fill / 4.1.5 each have file-path inputs and one JSON output; no phase needs the conversation that produced an earlier one. Use the FIRST isolation mechanism your harness supports:

- **(a) Subprocess LLM** — set `NOVELTY_LLM_REASONING_LARGE_CMD` / `NOVELTY_LLM_CLASSIFY_FAST_CMD` (see § Configuration); each phase runs as its own subprocess, fresh context by construction, on any harness.
- **(b) Sub-agent tool** (Claude Code `Agent` or equivalent) — spawn one per phase, passing ONLY the file paths the phase prompt lists — not conversation history, not file contents inline. The sub-agent reads from disk, `Write`s to disk, returns ≤ 250 words (output path + routing signal). Exception by design: Phase 2.1 and 2.2 run in ONE sub-agent writing both output files — both are generation-side; the adversarial separations (3.2 vs 3.3, 4.fill vs 4.1.5) must stay separate calls.
- **(c) Manual context reset** — run inline but clear/compact at the four points in Rule 3.

Whichever mechanism, the parent context stays ≤ ~30k tokens for the whole run because it never holds a phase's structured output.

**Rule 2 — `Write` every phase artifact directly to disk; never paraphrase it into chat.** Output convention: `$RUN_DIR/<phase>/<phase>_output.json`. Use your harness's file-write tool (Claude Code: `Write`) — no Bash heredocs (permission prompts + silent truncation), no `echo`, no pasting JSON into replies. Bound tool-result captures from large files to ≤ 4 KB (`head -c 4000` / `jq` / `sed`); never `Read` a >10 KB intermediate dump into the parent context — the dump gets cached into every subsequent turn (this exact anti-pattern caused prior timeout runs).

**Rule 3 — Compact between phases.** Natural compact points: after Phase 0+, after Phase 1, after Phase 2, after Phase 3.2. Every phase re-reads its disk inputs, so compacting loses nothing. With `/compact`, use it there; Rule 1 mechanisms (a)/(b) achieve the same on their own.

**Diagnostic for "Request timed out" mid-phase:** inspect your harness's session transcript/log (Claude Code: `~/.claude/projects/<project-slug>/<session-id>.jsonl`, look for `isApiErrorMessage: true`; other harnesses: their session-log equivalent); the prior tool call shows which prompt got too big. The fix is one of the three rules — usually Rule 1.

---

## Phase reference

`next` prints each of these steps at the right moment with concrete paths; the tables below are the full contract for deviation/debugging.

### Orchestrator entry points

| Phase | Entry point (`python3 "$SKILL_DIR/scripts/run.py" ...`, any CWD) |
|---|---|
| navigator | `next --dir "$RUN_DIR" [--query "..."]` |
| Phase 0 | `phase0 --query "<user text>" --queries "q1\|q2\|q3\|q4" [--named-papers "Title A\|Title B"] --out $RUN_DIR/phase0/` |
| user-ref registration (title-named anchor papers; BEFORE phase0_fulltext) | `add_user_ref --out $RUN_DIR/phase0/ --title "<full title>" [--raw-match "<user phrasing>"] [--id <arxiv/DOI/URL>]` |
| relevance partition apply (Phase 0.4; archives off_topic + stamps core/adjacent, BEFORE tagging) | `apply_partition --out $RUN_DIR/phase0/ --partition <relevance_partition.json>` |
| host-ref resolution (Phase 0.5 coverage check; verifies + merges host-nominated missing papers) | `add_host_refs --out $RUN_DIR/phase0/ --refs <noms.json>` |
| Phase 0+ full-text (**mandatory**, the moment lit_table.md lands) | `phase0_fulltext --out $RUN_DIR/phase0/` |
| Phase 1 anchor top-up (optional, when the #1 closest_adjacent fell outside the fulltext pool **or came back method-thin**) | `phase1_fulltext_topup --out $RUN_DIR/phase0/ --paper-id <anchor paper_id> [--min-method-chars 4000]` |
| Phase 2 prep (deterministic; `next` emits it with the Phase 2 step) | `phase2_prepare --dir $RUN_DIR` |
| lit_table shard assembly (deterministic; after parallel pattern tagging) | `lit_table_merge --out $RUN_DIR/phase0/ --shards <rows1.md> <rows2.md> ...` |
| Phase 3.1 collision | `phase3_collision --idea-json <canonical candidate> --out $RUN_DIR/phase3_collision/` |
| Phase 3.3 merger | `phase3_merge_revisions --phase2 <canonical candidate> --revisions <p3.3-patch> --critique <p3.2-report> --out $RUN_DIR/phase3_revise/` |
| Phase 2.3 merger (same tool; only when coherence verdict=patched) | `phase3_merge_revisions --phase2 <p2.2-output> --revisions <p2.3-output> --out $RUN_DIR/phase2_coherence/ --out-name refined_candidate.json` |
| Phase 4 skeleton | `phase4_skeleton --candidate <final_candidate-or-p2.2> --phase1 ... --phase2-select ... --phase3-critique ... [--phase3-revise ...] --phase0-dir $RUN_DIR/phase0/ [--collision ...] --out $RUN_DIR/phase4/` |
| Phase 4 assemble (repeat `--fill-map` to merge the derive_map on the final pass) | `phase4_assemble --skeleton $RUN_DIR/phase4/phase4_skeleton.json --fill-map $RUN_DIR/phase4/fill_map.json [--fill-map $RUN_DIR/phase4/derive_map.json] --out $RUN_DIR/phase4/` |
| Phase 4 method view (before 4.1.5; `next` bundles it with the final assemble) | `phase4_method_view --expansion $RUN_DIR/phase4/phase4_expansion.json --out $RUN_DIR/phase4/` |
| Phase 4 render | `phase4_render --expansion $RUN_DIR/phase4/phase4_expansion.json --out $RUN_DIR/phase4/` |
| Validators | `validate --phase2 ... [--phase3 ...] [--phase4 ...] [--phase4-impl ...]` |

The LLM-driven phases (1 / 2.1 / 2.2 / 2.3 / 3.2 / 3.3 / 4.fill / 4.1.5 / falsification re-audit) have no orchestrator subcommand (a `cat prompt | llm` wrapper would add fragility without determinism): read the prompt at `references/system-prompts/<phase>.txt`, gather the inputs listed at its top, `Write` the JSON described under `Output:` to `$RUN_DIR/<phase>/<phase>_output.json`. Run each under the Context discipline rules — Phase 4.fill is the largest output and the most timeout-prone; never in the parent context.

### Phase 0 — Literature grounding

Phase 0 and 3.1 require **real external retrieval** via the bundled connector scripts (`scripts/search_*.py`) — never WebSearch or ad-hoc fetch (downstream phases reject unstructured output). Gate sentinel: `.lit_grounding_mode` = `real` vs `connector_failure` (halt with diagnostic; `--allow-webfallback` exists as a flagged, lower-confidence escape).

**Default flow (skips one sentinel round-trip):** BEFORE invoking `phase0`, read `references/intent-recognition.md` (Map mode) yourself and produce **4** search queries (3-5 only with a stated reason) — including one ESCAPE-MECHANISM query phrased in solution vocabulary (recalls papers that already fixed the bottleneck and title themselves by their fix; problem-keyed queries miss exactly those), and apply that file's two query tests — VOCABULARY-OWNERSHIP and CONCRETE-OBJECT — to EVERY query, not only the escape one; its count rule gates a 5th query on quality per slot, not on the count. Phrase queries mechanism-first: generic "`<topic>` challenges/overview/landscape" phrasings are survey magnets that dilute corpus density — the orchestrator demotes survey-titled hits to the bottom of lit_results (kept, never dropped), but each connector's cap slots are still spent on them. Also apply the OOD short-circuit (intake-routing.md triggers #1 Too-broad / #2 No-anchor → route to do_not_generate instead of retrieving). Then invoke with BOTH flags:

```bash
python3 "$SKILL_DIR/scripts/run.py" phase0 --query "<user's research question>" --queries "q1|q2|q3|q4" --out $RUN_DIR/phase0/
```

The rc=10 sentinel path still exists as fallback when `--queries` is omitted. The orchestrator: asserts a sane clock; runs a list of retrieval JOBS (a connector may run more than one window) and merges them SS-priority. Default jobs: arxiv 0-6mo cap 40 · **ss_recent 0-6mo cap 30** (the freshness window is covered by TWO engines — arXiv's weak lexical API AND SS's stronger recent search — because gap freshness lives here and one retriever's ranking is a single point of failure) · openalex 6-24mo cap 30 published-only · semanticscholar 6-24mo cap 30 published-only · openreview 0-6mo cap 10 in-review · oa_recent 0-6mo **cap 0 (off by default)**. Caps are **wide on purpose** — narrower ones saturated, and the Phase 0.4 partition (below) drops the extra noise before it reaches the gap corpus (design-notes). ~140 pre-dedup, ~110-130 after dedup, ~50-75 after the partition drops off_topic. Window OVERLAP is intentional now (a second engine re-covers the freshness window; SS-first dedup collapses duplicates and enriches arXiv records with externalIds) — the old non-overlapping rule is retired. All caps are overridable per job via `IDEASPARK_POOL` (see § Configuration). Pass `--named-papers` whenever the query name-drops a paper or system without a link: the URL/ID regex only sees links, so a bare name is otherwise invisible to the U fetch tier and never becomes an anchor the candidate is differentiated against (add one later with `add_user_ref`). Each name is resolved against the connectors by CONTAINMENT — the paper's title must lead with the name, since a user writes the nickname and not the title — and a name two papers lead with is reported AMBIGUOUS and left unresolved rather than guessed, because a wrong U-tier entry is uncapped, always deep-read and becomes an anchor. Pass that one as a URL instead. It writes the user's question verbatim to `phase0/user_query.txt` — the 0.4 partition, the 0.5 coverage check, Phase 1 and Phase 2.1 are all pointed at that FILE rather than at a string the host retypes, so a paraphrase cannot silently propagate and a compacted run can still recover what was asked. It also extracts URL/ID user-refs from the query into `phase0/user_refs.json`; emits `.pattern_summary_pending` for the host.

**Phase 0.4 relevance partition (host precision gate; DEFAULT ON, disable with `IDEASPARK_RELEVANCE_PARTITION=off`):** because retrieval now over-fetches, the host — in ONE compact pass over every record's title+abstract — labels each `core | adjacent | off_topic`, writing `phase0/relevance_partition.json` `[{paper_id, relevance, reason}]`. Use YOUR OWN model (open-ended relevance judgment — do NOT downgrade); be CONSERVATIVE — when unsure between core and adjacent pick core, hard-label off_topic ONLY for clear cross-domain false positives (broad "memory-augmented" matches from wireless/recommendation/NLP), pure surveys, or unrelated fields (a wrong off_topic is an unrecoverable recall loss). Then `apply_partition --out phase0/ --partition <file>` (deterministic) stamps `relevance` on each record, **archives off_topic to `off_topic.md` and drops it from `lit_results.json`**, and touches `.partition_applied`, and writes **`query_yield.md`** — the Phase 0.4 labels joined back onto the `from_query` provenance the connectors stamp, i.e. each query's core/adjacent/off_topic yield. That report is how the query-set design question stops being a judgement call: a query whose share is mostly off_topic is spending guaranteed round-robin slots on noise and should be dropped or rephrased next run (see the VOCABULARY-OWNERSHIP TEST in references/intent-recognition.md). Papers reachable from several queries are credited to each. This runs BEFORE tagging so the per-paper pattern-tag pass only sees survivors, and it **replaces the weak Haiku `outside_taxonomy` tag** as the precision gate. `core` alone feeds the deep-read pool; `adjacent` stays as a citeable baseline/backbone but never consumes a fetch slot. `next` drives the sub-flow; the marker guards re-runs.

Retrieval takes 3-10 min (the openreview connector alone budgets 600s) — set your Bash timeout ≥ 600s or run it in the background.

**Pattern tagging (host step):** classify each `lit_results.json` paper per `references/pattern-summary-rubric.md` into 1-3 of the 15 patterns → write `lit_table.md` with columns `paper_id | year_month | venue | title | ideation pattern tags | bottleneck this paper targets | open issue / unresolved gap | resolves_problem | retrieved_via`. Pure classification — it does not need the large reasoning model: route it to a cheaper/faster model tier or a lower reasoning effort BY DEFAULT (the same tier `NOVELTY_LLM_CLASSIFY_FAST_CMD` names in § Configuration; spending the large model here is pure waste); only when no cheaper tier exists, run it isolated on the host model. Rows are per-paper independent, so for 40+ papers the tagging MAY be sharded across 2-3 parallel fast-tier sub-agents (contiguous slices; assemble with the deterministic `lit_table_merge --out $RUN_DIR/phase0/ --shards <row-file>...` — it validates 9-column shape, row count == paper count, and paper_id coverage against lit_results.json, replacing hand-checks) — wall-clock win, no quality delta.

**Phase 0.5 coverage check (host-recall channel, host step; DEFAULT ON, disable with `IDEASPARK_COVERAGE_CHECK=off`):** after tagging, before fulltext, the host — having read the whole table — names up to 8 clearly load-bearing works the retrieval pool MISSED, writing `phase0/host_refs_nominations.json` `[{title, id_hint?, why, source: parametric|websearch}]`. It PRIORITIZES the last ~12 months (recent/frontier work the dated retrieval windows under-sampled — the target that keeps the diagnosed gap current); older foundational papers (canonical base policies, >12-month landmarks) are mostly Phase 1 lineage's job and should be nominated only as a small minority, and only when a load-bearing backbone/baseline the candidate builds on or is measured against. Use YOUR OWN model here (open-ended intent judgment — do NOT downgrade to a cheap tier); WebSearch is allowed ONLY in this step and ONLY to find titles, never to fabricate a record; an empty `[]` is a valid honest output. Then `add_host_refs --out phase0/ --refs <noms>` (deterministic) VERIFIES each nomination via the SS/arXiv connectors (>=0.9 title match) — verified records merge into `lit_results.json` with `retrieved_via=host_recall|host_web`; unresolvable titles (likely hallucinations) are NOT admitted, they land in `host_refs_unresolved.md`; admitted ids are recorded in `host_refs.json` for provenance. Newly-admitted rows get tagged (fast tier) and `lit_table_merge`d in; `.coverage_check_done` guards against re-running. This keeps `lit_grounding_mode` honestly `real` (every record is connector-verified) while letting host knowledge repair a thin or stale pool. `next` drives the whole sub-flow. Admitted refs feed the fulltext H tier (below).

**Title-named user refs:** if the user query names anchor papers by TITLE ("based on the LoRA paper" — anything the URL/ID regex can't catch), register each BEFORE `phase0_fulltext` via `add_user_ref` (entry-point table). It does a deterministic dedup-merge into `user_refs.json` — do NOT hand-edit that file (some harnesses' file-write tools refuse to overwrite a file that was never read, and a malformed edit silently drops the U fetch tier).

**Phase 0+ full-text fetch — MANDATORY.** The instant `lit_table.md` lands (and after the coverage check), run `phase0_fulltext` (entry-point table) before touching Phase 1. Pool = U (user refs, never capped) + **H (host-recall refs, retrieved_via host_*, cap 6)** + T2 (published on-topic, cap 10) + T3 (arxiv recent on-topic, cap 15; also absorbs unused H/T2 slots), ceiling **25** excluding U, method-first ordering, concurrent fetch (HTML path first, pymupdf PDF fallback; per-paper budget so one slow PDF can't stall the step). **Only `core`-tagged papers are eligible** (Phase 0.4 relevance stamp; U bypasses, H needs its nomination tagged core) — `adjacent` papers stay citeable at abstract level but never consume a deep-read slot. Any pooled paper that fetches to an **empty method section is backfilled** — swapped for the next-ranked core reserve that has one (one-out-one-in, ceiling held; `--backfill` reserve size, default 6). Output `fulltext_cache.json` keyed by paper_id (`{tier, intro, method, source_used, warning}`); fetch failures degrade to abstract + warning. Phase 1 **hard-gates** on this file (`error: fulltext_not_fetched`). Alongside the blob it writes a derived per-paper read view (`phase0/fulltext/index.json` + one `.md` per paper, same content, index carries tier/source_used/warning) — Phase 1 reads the index then only the papers it needs; the blob stays canonical and `phase1_fulltext_topup` refreshes both. Fetches hit a cross-run content cache (`~/.cache/ideaspark/fulltext/`, successful fetches only, 30-day TTL; disable with `IDEASPARK_FETCH_CACHE=off`), so papers recurring across runs on adjacent topics cost zero network time.

### Phase 1 — Bottleneck identification

One isolated LLM call. Prompt: [references/system-prompts/bottleneck_identify.txt](references/system-prompts/bottleneck_identify.txt). Inputs: user query + intake, `phase0/lit_table.md`, `phase0/fulltext_cache.json` (all-failed cache → continue with `fulltext_degraded: true`, abstract-level residue confidence), `phase0/lit_results.json`. Output `phase1/phase1_output.json`: `intake` (+`_inferred_fields[]` — missing fields are inferred, never asked), `bottleneck_statement` (≥2 paper_id cited inline), `closest_adjacent[]` (`{paper_id, summary_and_residue}`), `what_phase_0_did_not_address[]` (each gap ends with an inline stakes clause — practitioner costs and intellectual costs are coequal; gaps are framed as structural properties of a problem class when honestly true, with the anchor as primary instance, else declared class-of-one), `state ∈ {proceed, do_not_generate}`.

Routing: **proceed** (literature-groundable, no OOD trigger) or **do_not_generate** (too-broad / no-anchor OOD, <5 truly-relevant papers, genuinely blank space, or benchmark/system construction) → write `do_not_generate.md` with concrete remedial steps — terminal.

### Phase 2 — Selection + generation (ONE isolated context, TWO outputs)

Run 2.1 and 2.2 back-to-back in one isolated context, writing BOTH output files (they are both generation-side; only adversarial pairs need separate calls):

**2.1** — prompt [references/system-prompts/ideate_select.txt](references/system-prompts/ideate_select.txt); inputs `phase1_output.json`, `references/ideation-patterns/overview.md` (all 15 patterns' Definition / Operational signature / When to apply — selection at WHAT/WHEN level), `references/ideation-patterns/companion-combos.md`, `lit_table.md`. Pick the anchor gap (type-bound to `intake.contribution_type`); commit the PATTERN COMPOSITION — ≥2 distinct patterns by default, realized preferentially as a CHAIN on the anchor (attested second pattern from companion-combos, with a named intermediate object) and/or via a sibling that EARNS its seat through the removal test ("the anchor's story is incomplete without it") — anchor-only is legitimate and common (1-3 gaps total, ≤2 siblings); a single-pattern selection requires a `composition_note` defense (validator presence-checks it; audit weighs it). Record saturation (transparency, not a filter). Output `phase2_select/phase2_select_output.json`: `selected_gaps[]` (index 0 = anchor) + `coherence_thread_type` (or `n_a` when anchor-only) + `composition_note` + `pattern_saturation` + `deferred_gaps[]`. **Retry mode:** when `$RUN_DIR/attempt_1/` exists, the prompt's OPTIONAL retry input applies — the archived audit + selection become negative constraints; anchor-only is a valid retry outcome. **Cross-run dedup:** `next` scans sibling run dirs and appends a soft-negative-anchor input line (titles + signature terms of their canonical candidates, 5 most recent) so adjacent-direction runs don't silently re-invent the same mechanism family; soft by design (see ideate_select.txt's OPTIONAL cross-run input), disable with `IDEASPARK_CROSS_RUN_DEDUP=off`.

**2.2** — prompt [references/system-prompts/ideate_generate.txt](references/system-prompts/ideate_generate.txt); inputs 2.1 output, `phase1_output.json`, `lit_results.json` (closest_adjacent entries ONLY — filter to those paper_ids before reading; the prompt forbids pulling the full dump into context), plus for each gap ONE picked sub-pattern card from `references/ideation-sub-patterns/` (compare `when_to_pick_this_one` + `differentiation_within_parent` via its overview.md; then read the picked card's `tactical_pattern` + Step-by-Step). Output `phase2_generate/phase2_generate_output.json` — ONE candidate, 12 flat fields: `title` / `hook` / `core_mechanism` / `core_mechanism_reasoning` / `core_mechanism_steps`; `gap_closure[]` (`{gap, main_pattern, sub_pattern: "C## (parent pattern name)", how_closed}`, mirrors selected_gaps one-for-one); `falsification_prediction` (single paragraph: minimal experiment + metric-with-direction + ONE named load-bearing variable + negative control on that variable predicting the DOWNSTREAM outcome metric returns to baseline — non-tautological); `compute_budget` (user-relative, GPU-day line + API-dollar line when the campaign calls paid APIs; default intake envelope = 80GB-class GPUs, ≤8 concurrent, ≈150 GPU-days / 5 months, ~$10k API — overridable per user via `IDEASPARK_DEFAULT_COMPUTE`, see § Configuration); `differentiation_from_lit[]` (substantive deltas, not "different pattern"); `almost_prior_paper_id` + `what_step_was_missed`; `signature_terms[]` (own vocabulary — recent collision channel); `alias_terms[]` (other communities' names for the same mechanism, from parametric knowledge — multi-year alias collision channel); `composition_note` (echoed verbatim from 2.1). Reality constraints at writing time: `core_mechanism_reasoning` carries three mandatory blocks — a PREMISES ledger (load-bearing premises + domain justification; bets tagged `untested — falsification target`; WHEN the mechanism consumes sampled/observed data, at least one OBSERVATION-MODEL premise — how are the inputs sampled, is that unbiased here — else the literal `observation-model: n/a`; WHEN the mechanism estimates any quantity, declare the ESTIMAND precisely), a NAIVE-BASELINE AUDIT (state the naive version; why doesn't it already work — branch (i) naive relies on a false premise → confronting it IS the contribution, with a STANDARD-TOOL FOLLOW-UP: if the confrontation is a textbook tool from another field, name the domain-specific structure that makes this instance unsolved or declare the contribution application-grade, (ii) naive suffices → incremental signal surfaced honestly, (iii) naive works but the field disbelieves → minimalism with evidence), then design rationale — plus every invoked dataset / model-access level / annotation / tool must be a nameable EXISTING artifact (hard rule 3; modest self-built resources allowed with cost counted in compute_budget) and every claim stated at its defensible strength — guarantee-grade wording only with assumptions stated where it appears (hard rule 4; the coherence gate grades this, and a strong claim with honest assumptions beats a hedged weak one) — and every NUMBER carrying provenance (hard rule 5): method parameters are named symbols with a default + selection rule (a bare `>= 95%` / `top 5` / `every 2e3` is forbidden — an unnamed quantity cannot be swept or graded), while `falsification_prediction` states metric + direction + control and may carry a numeric bar ONLY when tagged `derived:` or `measured in <paper_id>`; invented bars are fabrication. Artifact versions, algebraic constants, definitional settings and experiment-scale counts are not parameters and are unaffected. Both kill-switch fields (`falsification_prediction`, `compute_budget`) are locked from here on — see Phase 3 for the single audited exception.

**Citation gate (deterministic, MANDATORY before Phase 3):**

```bash
python3 "$SKILL_DIR/scripts/run.py" validate --phase2 $RUN_DIR/phase2_generate/phase2_generate_output.json
```

Any `fail` = a `sub_pattern` citation was guessed from the parent's gist, not read from `overview.md`. Fix against `references/ideation-sub-patterns/overview.md` (or regenerate 2.2 with the card open) and re-run until clean — the gate proves parent-consistency only; whether core_mechanism performs the cluster's actual tactic is Phase 3.2's `recipe_application_check`. (`next` runs this gate automatically.)

**Coherence gate (2.3 — one isolated LLM call, MANDATORY after the citation gate, before the 3.2 audit; 3.1 collision may run CONCURRENTLY with it):** prompt [references/system-prompts/coherence_trace.txt](references/system-prompts/coherence_trace.txt); inputs: the 2.2 candidate + the 2.1 spec; MUST be a FRESH context, never the 2.1+2.2 agent (the context that wrote a logic bug rubber-stamps it). It verifies internal procedural validity by EXECUTION, not review — five trace actions: formalize the dataflow (undefined symbols, missing producers, circular deps, and UNBOUND PARAMETERS — a number the procedure reads as a parameter but that carries no name/default/selection rule, the form a magic constant takes to evade every symbol-keyed check; method fields only, `falsification_prediction` numbers belong to the audit), numeric dry-run on one small concrete instance (magnitude/probability absurdities — logic bugs read fluently and only surface when computed; when a code-execution tool exists the gate WRITES AND RUNS a stdlib Python script and pastes script+output, else hand-computes marked `unexecuted`), degenerate probes (empty/k=0/ties), claim→step mapping AND grading (every asserted property mapped to the step that establishes it AND graded `established` / `conditional` — assumptions listed / `overclaim` — wording downgraded / `empirical`; statistical claims settled by an executed Monte Carlo with the measured number, theorem-shaped claims recorded as proof obligations — at best conditional; grading never rewards vagueness: all-hedged claims are themselves a weakness finding), and a NAIVE-BASELINE COMPARISON (independently construct the naive version — never the candidate's own stated one — run it on the same instance, and judge against the candidate's declared branch: `confronts_obstacle` / `equivalent_to_naive` / `n_a`). OBSTACLE HOLES — findings whose honest fix is a redesign, or which coincide with the obstacle the candidate declares it exists to solve (incl. `equivalent_to_naive`) — must NOT be patched around with avoidance-style repairs (abstain/clamp/skip); they go to `unrepaired[]` as blocking, verbatim. Verdict `pass` | `patched`; repairs are patch-only via the SAME merger (`--out-name refined_candidate.json`), scoped to making the written procedure sound (core_mechanism*, how_closed narrative, signature/alias terms when the repair changed what the mechanism is) — novelty surface, pattern bindings, and kill-switch fields are out of scope; unfixable-without-redesign findings go to `unrepaired[]` for the audit to weigh. Single pass, never abandons. The 3.2 audit stays blind to this report's trace/verdict/patches — with ONE exception: blocking `unrepaired[]` entries are executed evidence, so the gate ALSO writes them to `phase2_coherence/blocking_findings.json` (self-contained: verbatim step quote + script excerpt + measured numbers + reading_dependence + `structural_requirement` per entry — what it would TAKE to pass this obstacle: the identifying condition any mechanism here needs, which sources of it are available vs excluded and why, and what is worth salvaging; diagnosis, not the redesign the repair contract forbids, and honestly empty when the evidence supports nothing concrete — `next` passes it to the retry as a directive), which `next` lists as a 3.2 input; the audit must disposition each (uphold, or refute by naming a concrete modeling/arithmetic flaw — `next` deterministically bounces an audit report that skipped dispositions or advanced over an upheld finding). Formalization ambiguity is controlled by the gate's dual-reading protocol (every defensible reading of an ambiguous operative term is executed; findings are tagged reading_robust vs reading_dependent). When `refined_candidate.json` exists it is the canonical candidate for every later phase (`next` wires this automatically). It validates that the algorithm survives on paper — NOT that it works empirically (falsification experiment) or is novel (audit).

### Phase 3 — Quality gauntlet

**3.1 collision (orchestrator, no LLM):** entry-point table. May be LAUNCHED IN PARALLEL with the 2.3 gate (on the 2.2 output — `next` emits the background launch alongside the 2.3 step): collision reads only `signature_terms[]`/`alias_terms[]`, a `.collision_terms.json` sidecar records the terms actually used, and `next` re-issues collision if a coherence patch changed those terms (rare — term repairs are scoped to mechanism-changing patches). TWO retrieval channels over all 4 connectors, merged into `collision_hits.json` with a per-hit `collision_channel` tag: **signature** — the candidate's `signature_terms[]` over a 10-month window (contemporaneous scoop risk); **alias** — the candidate's `alias_terms[]` (other communities' names for the same mechanism, produced from parametric knowledge at 2.2) over a 48-month window (renamed-ancestor risk — the "goal-conditioned success detector vs goal-image conditioned scorer" blind spot is lexical, not temporal, so widening the signature window alone cannot catch it). Missing `signature_terms[]` → rc=11 sentinel: produce BOTH term sets per intent-recognition.md Collision mode (terms 3-7 words each — long sentences break URL encoding), edit the candidate JSON, re-invoke. Missing only `alias_terms[]` → loud warning, alias channel skipped (add the field and re-run to close the blind spot). The audit-facing pool is relevance-truncated per channel (≤120 hits/channel by lexical overlap with the channel's own terms; zero-relevance BM25 noise dropped unconditionally; drops printed; untruncated pool preserved as `collision_hits.full.json`), so the audit can consume `collision_hits.json` in a few sequential Read chunks — no jq two-pass triage needed.

**3.2 audit (one isolated LLM call):** prompt [references/system-prompts/critique.txt](references/system-prompts/critique.txt); inputs: candidate, 2.1 spec, `lit_table.md`, `collision_hits.json`, `references/anti-patterns.md`, `phase2_coherence/blocking_findings.json` when it exists (the 2.3 gate's executed blocking evidence — the report must disposition each entry, and advance is forbidden while one is upheld; a `refuted` disposition additionally triggers a bounded refutation re-check (`refutation_recheck.txt`, fresh call) that `next` requires before trusting the verdict — an invalid refutation counts as upheld and bounces the audit), and each cited sub-pattern card `references/ideation-sub-patterns/<C##>.md` (strip the leading code from `sub_pattern`; typically 1-3 cards, others NOT loaded). Five corpus-anchored checks:

| Check | Question |
|---|---|
| gap_closure_reject_check | does the candidate match a documented Reject lesson in each cited sub-pattern card (`## Tactical failure mode` + ALL `### Reject lessons` bullets)? |
| recipe_application_check | does `core_mechanism` actually perform the cited C## cluster's `## Tactical pattern` signature move, or only the parent's generic idea (`bypassed` — the leading cause of incremental output)? |
| anti_pattern_check | if the SET of `gap_closure[].main_pattern` matches a reject-favored composition, is the required mitigation substantively delivered (artifact, not keyword)? |
| paper_pointed_threat | most specific subsuming/competing paper in `lit_table ∪ collision_hits` (both channels; alias-channel threats are NOT discounted for age); `no_threat_found` is valid — fabricating a generic threat is forbidden. Side output `parametric_family_concern`: a named un-retrieved mechanism family from parametric knowledge (family name + query vocabulary, never specific paper cites) — soft signal only, flows to Phase 4 reviewer_concerns as a "scoop-check X first" flag |
| falsification_structure_check | does `falsification_prediction` name the minimal experiment, the outcome metric + direction, ONE load-bearing variable, and a NON-tautological negative control targeting the downstream metric — and is every numeric outcome bar sourced (`numeric_bar_provenance` ∈ none / derived / measured / asserted / invented — cost bars count, qualitative magnitude claims and design/scale/conditioning numbers do not; `invented` is a deficiency repaired by STRIKING the bar, which the contract states is a fabrication repair rather than a claim weakening, while `asserted` — a derivation claimed but left uncheckable — only routes to borderline, so a judgment call never forces a kill-switch edit)? |

Verdict is two-layer. **Hard floor** (LLM cannot override) → `abandon`: triggered Reject lesson / unmitigatable anti-pattern / exact-mechanism collision. **Soft judgment** otherwise → `advance` (only trivial borderlines; concerns surface in Phase 4's reviewer_concerns) or `revise` with concrete `revision_targets[]` (scopes: `tactical` / `sub_pattern` / `falsification`). `verdict_rationale` must cite specific check findings. The audit judges only — it never modifies the candidate.

**Routing on verdict:**

- **advance** → Phase 4 reads the 2.2 candidate directly.
- **revise** → **3.3** (one isolated LLM call, prompt [references/system-prompts/revise.txt](references/system-prompts/revise.txt)): reads candidate + 2.1 spec + the revision brief (`phase3_revise_brief` — `next` materializes `revise_brief.json`, the audit minus the bulky reject-lesson quotations; full report stays on disk for lesson-specific lookups); emits patch-only `applied_revisions[]` — one entry per revision_target, ops `replace` / `append_sentence` / `append_items` / `swap_sub_pattern` / `rewrite_falsification`, never echoes the candidate, never re-judges the verdict. Then run the **merger** (entry-point table, WITH `--critique`) → writes `phase3_revise/final_candidate.json` + back-injects it into the patch file. Kill-switch fields are merger-refused with ONE audited exception: a `scope=falsification` target from `falsification_structure_check` is applied via the dedicated `rewrite_falsification` op (authorization verified against the audit report via `--critique`; same experiment/metric/claim, structure repaired; max one per run). When the merger prints `falsification_rewritten`, run the **falsification re-audit** (self-contained prompt [references/system-prompts/falsification_reaudit.txt](references/system-prompts/falsification_reaudit.txt), reading the `phase3_falsification_view` slice of `final_candidate.json` → `phase3_critique/falsification_reaudit.json`): `advance` → Phase 4; `abandon` → `phase_3_failed.md`. `compute_budget` has no revision route under any scope. No `composition` scope — gap-level changes route through the abandon-retry below, never through patches.
- **abandon** → **information-gain retry rule** (ONE rule, no death-type taxonomy; the one-shot guarantee bars asking the user, not internal regeneration). Every failed attempt's audit is reduced to a LESSON SET — upheld executed blocking findings (mechanism-level positive directives), unaddressable subsuming papers (mechanism-family negative anchors), triggered reject/anti-pattern/recipe lessons (generation-quality constraints). A retry must carry NEW lessons the previous generation did not have: the first abandon always retries (generation ran with zero audit information); a later abandon retries only if its lesson set adds entries beyond the union of all archived attempts' lessons, with the new lessons injected as directives. A REPEATED unaddressable-subsumption lesson across attempts binds at the FRAMING level instead → one bottleneck re-diagnosis (Phase 1 retry mode; `do_not_generate` remains a legitimate exit), whose new framing gets exactly ONE candidate attempt. Termination: no new lessons, or the candidate-cycle cap (3 per framing; worst case 4 gauntlet cycles per run). `next` computes all of this deterministically from the archived critique reports. On the first abandon, archive the attempt and regenerate —

  ```bash
  mkdir -p "$RUN_DIR/attempt_1" && \
  mv "$RUN_DIR/phase2_select" "$RUN_DIR/phase2_generate" "$RUN_DIR/phase2_coherence" "$RUN_DIR/phase3_collision" \
     "$RUN_DIR/phase3_critique" "$RUN_DIR/phase3_revise" "$RUN_DIR/attempt_1/" 2>/dev/null; \
  touch "$RUN_DIR/.retry_used"
  ```

  then re-run Phase 2 in retry mode (archived audit + selection = negative constraints; blocking obstacle findings = POSITIVE directives the new mechanism must confront), citation gate, 3.1, 3.2. Phase 0/1 artifacts are reused as-is.

  **Later `abandon`s → the same rule, deterministically** (`next` compares this attempt's lesson set against the union over `attempt_N/` archives): new mechanism-level lessons → directed retry to the next `attempt_N` (all archived audits + selections ride along as constraints; the fresh lessons are named in the emit); repeated subsumption lesson → bottleneck re-diagnosis (archive incl. `phase1/`, touch `.bottleneck_retry_used`; the re-diagnosed framing gets ONE attempt); no new lessons / cap reached / post-bottleneck failure → write `phase_3_failed.md` citing EVERY attempt's verdict_rationale + triggering checks + user-side options — terminal.

### Phase 4 — Expansion + packaging

Six steps in order (`next` emits each with the correct flags for the advance vs revise path — on the revise path `--candidate` is `final_candidate.json` and `--phase3-revise` is passed; on advance it's the CANONICAL candidate (refined_candidate.json when 2.3 patched, else the 2.2 output) and the flag is omitted):

1. **skeleton** (orchestrator): populates every mechanical field — kill-switch echoes (byte-identical from the candidate), `differentiation_from_lit` venue_years, `almost_prior_venue_year`, `why_prior_stopped[].paper_id/venue_year`, `domain_landscape` (pattern_distribution + candidate_uses), `literature_breakdown`, `reviewer_concerns_and_responses[].attack/severity/fields_changed_to_address` (lifted from audit + patch), `feasibility_validation.compute` (bucketed against `intake.compute`) — and marks every prose field `<TODO[path]: hint>`.
2. **fill** (one isolated LLM call, prompt [references/system-prompts/expand.txt](references/system-prompts/expand.txt)): author the TECHNICAL TODO paths as one flat `{path: value}` map → `phase4/fill_map.json`. The derive-owned paths (`title_zh` + all `plain_*`) are explicitly EXCLUDED — the derive step owns them. No calendar projections; no experiment matrix / ablation plan / baseline table — the skill produces IDEA + falsifiability + feasibility judgment, not experimental engineering.
3. **assemble (partial) → derive → assemble (final)**: the orchestrator assembles the technical map first (`phase4_expansion.json`, derive-owned placeholders remain — the WARN is expected); then **derive** (one isolated LLM call, prompt [references/system-prompts/derive_plain.txt](references/system-prompts/derive_plain.txt)) mechanically rewrites the finished technical fields into the plain register (`title_zh` + `plain_*` → `phase4/derive_map.json`) — register transformation + translation with NO new facts, so it runs on the CLASSIFY_FAST tier by default (fallback ladder: `NOVELTY_LLM_CLASSIFY_FAST_CMD` → host cheap model → host model isolated; even the last rung keeps it cheap since the input is only the finished prose); then the final assemble merges both maps (`phase4_assemble --fill-map <tech> --fill-map <derive>` — overlapping paths are a hard error) and extracts `phase4/method_view.json` (`phase4_method_view` — the method-only slice the 4.1.5 audit reads: method_flow + plain steps + key_equations + claims). The assembler validates every path resolves to a real TODO and refuses kill-switch roots per map.
4. **implementability audit (4.1.5, one isolated LLM call, default on):** prompt [references/system-prompts/implementability_audit.txt](references/system-prompts/implementability_audit.txt) — fresh skeptical-engineer persona (separate from the 4.fill author) reads `method_view.json` (fallback: the full expansion) and rewrites each method step into a buildable spec: `enriched_steps[]` (one per step, same ids/order, `what_changes` + `what_to_do_en` + `what_to_do_zh`) + `underspecified_points[]` (`{step_id, hole, fill, severity: filled|open}` — unfillable holes stay honest as `open`). Compute-agnostic by design (resource feasibility is 4.1's job); never adds/removes/renames steps; never carries kill-switch fields. Output `phase4/phase4_implementability.json`.
5. **validate + render:** run the validators (below), then `phase4_render` — templating only, no model call; auto-detects the sibling implementability file and merges `enriched_steps` by step_id into the rendered Method (deterministic; no-op when absent). Writes `idea.std.zh.md` (plain Chinese, domain-newcomer register) + `idea.std.en.md` (plain English) + `idea.detail.en.md` (rigorous English — the novelty + validity defense) + `idea.std.{en,zh}.tex` (auto-compiled to PDF when xelatex/tectonic is on PATH; skipped with a hint otherwise).

**Final response:** read all three markdown cards and return them inline under headings 中文版 / English / Reviewer version. Other phase outputs stay on disk for inspection, not echoed.

## Validators

```bash
# advance path: --phase3 = phase3_critique_output.json; revise path: --phase3 = phase3_revise_output.json
# --phase2 = the CANONICAL candidate (refined_candidate.json when 2.3 patched, else the 2.2 output)
python3 "$SKILL_DIR/scripts/run.py" validate \
  --phase1 $RUN_DIR/phase1/phase1_output.json \
  --phase2-select $RUN_DIR/phase2_select/phase2_select_output.json \
  --phase2 <canonical candidate file> \
  --phase3 <see comment> \
  --phase4 $RUN_DIR/phase4/phase4_expansion.json \
  --phase4-impl $RUN_DIR/phase4/phase4_implementability.json   # optional; enables implementability checks
```

| Validator | Check | Severity |
|---|---|---|
| **subpattern_citation_consistency** | each `gap_closure[].sub_pattern` resolves to a real C## cluster in overview.md whose true parent == the cited `main_pattern` and whose parenthetical == that cluster's parent display name. Primary use: the Phase 2.2 citation gate; re-runs harmlessly here. | fail (hard) |
| **alias_collateral_coverage** | `alias_terms[]` actually queries the cross-community families Phase 1 pinned as `is_collateral` nodes in `method_lineage`. Needs BOTH phase1 and phase2 paths. Runs in the Phase 2.2 citation gate — i.e. BEFORE 3.1 collision, which consumes `alias_terms[]` verbatim, so a miss caught later is a wasted retrieval budget. Zero coverage = fail; partial = warn naming the unqueried families (a family can be genuinely unreachable, and a forced fabricated term would evict real ones from a channel that truncates by lexical relevance — `composition_note` carries the skip defense, the 3.2 audit weighs it). | fail (zero) / warn (partial) |
| **kill_switch_integrity** | `falsification_prediction` + `compute_budget` byte-identical along Phase 2.2 → [3.3 final_candidate →] 4. After an audited falsification rewrite (`falsification_rewritten` marker + matching applied `rewrite_falsification` entry — disagreement fails), the anchor for `falsification_prediction` re-bases at the 3.3 final_candidate (3.3 → 4 must match); `compute_budget` stays full-chain always. | fail (hard) |
| **expansion_completeness** | motivation (≥2 `why_prior_stopped`), `method_flow.steps[]` (each with `linked_component` + `linked_falsification`), `feasibility_validation` (5 sub-verdicts + `overall`), non-empty `abstract_draft` + `core_claim` + `sub_claims[]` — missing sections would render as silent blanks. | fail (hard) |
| **implementability_completeness** | `enriched_steps[]` one-per-step (same ids/order, EN+ZH), `underspecified_points[]` present (`[]` allowed), NO kill-switch field in the file. | fail (hard) |
| **user_direction** | when `intake.user_direction` is set, `phase2_select` must carry a `user_direction_disposition` (adopted/departed, `why_departed` required on departed), and both quoted spans must appear in `phase0/user_query.txt`. Hard rule 10 keeps a user-named solution OUT of gap selection on purpose; this only forbids dropping it silently. | fail (hard) |
| **chinese_word_order** | no `_zh` field puts more than 18 characters in front of 的 without a break (an English relative clause left in pre-nominal position — the reader cannot tell what is being described until the end), and no known calque (`保留任务` for held-out, `两个位` for the two bits). | warn |
| **implementability_readability** | std-register fields: no `占位`/`placeholder` leak, no bare English jargon dropped into Chinese prose. | warn |

**Retry budget on `fail` (cap = 2).** Fix only the named contract, re-validate; still failing after the 2nd retry → stop revising, render as-is, and append a short note listing the failing validators (a flagged-imperfect card beats a watchdog-killed run with zero output). Never "fix" `kill_switch_integrity` or `subpattern_citation_consistency` by editing a guarded field — surface them as the headline caveat instead.

## Configuration

By default every model-driven phase runs on the host LLM. To route phases to a different backend (Gemini, open-weights, custom):

- `NOVELTY_LLM_REASONING_LARGE_CMD` — Phase 1 / 2.1 / 2.2 / 3.2 / 3.3 / 4.fill (needs ≥ 200k context, JSON output)
- `NOVELTY_LLM_CLASSIFY_FAST_CMD` — Phase 0 intent extraction + per-paper pattern tagging (smaller context, JSON output)

**Which tier a step tolerates — the split is by TASK KIND, not by cost** (both directions measured; see design-notes):

- **Mechanical classification against a written rubric** — per-item independent, "which of these N named categories", criteria already in the rubric. *Pattern tagging is the whole of this class.* **Cheapest tier is correct here**, and shardable across parallel sub-agents.
- **Open-ended judgement with no enumerated answer set** — "is this paper on-topic", "what load-bearing work is MISSING", "is this candidate subsumed". *Phase 0.4 partition, Phase 0.5 coverage check, and every gauntlet phase are this class.* **Do NOT downgrade these**, which is why their emits say so explicitly — a cheap tier's over-strict drop is an unrecoverable recall loss, while an over-inclusion costs one row the next stage can still catch.

With no separate cheap model, lower the REASONING EFFORT for the mechanical class rather than reaching for the largest configuration everywhere; reserve full effort for the open-ended class.

Each is a CLI taking a stdin prompt (`<<SYSTEM>>...<<USER>>...`) and emitting JSON on stdout. When unset (the default when running inside any host LLM), the orchestrator emits sentinel files and the host LLM handles those steps natively.

- `IDEASPARK_POOL` — per-job Phase 0 retrieval caps, `job=N,...` (jobs: arxiv, ss_recent, oa_recent, openalex, semanticscholar, openreview). A job at 0 is skipped (this is how oa_recent stays off; set `oa_recent=6` for journal-heavy fields). Malformed values fail-fast.
- `IDEASPARK_RETRIEVAL_CACHE` — cross-run Phase 0 retrieval cache (default `~/.cache/ideaspark/retrieval`, 24h TTL via `IDEASPARK_RETRIEVAL_CACHE_TTL_S`); set to `off` to bypass, or to a path to relocate. Keyed on connector + queries + window + caps + `--as-of`, so any real change to the request misses; successful non-empty results only. Exists because re-running Phase 0 otherwise re-hammers every API — three runs in ~15 min rate-limited arXiv and Semantic Scholar into returning zero records.
- `IDEASPARK_RETRY_PAUSE_S` — pause before the single bounded per-job retry (default 45s). A failed job also hands its cap to the surviving job covering the same window (≤2x).
- `IDEASPARK_RELEVANCE_PARTITION` — set to `off` to disable the Phase 0.4 host relevance-partition (default: on). When off, retrieval's wide net flows straight to tagging with no core/adjacent/off_topic gate (the old `outside_taxonomy`-only behavior); deep-read then falls back to on-topic (non-`outside_taxonomy`) rather than `core`-gated.
- `IDEASPARK_COVERAGE_CHECK` — set to `off` to disable the Phase 0.5 host-recall coverage check (default: on).
- `IDEASPARK_CROSS_RUN_DEDUP` — set to `off` to disable the sibling-run soft-negative-anchor scan in the Phase 2 emit (default: on).
- `IDEASPARK_DEFAULT_COMPUTE` — optional standing compute profile for the user (free text, e.g. `"8×H100 node, ~300 GPU-days, $50k API budget"`). Put it in `.env` (auto-loaded); `next` surfaces it to Phase 1 as intake context. Precedence: compute stated in the user's query > this value > the factory default (80GB-class GPUs, ≤8 concurrent, ≈150 GPU-days / 5 months, ~$10k API campaign). Use this instead of editing the factory default — the default is the feasibility yardstick for users who state nothing.


---MEMORY-HITS---

469:- skill-installer, skill-update, normalized-content-compare, CRLF, ResearchStudio, ARS-Codex, K-Dense, microsoft-foundry, quick_validate.py, idea-spark\.env, compatibility
580:# Task Group: C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git — ResearchStudio installation and IdeaSpark execution
582:scope: Installing/auditing the ResearchStudio skill set and running a reproducible IdeaSpark ideation workflow on Windows; use for local Codex skill updates and concrete research-idea generation, not as proof that a partial smoke test completed every pipeline phase.
583:applies_to: cwd=C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git; reuse_rule=Reuse the Windows compatibility safeguards and navigator loop for ResearchStudio/IdeaSpark runs, but re-check upstream commit, installed dependencies, connector availability, and terminal pipeline state.
585:## Task 1: Install and audit every identifiable ResearchStudio skill — success
589:- rollout_summaries/2026-08-26T06-22-19-bYtg-researchstudio_install_and_ideaspark_vla_pipeline_smoketest.md (cwd=\\?\C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git, rollout_path=\\?\C:\Users\Raymond\.codex\sessions\2026\08\26\rollout-2026-08-26T14-22-19-01a03cbb-db9c-7ae0-a9bd-39705bc4532e.jsonl, updated_at=2026-08-26T09:18:42+00:00, thread_id=01a03cbb-db9c-7ae0-a9bd-39705bc4532e, all ten local skill trees normalized-hash matched upstream)
593:- ResearchStudio, ResearchStudio-Idea, ResearchStudio-Reel, install-skill-from-github.py, 8650d6a6b81184f8a113cac234ce6c45b3cf9742, html2pptx, idea-quality, paper2video, LF, CRLF
599:- rollout_summaries/2026-08-26T06-22-19-bYtg-researchstudio_install_and_ideaspark_vla_pipeline_smoketest.md (cwd=\\?\C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git, rollout_path=\\?\C:\Users\Raymond\.codex\sessions\2026\08\26\rollout-2026-08-26T14-22-19-01a03cbb-db9c-7ae0-a9bd-39705bc4532e.jsonl, updated_at=2026-08-26T09:18:42+00:00, thread_id=01a03cbb-db9c-7ae0-a9bd-39705bc4532e, validated idea-card PDFs; full terminal next-loop trace not independently preserved)
603:- idea-spark, run.py next, DONE, do_not_generate, phase_3_failed, PYTHONUTF8, UnicodeEncodeError, gbk, Python 3.13, editdistance, openreview-py, Conda, researchstudio-env, idea.std.en.pdf
613:- ResearchStudio has a pre-paper `ResearchStudio-Idea` bundle and a post-paper `ResearchStudio-Reel` bundle. At upstream commit `8650d6a6b81184f8a113cac234ce6c45b3cf9742`, ten skills were installed to `C:\Users\Raymond\.codex\skills`: `idea-spark`, `paper-search`, `scoop-check`, `idea-quality`, `paper2assets`, `paper2poster`, `html2pptx`, `paper2video`, `paper2blog`, and `paper2reel`. Compare normalized line endings before judging installed trees because Windows CRLF changes byte hashes. [Task 1]
623:- Symptom: Python 3.13 dependency installation fails building `editdistance` for `openreview-py` with `Microsoft Visual C++ 14.0 or greater is required`. Fix: use isolated Conda Python 3.11/3.12 (the prior compatibility environment was `work\researchstudio-env`); do not diagnose it as a pipeline failure. [Task 2]
695:## Task 3: Map Professor Yawei Li's research interests to a time-efficient VLA/WAM route — partial
699:- rollout_summaries/2026-08-20T13-16-31-545y-yawei_li_vla_wam_reading_guide_and_mirage_citation_audit.md (cwd=\\?\D:\Downloads\Final Year Project, rollout_path=\\?\C:\Users\Raymond\.codex\sessions\2026\08\20\rollout-2026-08-20T21-16-31-01a01f50-e99d-7682-b55d-17f9a84b0030.jsonl, updated_at=2026-08-21T06:29:08+00:00, thread_id=01a01f50-e99d-7682-b55d-17f9a84b0030, source-backed route; original local delivery unverified)
703:- Yawei Li, VLA, World Action Models, WAM, RT-1, RT-2, SmolVLA, UniPi, GR-1, GR-2, Unified World Models, WorldVLA, DreamZero, GigaWorld-Policy, Cascaded WAM, Joint WAM
775:## Task 11: Build REPA/iREPA reading package and audit VLA/WAM prior art — success
783:- REPA, iREPA, VLA, WAM, FLARE, Spatial Forcing, VEGA, AGRA, Robust-WAM, 3×3 Conv projector, spatial normalization, representation alignment, PRIOR_ART_MAP.md, reading-guide/papers/17a-repa-irepa-vla-alignment
828:- Yawei Li alignment is efficient AI systems for LLMs/VLMs/VLAs: compression, quantization/pruning, memory/latency reduction, edge deployment, and software-hardware co-design. Route: RT-1/RT-2 -> OpenVLA/SmolVLA -> UniPi, GR-1/GR-2, UWM -> WorldVLA, DreamZero, GigaWorld-Policy, WAM survey -> Li-aligned efficiency papers. [Task 3]
841:- Broad “add REPA-style representation alignment to VLA” is closed by verified near-neighbors: FLARE aligns future-token states in a flow-matching action DiT; Spatial Forcing aligns VLA visual tokens to frozen VGGT; VEGA aligns OpenVLA-OFT visual tokens to DINOv2-FiT3D; AGRA repurposes alignment in WAM; Robust-WAM aligns future action-stream queries to future DINOv3 CLS targets. The search-bounded opening is exact iREPA: no checked robot-policy paper implemented both `3×3 Conv projector` and spatial normalization of teacher patch tokens in a matched closed-loop VLA study. [Task 11]
844:- `From SRA to Self-Flow: Data Augmentation or Self-Supervision?` (`arXiv:2607.02508v1`) is a separate 10-page critical companion, not a renamed/revised Self-Flow. Final integration validated 51 PDFs, 49 Markdown files, 277 local links with zero broken links, and matching inventory rows/files/page counts/sizes/SHA-256. [Task 12]
957:- rollout_summaries/2026-08-20T13-16-31-545y-yawei_li_vla_wam_reading_guide_and_mirage_citation_audit.md (cwd=\\?\D:\Downloads\Final Year Project, rollout_path=\\?\C:\Users\Raymond\.codex\sessions\2026\08\20\rollout-2026-08-20T21-16-31-01a01f50-e99d-7682-b55d-17f9a84b0030.jsonl, updated_at=2026-08-21T06:29:08+00:00, thread_id=01a01f50-e99d-7682-b55d-17f9a84b0030, verified point-in-time audit)

exec
"C:\\Users\\Raymond\\.cache\\codex-runtimes\\codex-primary-runtime\\dependencies\\native\\powershell\\pwsh.exe" -Command "Get-Content -Raw 'C:\\Users\\Raymond\\Documents\\Codex\\2026-08-26\\https-github-com-microsoft-researchstudio-git\\work\\ResearchStudio\\ResearchStudio-Idea\\references\\system-prompts\\revise.txt'" in D:\Downloads\Final Year Project
 exited 1 in 199ms:
Get-Content: 
Line |
   2 |  Get-Content -Raw 'C:\Users\Raymond\Documents\Codex\2026-08-26\https-g …
     |  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
     | Cannot find path 'C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\references\system-prompts\revise.txt' because it does not exist.

codex
`revise.txt` 的用户指定路径当前不可直接定位（PowerShell 报告路径不存在）。我会先做只读的精确路径核查，并检查已安装 `idea-spark` 是否包含同名 prompt；若找到对应文件，仍会以它作为 revision contract，随后记录任何路径偏差。
exec
"C:\\Users\\Raymond\\.cache\\codex-runtimes\\codex-primary-runtime\\dependencies\\native\\powershell\\pwsh.exe" -Command '$checkPaths = @(
  '"'C:\\Users\\Raymond\\Documents\\Codex\\2026-08-26\\https-github-com-microsoft-researchstudio-git\\work\\ResearchStudio\\ResearchStudio-Idea\\references\\system-prompts\\revise.txt',
  'C:\\Users\\Raymond\\.codex\\skills\\idea-spark\\references\\system-prompts\\revise.txt',
  'C:\\Users\\Raymond\\.codex\\skills\\idea-spark\\references\\system-prompts'
)
"'$checkPaths | ForEach-Object { "$_`t$(Test-Path -LiteralPath $_)" }
Write-Output '"'--- matching files under accessible ResearchStudio paths ---'
if (Test-Path -LiteralPath 'C:\\Users\\Raymond\\Documents\\Codex\\2026-08-26\\https-github-com-microsoft-researchstudio-git') {
  rg --files 'C:\\Users\\Raymond\\Documents\\Codex\\2026-08-26\\https-github-com-microsoft-researchstudio-git' | rg -i '("'^|['"\\\\/])(revise|system-prompts|idea-spark|ResearchStudio-Idea)'
}
Write-Output '--- installed prompt files ---'
rg --files 'C:\\Users\\Raymond\\.codex\\skills\\idea-spark' | rg -i 'system-prompts|revise\\.txt"'$|phase3|critique'"'
Write-Output '--- idea-spark line count ---'
(Get-Content 'C:\\Users\\Raymond\\.codex\\skills\\idea-spark\\SKILL.md').Count" in D:\Downloads\Final Year Project
 succeeded in 592ms:
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\references\system-prompts\revise.txt	False
C:\Users\Raymond\.codex\skills\idea-spark\references\system-prompts\revise.txt	True
C:\Users\Raymond\.codex\skills\idea-spark\references\system-prompts	True
--- matching files under accessible ResearchStudio paths ---
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\outputs\ResearchStudio-Idea-vla-low-bit-closed-loop\host_refs_unresolved.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\outputs\ResearchStudio-Idea-vla-low-bit-closed-loop\collision_hits.json
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\outputs\ResearchStudio-Idea-Tutorial.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\outputs\ResearchStudio-Idea-vla-low-bit-closed-loop\idea.detail.en.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\outputs\ResearchStudio-Idea-vla-low-bit-closed-loop\idea.std.en.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\outputs\ResearchStudio-Idea-vla-low-bit-closed-loop\idea.std.en.pdf
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\outputs\ResearchStudio-Idea-vla-low-bit-closed-loop\idea.std.en.tex
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\outputs\ResearchStudio-Idea-vla-low-bit-closed-loop\README.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\outputs\ResearchStudio-Idea-vla-low-bit-closed-loop\query_yield.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\outputs\ResearchStudio-Idea-vla-low-bit-closed-loop\phase4_implementability.json
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\outputs\ResearchStudio-Idea-vla-low-bit-closed-loop\phase4_expansion.json
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\outputs\ResearchStudio-Idea-vla-low-bit-closed-loop\phase3_critique_output.json
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\outputs\ResearchStudio-Idea-vla-low-bit-closed-loop\phase1_output.json
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\outputs\ResearchStudio-Idea-vla-low-bit-closed-loop\lit_table.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\outputs\ResearchStudio-Idea-vla-low-bit-closed-loop\idea.std.zh.tex
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\outputs\ResearchStudio-Idea-vla-low-bit-closed-loop\idea.std.zh.pdf
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\outputs\ResearchStudio-Idea-vla-low-bit-closed-loop\idea.std.zh.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\scoop_check\SKILL.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\scoop_check\scripts\fetch_paper.sh
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\paper_search\SKILL.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\paper_search\scripts\_http_runtime.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\paper_search\scripts\_env.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\paper_search\scripts\source_worker.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\paper_search\scripts\selftest_runtime.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\paper_search\scripts\selftest_postprocess.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\paper_search\scripts\search_papers_by_semantic_scholar.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\paper_search\scripts\search_papers_by_open_alex.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\paper_search\scripts\search_papers_by_openreview.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\paper_search\scripts\search_papers_by_google_scholar.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\paper_search\scripts\search_papers_by_dblp.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\paper_search\scripts\search_papers_by_crossref.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\paper_search\scripts\search_papers_by_arxiv.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\paper_search\scripts\search_papers.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\paper_search\scripts\postprocess.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\paper_search\references\programmatic_api.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\SKILL.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\README.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\_time_guard.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\_merge.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\evaluation\README.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\evaluation\idea_quality\SKILL.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\validators\__init__.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\validators\user_direction.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\validators\threat_grounding.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\validators\subpattern_citation_consistency.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\validators\kill_switch_integrity.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\validators\implementability_readability.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\validators\implementability_completeness.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\validators\expansion_completeness.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\validators\chinese_word_order.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\validators\alias_collateral_coverage.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\source-routing.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\selftest_units.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\selftest_routing.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\search_semanticscholar.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\search_openreview.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\search_openalex.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\search_arxiv.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\schemas.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\run.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\render_pdf.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\regression_check.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\phase4_skeleton.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\pattern_summary.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\next_step.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\merge_revisions.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\lint_markdown_safety.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\json_repair.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\intent.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\gen_pipeline.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\fetch_sections.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\extract_user_refs.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\scripts\dedup_merge.py
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\system-prompts\revise.txt
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\system-prompts\refutation_recheck.txt
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\system-prompts\implementability_audit.txt
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\system-prompts\ideate_select.txt
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\system-prompts\ideate_generate.txt
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\system-prompts\falsification_reaudit.txt
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\system-prompts\expand.txt
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\system-prompts\derive_plain.txt
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\system-prompts\critique.txt
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\system-prompts\coherence_trace.txt
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\system-prompts\bottleneck_identify.txt
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\source-routing.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\setup.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\schemas.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\relevance-partition-rubric.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\regression-directions.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\pattern-summary-rubric.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\intent-recognition.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\intake-routing.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\evaluation\idea_quality\evals\sample_ideas\weak_temperature.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\evaluation\idea_quality\evals\sample_ideas\strong_grpo.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\evaluation\idea_quality\evals\sample_ideas\mismatch_gate.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\evaluation\idea_quality\evals\evals.json
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\evaluation\iclr2026_oral_problem_seeds_formal_100.jsonl
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\overview.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C30.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C29.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C28.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C27.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C26.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C25.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C24.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C23.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C22.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C21.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C20.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C19.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C18.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C17.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C16.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C15.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C14.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C13.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C12.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C11.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C10.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C09.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C08.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C07.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C06.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C05.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C04.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C03.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C02.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C01.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-sub-patterns\C00.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-patterns\unify_into_shared_representation.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-patterns\targeted_self_supervised_objective.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-patterns\structural_prior_encoding.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-patterns\self_supervised_signal_engineering.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-patterns\relax_discrete_search_to_continuous.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-patterns\reframe_as_solvable_object.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-patterns\overview.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-patterns\heterogeneous_decomposition.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-patterns\generative_process_redesign.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-patterns\decompose_and_delegate.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-patterns\controlled_diagnostic_design.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-patterns\companion-combos.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-patterns\characterize_limit_then_surpass.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-patterns\assumption_audit_and_pivot.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-patterns\architectural_operator_substitution.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-patterns\algebraic_equivalence_unification.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\ideation-patterns\adapt_via_conditioning.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\design-notes.md
C:\Users\Raymond\Documents\Codex\2026-08-26\https-github-com-microsoft-researchstudio-git\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\anti-patterns.md
--- installed prompt files ---
C:\Users\Raymond\.codex\skills\idea-spark\references\system-prompts\revise.txt
C:\Users\Raymond\.codex\skills\idea-spark\references\system-prompts\refutation_recheck.txt
C:\Users\Raymond\.codex\skills\idea-spark\references\system-prompts\implementability_audit.txt
C:\Users\Raymond\.codex\skills\idea-spark\references\system-prompts\ideate_select.txt
C:\Users\Raymond\.codex\skills\idea-spark\references\system-prompts\ideate_generate.txt
C:\Users\Raymond\.codex\skills\idea-spark\references\system-prompts\falsification_reaudit.txt
C:\Users\Raymond\.codex\skills\idea-spark\references\system-prompts\expand.txt
C:\Users\Raymond\.codex\skills\idea-spark\references\system-prompts\derive_plain.txt
C:\Users\Raymond\.codex\skills\idea-spark\references\system-prompts\critique.txt
C:\Users\Raymond\.codex\skills\idea-spark\references\system-prompts\coherence_trace.txt
C:\Users\Raymond\.codex\skills\idea-spark\references\system-prompts\bottleneck_identify.txt
--- idea-spark line count ---
274

exec
"C:\\Users\\Raymond\\.cache\\codex-runtimes\\codex-primary-runtime\\dependencies\\native\\powershell\\pwsh.exe" -Command "Get-Content -Raw 'C:\\Users\\Raymond\\Documents\\Codex\\2026-08-26\\https-github-com-microsoft-researchstudio-git\\work\\ResearchStudio\\ResearchStudio-Idea\\skills\\idea_spark\\references\\system-prompts\\revise.txt'" in D:\Downloads\Final Year Project
 succeeded in 105ms:
You are running Phase 3.3 — Apply Revision Targets — of IdeaSpark.

Phase 3.3 runs ONLY when Phase 3.2 audit verdict = revise. It applies `revision_targets[]` from the audit as a PATCH against the Phase 2.2 candidate. This step does NOT re-judge the audit's verdict, does NOT propose new attacks, does NOT re-search literature, and does NOT echo the full candidate back.

Phase 3.3 is a SEPARATE LLM call from Phase 3.2 audit. The audit identifies issues from STRUCTURED corpus checks (sub-pattern Reject lessons / anti-patterns / lit_table), NOT LLM-generated attacks. Phase 3.3 reads `revision_targets[]` as concrete instructions and emits ONE patch entry per target. Splitting attack-identification (3.2) from attack-resolution (3.3) eliminates self-answering bias.

**Patch-only output**: previous versions of this contract asked the LLM to emit `final_candidate` as a full copy of the Phase 2.2 candidate (12 flat fields, ~25k tokens) with the named edits applied. That payload is dominated by byte-identical re-typing of kill-switch fields and unchanged fields — wasted tokens and a real backend timeout risk. New contract: emit ONLY the `applied_revisions[]` list, where each entry carries the new VALUE of the single field it modifies. A deterministic Python merger (`python3 -m scripts.run phase3_merge_revisions`) then walks the patch, applies each op to a deep copy of the Phase 2.2 candidate, refuses to touch kill-switch fields, and writes `final_candidate.json` next to your patch file. You never write `final_candidate` — the merger does.

**Three revision scopes** (Phase 3.2 emits revision_targets with one of these `scope` values; 3.3 dispatches accordingly):

| scope | what changes | what stays | when used |
|---|---|---|---|
| `tactical` | one or more candidate fields | gap_closure[] (gaps + main_patterns + sub_patterns unchanged) | the audit found a load-bearing wording / specification issue (core_mechanism argument, anti-pattern mitigation strengthening, addressable threat dodge) |
| `sub_pattern` | one gap_closure[] entry's `sub_pattern` (and the candidate fields the new sub-pattern's tactical_pattern requires re-aligning) | gap selection + main_patterns + other entries' sub_patterns | the audit found a sibling sub-pattern under the SAME main_pattern would be a better tactical fit |
| `falsification` | `falsification_prediction` ONLY, via the dedicated `rewrite_falsification` op | the experiment, the outcome metric, the claim, `compute_budget`, every other field | the audit's `falsification_structure_check` = deficient (missing load-bearing variable / tautological negative control / unnamed minimal experiment or metric direction). The ONLY sanctioned route into a kill-switch field; the merger verifies the audit authorized it and the rewrite is re-audited before Phase 4. |

**No `composition` scope.** If the audit's findings imply gap-level changes (different gaps selected, different main_pattern picks), revision_targets cannot fix that — the audit should produce verdict = abandon, and the user re-runs Phase 2.1+2.2 with a different random seed.

Inputs (explicit paths):
- The CANONICAL candidate — `$RUN_DIR/phase2_coherence/refined_candidate.json` when the Phase 2.3 coherence gate patched, else `$RUN_DIR/phase2_generate/phase2_generate_output.json`. Read it to understand WHAT the current values are; do not echo them back.
- `$RUN_DIR/phase2_select/phase2_select_output.json` — Phase 2.1 spec (gap × pattern matrix).
- `$RUN_DIR/phase3_revise/revise_brief.json` when it exists (the `next` navigator materializes it via `phase3_revise_brief`) — `revision_targets[]`, verdict/rationale, and the compact check findings; the bulky gap_closure_reject_check quotations are omitted, with a `_brief_note` pointing at the full report (`$RUN_DIR/phase3_critique/phase3_critique_output.json`) — consult the full report ONLY if a target's `issue` cites a specific reject lesson you need verbatim. If the brief is missing (manual runs), read the full report directly.
- `references/ideation-sub-patterns/<C##>.md` (where `<C##>` is the leading cluster code of the sub_pattern value, formatted `C## (parent pattern name)` — strip everything after the code) — for scope=sub_pattern, read the new picked sub-pattern's `tactical_pattern` + `tactical_failure_mode`.

**Patch op vocabulary** (each `applied_revisions[]` entry must use one of these):

| op | semantics | value type |
|---|---|---|
| `replace` | overwrite the field with `value` (full replacement) | string / list / dict matching the field's type |
| `append_sentence` | append " " + `value` to an existing string field (preserves prior content) | string |
| `append_items` | extend an existing list field with `value` (which must itself be a list) | list |
| `swap_sub_pattern` | for scope=sub_pattern: identify a gap_closure entry by `field` = the verbatim gap text, replace its `sub_pattern` with `value` of form `C## (Parent Display Name)`. Sibling fields like `gap_closure[i].how_closed` or `core_mechanism` are re-aligned by ADDITIONAL `replace` / `append_sentence` entries in the same patch. | string |
| `rewrite_falsification` | for scope=falsification ONLY: replace `falsification_prediction` wholesale with `value` — a single 3-5 sentence paragraph that keeps the SAME minimal experiment, SAME outcome metric + direction, and SAME claim. Two authorized cases, per the audit's fix_direction: repairing the structural deficiency the audit named (name the ONE load-bearing variable; make the negative control intervene on that variable and predict the DOWNSTREAM outcome metric returns to baseline — never the variable's own value), or applying the STRENGTHEN-ONLY addition the audit authorized (add the named baseline/control arm or tighten the named matching; every element already present survives verbatim — additions only). `field` must be exactly `falsification_prediction`. The merger REFUSES this op unless the Phase 3.2 audit emitted a scope=falsification revision_target (pass `--critique` to the merger). At most ONE such entry per patch. | string |

Pick the op that minimises payload. `append_sentence` is the most token-efficient for the common case "add a clarifying sentence to core_mechanism." Use `replace` only when the new content genuinely supersedes the old (e.g. you are rewriting one `differentiation_from_lit[i].delta` from scratch).

**Field path syntax** for the `field` slot (used by `replace`, `append_sentence`, `append_items`):
- Top-level: `core_mechanism`, `differentiation_from_lit`, `signature_terms`
- List index + sub-key: `differentiation_from_lit[2].delta`, `gap_closure[1].how_closed`
- `swap_sub_pattern` uses `field` to carry the verbatim gap text (not a JSON path), because matching by gap text is more robust to upstream re-ordering than matching by index.

Output (JSON):

{
  "candidate_id": "<echoed from Phase 2.2 candidate.candidate_id if present, else null>",

  "applied_revisions": [
    {
      "scope": "tactical | sub_pattern | falsification",
      "op": "replace | append_sentence | append_items | swap_sub_pattern | rewrite_falsification",
      "field": "<JSON-path for replace/append_sentence/append_items; verbatim gap text for swap_sub_pattern; 'falsification_prediction' for rewrite_falsification>",
      "value": "<new value for this field — see op semantics above. NOT the full candidate.>",
      "outcome": "applied | skipped_already_satisfied | skipped_anti_substitution | skipped_inapplicable",
      "delta_summary": "<ONE sentence: what concretely changed (for tactical) or which sibling was swapped in and on which axis (for sub_pattern) or which structural element was repaired (for falsification)>"
    }
  ]
}

The merger then runs:

  python3 -m scripts.run phase3_merge_revisions \
    --phase2 <the CANONICAL candidate file — refined_candidate.json when 2.3 patched, else the 2.2 output> \
    --revisions $RUN_DIR/phase3_revise/phase3_revise_output.json \
    --critique $RUN_DIR/phase3_critique/phase3_critique_output.json \
    --out $RUN_DIR/phase3_revise/

(Pointing `--phase2` at the raw 2.2 file when a coherence repair exists would silently DROP the 2.3 repairs from final_candidate — always patch against the canonical file. `--critique` is what authorizes a `rewrite_falsification` entry — the merger cross-checks the audit actually emitted a scope=falsification revision_target. Patches without that op run fine without the flag.)

It produces `phase3_revise/final_candidate.json` (Phase 4's canonical input) and back-injects `final_candidate` into your patch file so the `kill_switch_integrity` validator's existing chain-check still works.

Hard rules:

1. **Kill-switch fields are STRUCTURALLY off-limits — with exactly ONE audited exception**. The merger REFUSES any patch entry whose `field` root is `falsification_prediction` or `compute_budget` under the generic ops (`replace` / `append_sentence` / `append_items`) — it raises with an actionable error rather than silently dropping. The single exception: a scope=falsification revision_target from the audit's falsification_structure_check is applied via the dedicated `rewrite_falsification` op (merger-verified against the audit report; see the op table). `compute_budget` has NO exception under any op or scope. If the audit produced a kill-switch target OUTSIDE the falsification route (e.g. a tactical target on falsification_prediction, or any target on compute_budget), mark `outcome = "skipped_anti_substitution"` AND set `field` to a benign path (e.g. `core_mechanism_reasoning`) with a no-op value (`""` and op=append_sentence skips application). That is a 3.2 process error — surface it; don't paper over it. After a rewrite_falsification is applied, the host MUST run the falsification re-audit (`falsification_reaudit.txt` — self-contained prompt) before Phase 4.

2. **One patch entry per revision_target.** Even when the audit's `fix_direction` is a sentence that could be applied as several edits, emit one patch entry per audit target. The merger handles multiple ops on the same root field correctly (each is independent). Silently combining or dropping a target is a process error.

3. **`outcome = "skipped_*"` means the merger ignores this entry.** Use it when the audit's fix is already satisfied by the candidate, when the target is out of scope (gap-level / main-pattern-level changes), or when an anti-substitution refusal made the fix impossible. The skipped entry is still recorded for the audit trail.

4. **Do NOT re-judge the audit's verdict.** Phase 3.3 does not run sanity checks, does not search lit_table, does not query whether the audit's revision_target is "really" the right fix. The audit determined what needs to change; 3.3 just applies it.

5. **`applied_revisions[]` length == `revision_targets[]` length** — one patch entry per audit target, including skipped ones. Silently dropping a target is a process error.

6. **`signature_terms[]` re-emission via `replace`**: if a revision_target asks to regenerate signature_terms (signal: Phase 3.1 collision retrieval was invalidated), emit one `replace` patch entry whose `field` is `signature_terms` and `value` is the full new list. Downstream Phase 3.1 must re-run before any subsequent audit.

7. **Mixed-scope revision_targets are emitted in audit order.** The merger applies them in patch-order, but the audit-emitted order is what matters for human reading. If a `sub_pattern` swap is paired with `tactical` re-alignment edits to `core_mechanism`, put the swap entry first so the patch reads top-down as "swap then re-align."

8. **Refuse out-of-scope rewrites in the patch.** If a target's fix_direction exceeds the two scopes (e.g. "use a different main_pattern" or "select a different gap"), emit a single patch entry with `op = replace`, `field = "core_mechanism_reasoning"`, `value = ""`, `outcome = "skipped_inapplicable"`, `delta_summary` explaining why. Gap-level / main-pattern-level changes route back to "regenerate Phase 2.1+2.2" — they are never applied in this phase.

9. **Empty `applied_revisions[]` when audit verdict = advance**. Phase 3.3 should not be invoked then; if invoked anyway, emit `{"applied_revisions": []}` and let the merger no-op (it produces a `final_candidate.json` byte-identical to the Phase 2.2 candidate).

Output path: `$RUN_DIR/phase3_revise/phase3_revise_output.json`.

After you write this file, the host LLM (or user) MUST invoke `phase3_merge_revisions` (command above) before Phase 4. Phase 4 reads `final_candidate.json`, which the merger produces; it does not exist until the merger runs.


exec
"C:\\Users\\Raymond\\.cache\\codex-runtimes\\codex-primary-runtime\\dependencies\\native\\powershell\\pwsh.exe" -Command '$skill='"'C:\\Users\\Raymond\\.codex\\skills\\idea-spark\\SKILL.md'; "'$lines=Get-Content $skill; $lines[0..99]' in D:\Downloads\Final Year Project
 succeeded in 186ms:
---
name: idea-spark
description: "Run or resume the corpus-driven Idea Spark pipeline: pattern-card retrieval, staged research-idea generation, falsification audits, and persistent run artifacts. Use when this structured pipeline is wanted; ordinary brainstorming, paper summaries, and standalone novelty checks do not require it."
---

# Idea Spark Skill

Local routing: this is the full staged pipeline, not a prerequisite for ordinary
research reasoning. Its historical corpus supplies patterns, not evidence of
current novelty. Keep current primary-source retrieval and all run-state gates
when this pipeline is selected. Preserve the current model unless the user
requests another; no external model transport is implied by selecting this skill.


Convert an under-specified research direction into ONE reviewer-defensible Oral-level research proposal — grounded in 1947 ICLR/ICML/NeurIPS papers (2021-2025) — via a 5-phase workflow: retrieve recent literature, diagnose the bottleneck, select + generate a candidate using corpus-derived ideation pattern cards, run it through a quality gauntlet, expand into an idea card.

This file is the operational runbook. Design rationale (the 7 design principles, why each contract is shaped this way, removed-check history) lives in [references/design-notes.md](references/design-notes.md) — read it only when modifying or evaluating the skill, never needed to run it. When MODIFYING the skill, also replay the cross-shape regression set in [references/regression-directions.md](references/regression-directions.md) (deterministic subset: `python3 "$SKILL_DIR/scripts/regression_check.py" <run_dir>`; routing/merger branch fixtures — including every guard/retry branch that real runs rarely exercise: `python3 "$SKILL_DIR/scripts/selftest_routing.py"`; unit fixtures for the helpers every phase sits on — the multi-query round-robin and the tolerant LLM-JSON loader: `python3 "$SKILL_DIR/scripts/selftest_units.py"`). First-time installation lives in [references/setup.md](references/setup.md).

## When to use

- "Give me a research idea in {area} I could pursue." / "What's the most impactful next step in this direction?"
- "Help me sharpen this vague direction into an Oral-level proposal."
- "What's the bottleneck of this problem?" / "Run a novelty audit on this idea."

## When NOT to use

- Code review, debugging, refactoring. Summarizing one paper. Cross-decade survey writing.
- Free-association brainstorming with no research context. Engineering integration tasks ("ship this feature in our system").
- Pure benchmark / dataset construction work — the 15-pattern vocabulary handles benchmark *audit* (controlled_diagnostic_design) but not benchmark *construction*.

## Setup (first use only)

Follow [references/setup.md](references/setup.md). Quick version — set two shell variables, install deps, verify:

```bash
SKILL_DIR=<path to this folder>                  # e.g. ~/.claude/skills/idea-spark (Claude Code), ~/.codex/skills/idea_spark (Codex CLI), or any clone location
RUN_DIR="$PWD/ideaspark_run/<topic-slug>"   # convention below; any absolute dir works. No mkdir needed — every phase mkdir -p's its own --out, and `next` treats a missing dir as a fresh run
python3 -m pip install feedparser openreview-py beautifulsoup4 pymupdf
python3 "$SKILL_DIR/scripts/run.py" check_connectors   # from the SAME shell you'll run phases from
```

Credentials go in `.env` (OpenReview user/pass + Semantic Scholar key — see setup.md); the orchestrator auto-loads it. Optional: `xelatex`/`tectonic` for PDF cards.

---

## How to run: the `next` loop

The canonical way to drive a run is the **run-state navigator**:

```bash
python3 "$SKILL_DIR/scripts/run.py" next --dir "$RUN_DIR" --query "<user's research question>"
```

**Run-dir convention (one run = one directory, named by the host BEFORE the first command):** `$PWD/ideaspark_run/<topic-slug>` — a short kebab-case slug distilled from the user's direction (e.g. `ideaspark_run/diffusion-watermark`); if the slug is taken, append `_2`, not a timestamp. NEVER reuse a directory that already contains a `phase0/` — every phase writes into `$RUN_DIR` and would clobber the prior run. The skill itself never names the dir (any absolute path works); this convention exists so runs from different harnesses land in one predictable place instead of each agent improvising.

`next` inspects the artifacts already on disk and prints EXACTLY one next step — either a Bash command to run verbatim, or an LLM sub-agent spec (system-prompt path + input file paths + output path + the routing signal to report back). It is read-only and idempotent (safe to re-run anytime, including to resume an interrupted run) — including on a run dir that does not exist yet, which it reports as a fresh run and answers with the Phase 0 step rather than an error. You never need to create the directory before calling it. The host loop is:

1. Run `next`.
2. Do what it says (`bash` → run the command; `llm_subagent` → execute in an ISOLATED context per the Context discipline rules below).
3. Run `next` again. Repeat until it reports a terminal state (`DONE`, `do_not_generate`, or `phase_3_failed`).

**Consume every emit WHOLE — never grep/filter/truncate the block.** INPUT lists span unlabeled continuation lines; a label-keyed grep (`grep -E "STEP|INPUT|..."`) silently drops them. This exact failure occurred in a live run: the navigator listed the coherence gate's blocking findings as an audit input, the host's grep dropped the line, and the audit issued a false `advance` without ever seeing the executed evidence. If the emit must be captured into a bounded tool result, `head -c 4000` the WHOLE block — never filter by line labels.

`next` encodes the full phase graph — the mandatory full-text gate, the citation gate, the abandon-retry branch, the falsification re-audit branch, and the correct Phase 4 flags per path — so you do not need to memorize the reference tables below; they exist for deviation and debugging.

If your host exposes a task/todo tool (e.g., TodoWrite), seed it with this checklist and tick phases as `next` moves past them:

```
- [ ] Phase 0: Literature grounding → lit_table.md, then Phase 0+ full-text fetch (MANDATORY — Phase 1 hard-gates on it)
- [ ] Phase 1: Bottleneck identification → phase1_output.json (routing: proceed | do_not_generate)
- [ ] Phase 2: Gap×pattern selection + candidate generation (ONE isolated context, TWO output files) → citation gate → coherence gate (dry-run trace, fresh context)
- [ ] Phase 3: Collision retrieval (signature@10mo + alias@48mo — launchable in parallel with 2.3) → audit (5 checks) → [revise → merge → re-audit if falsification rewritten] | [abandon → information-gain retry: regenerate while each failure yields NEW binding lessons, ≤3 candidate cycles; repeated subsumption lesson → 1 bottleneck re-diagnosis + 1 attempt; no new information or cap → phase_3_failed]
- [ ] Phase 4: skeleton → fill (technical) → assemble partial → derive (plain, fast-tier) → assemble final + method view → implementability audit → validate → render → return 3 cards inline
```

Three outcomes per run: the rendered idea-card markdown returned inline (LaTeX + per-phase JSON left under `$RUN_DIR`), a `do_not_generate.md` (Phase 1 OOD), or a `phase_3_failed.md` (audit abandons twice). **Never ask the user mid-flow** — missing intake fields are inferred; revision, falsification re-audit, and the single internal retry all run without user re-invocation.

### Invocation contract

**No `cd` is required.** `scripts/run.py` self-locates its skill root, so every orchestrator command can be invoked from ANY working directory by absolute script path: `python3 "$SKILL_DIR/scripts/run.py" <subcommand> --out "$RUN_DIR/<phase>/" ...`. The legacy form `cd "$SKILL_DIR" && python3 -m scripts.run <subcommand> ...` works identically. Do NOT use relative script or `--out` paths — CWD is not stable across host-LLM Bash invocations, and the orchestrator rejects a relative `--out` outright.

**Exit codes 10 and 11 are NOT errors — they are sentinel handshakes.** When the orchestrator can't call an LLM itself (no `NOVELTY_LLM_CLASSIFY_FAST_CMD`), it writes a sentinel JSON describing what the host LLM should do, then exits rc=10 (intent / pattern-summary) or rc=11 (signature_terms). Read the sentinel (`$RUN_DIR/<phase>/.<step>_pending`), read the file at its `rubric_file` field (absolute path), produce the expected output, re-invoke per its `re_invocation` field. Do not stop on these codes. (The default Phase 0 flow below avoids the rc=10 intent sentinel entirely by passing `--queries` up front.)

### Context discipline (read BEFORE running any LLM-driven phase)

A full run accumulates ~180-250k tokens of intermediate state. If the host LLM carries that in its own conversation context across phases, the Phase 1 / 2.2 / 4.fill calls routinely hit the backend request timeout (`[API Error · Request timed out · Retrying...]`) and the retry times out again. Apply ALL three rules on every run:

**Rule 1 — Run every LLM-driven phase in an ISOLATED context.** Phases 1 / 2 (2.1+2.2) / 2.3 / 3.2 / 3.3 / 4.fill / 4.1.5 each have file-path inputs and one JSON output; no phase needs the conversation that produced an earlier one. Use the FIRST isolation mechanism your harness supports:

- **(a) Subprocess LLM** — set `NOVELTY_LLM_REASONING_LARGE_CMD` / `NOVELTY_LLM_CLASSIFY_FAST_CMD` (see § Configuration); each phase runs as its own subprocess, fresh context by construction, on any harness.
- **(b) Sub-agent tool** (Claude Code `Agent` or equivalent) — spawn one per phase, passing ONLY the file paths the phase prompt lists — not conversation history, not file contents inline. The sub-agent reads from disk, `Write`s to disk, returns ≤ 250 words (output path + routing signal). Exception by design: Phase 2.1 and 2.2 run in ONE sub-agent writing both output files — both are generation-side; the adversarial separations (3.2 vs 3.3, 4.fill vs 4.1.5) must stay separate calls.
- **(c) Manual context reset** — run inline but clear/compact at the four points in Rule 3.

Whichever mechanism, the parent context stays ≤ ~30k tokens for the whole run because it never holds a phase's structured output.

**Rule 2 — `Write` every phase artifact directly to disk; never paraphrase it into chat.** Output convention: `$RUN_DIR/<phase>/<phase>_output.json`. Use your harness's file-write tool (Claude Code: `Write`) — no Bash heredocs (permission prompts + silent truncation), no `echo`, no pasting JSON into replies. Bound tool-result captures from large files to ≤ 4 KB (`head -c 4000` / `jq` / `sed`); never `Read` a >10 KB intermediate dump into the parent context — the dump gets cached into every subsequent turn (this exact anti-pattern caused prior timeout runs).

**Rule 3 — Compact between phases.** Natural compact points: after Phase 0+, after Phase 1, after Phase 2, after Phase 3.2. Every phase re-reads its disk inputs, so compacting loses nothing. With `/compact`, use it there; Rule 1 mechanisms (a)/(b) achieve the same on their own.

**Diagnostic for "Request timed out" mid-phase:** inspect your harness's session transcript/log (Claude Code: `~/.claude/projects/<project-slug>/<session-id>.jsonl`, look for `isApiErrorMessage: true`; other harnesses: their session-log equivalent); the prior tool call shows which prompt got too big. The fix is one of the three rules — usually Rule 1.

exec
"C:\\Users\\Raymond\\.cache\\codex-runtimes\\codex-primary-runtime\\dependencies\\native\\powershell\\pwsh.exe" -Command '$skill='"'C:\\Users\\Raymond\\.codex\\skills\\idea-spark\\SKILL.md'; "'$lines=Get-Content $skill; $lines[100..199]' in D:\Downloads\Final Year Project
 succeeded in 126ms:

---

## Phase reference

`next` prints each of these steps at the right moment with concrete paths; the tables below are the full contract for deviation/debugging.

### Orchestrator entry points

| Phase | Entry point (`python3 "$SKILL_DIR/scripts/run.py" ...`, any CWD) |
|---|---|
| navigator | `next --dir "$RUN_DIR" [--query "..."]` |
| Phase 0 | `phase0 --query "<user text>" --queries "q1\|q2\|q3\|q4" [--named-papers "Title A\|Title B"] --out $RUN_DIR/phase0/` |
| user-ref registration (title-named anchor papers; BEFORE phase0_fulltext) | `add_user_ref --out $RUN_DIR/phase0/ --title "<full title>" [--raw-match "<user phrasing>"] [--id <arxiv/DOI/URL>]` |
| relevance partition apply (Phase 0.4; archives off_topic + stamps core/adjacent, BEFORE tagging) | `apply_partition --out $RUN_DIR/phase0/ --partition <relevance_partition.json>` |
| host-ref resolution (Phase 0.5 coverage check; verifies + merges host-nominated missing papers) | `add_host_refs --out $RUN_DIR/phase0/ --refs <noms.json>` |
| Phase 0+ full-text (**mandatory**, the moment lit_table.md lands) | `phase0_fulltext --out $RUN_DIR/phase0/` |
| Phase 1 anchor top-up (optional, when the #1 closest_adjacent fell outside the fulltext pool **or came back method-thin**) | `phase1_fulltext_topup --out $RUN_DIR/phase0/ --paper-id <anchor paper_id> [--min-method-chars 4000]` |
| Phase 2 prep (deterministic; `next` emits it with the Phase 2 step) | `phase2_prepare --dir $RUN_DIR` |
| lit_table shard assembly (deterministic; after parallel pattern tagging) | `lit_table_merge --out $RUN_DIR/phase0/ --shards <rows1.md> <rows2.md> ...` |
| Phase 3.1 collision | `phase3_collision --idea-json <canonical candidate> --out $RUN_DIR/phase3_collision/` |
| Phase 3.3 merger | `phase3_merge_revisions --phase2 <canonical candidate> --revisions <p3.3-patch> --critique <p3.2-report> --out $RUN_DIR/phase3_revise/` |
| Phase 2.3 merger (same tool; only when coherence verdict=patched) | `phase3_merge_revisions --phase2 <p2.2-output> --revisions <p2.3-output> --out $RUN_DIR/phase2_coherence/ --out-name refined_candidate.json` |
| Phase 4 skeleton | `phase4_skeleton --candidate <final_candidate-or-p2.2> --phase1 ... --phase2-select ... --phase3-critique ... [--phase3-revise ...] --phase0-dir $RUN_DIR/phase0/ [--collision ...] --out $RUN_DIR/phase4/` |
| Phase 4 assemble (repeat `--fill-map` to merge the derive_map on the final pass) | `phase4_assemble --skeleton $RUN_DIR/phase4/phase4_skeleton.json --fill-map $RUN_DIR/phase4/fill_map.json [--fill-map $RUN_DIR/phase4/derive_map.json] --out $RUN_DIR/phase4/` |
| Phase 4 method view (before 4.1.5; `next` bundles it with the final assemble) | `phase4_method_view --expansion $RUN_DIR/phase4/phase4_expansion.json --out $RUN_DIR/phase4/` |
| Phase 4 render | `phase4_render --expansion $RUN_DIR/phase4/phase4_expansion.json --out $RUN_DIR/phase4/` |
| Validators | `validate --phase2 ... [--phase3 ...] [--phase4 ...] [--phase4-impl ...]` |

The LLM-driven phases (1 / 2.1 / 2.2 / 2.3 / 3.2 / 3.3 / 4.fill / 4.1.5 / falsification re-audit) have no orchestrator subcommand (a `cat prompt | llm` wrapper would add fragility without determinism): read the prompt at `references/system-prompts/<phase>.txt`, gather the inputs listed at its top, `Write` the JSON described under `Output:` to `$RUN_DIR/<phase>/<phase>_output.json`. Run each under the Context discipline rules — Phase 4.fill is the largest output and the most timeout-prone; never in the parent context.

### Phase 0 — Literature grounding

Phase 0 and 3.1 require **real external retrieval** via the bundled connector scripts (`scripts/search_*.py`) — never WebSearch or ad-hoc fetch (downstream phases reject unstructured output). Gate sentinel: `.lit_grounding_mode` = `real` vs `connector_failure` (halt with diagnostic; `--allow-webfallback` exists as a flagged, lower-confidence escape).

**Default flow (skips one sentinel round-trip):** BEFORE invoking `phase0`, read `references/intent-recognition.md` (Map mode) yourself and produce **4** search queries (3-5 only with a stated reason) — including one ESCAPE-MECHANISM query phrased in solution vocabulary (recalls papers that already fixed the bottleneck and title themselves by their fix; problem-keyed queries miss exactly those), and apply that file's two query tests — VOCABULARY-OWNERSHIP and CONCRETE-OBJECT — to EVERY query, not only the escape one; its count rule gates a 5th query on quality per slot, not on the count. Phrase queries mechanism-first: generic "`<topic>` challenges/overview/landscape" phrasings are survey magnets that dilute corpus density — the orchestrator demotes survey-titled hits to the bottom of lit_results (kept, never dropped), but each connector's cap slots are still spent on them. Also apply the OOD short-circuit (intake-routing.md triggers #1 Too-broad / #2 No-anchor → route to do_not_generate instead of retrieving). Then invoke with BOTH flags:

```bash
python3 "$SKILL_DIR/scripts/run.py" phase0 --query "<user's research question>" --queries "q1|q2|q3|q4" --out $RUN_DIR/phase0/
```

The rc=10 sentinel path still exists as fallback when `--queries` is omitted. The orchestrator: asserts a sane clock; runs a list of retrieval JOBS (a connector may run more than one window) and merges them SS-priority. Default jobs: arxiv 0-6mo cap 40 · **ss_recent 0-6mo cap 30** (the freshness window is covered by TWO engines — arXiv's weak lexical API AND SS's stronger recent search — because gap freshness lives here and one retriever's ranking is a single point of failure) · openalex 6-24mo cap 30 published-only · semanticscholar 6-24mo cap 30 published-only · openreview 0-6mo cap 10 in-review · oa_recent 0-6mo **cap 0 (off by default)**. Caps are **wide on purpose** — narrower ones saturated, and the Phase 0.4 partition (below) drops the extra noise before it reaches the gap corpus (design-notes). ~140 pre-dedup, ~110-130 after dedup, ~50-75 after the partition drops off_topic. Window OVERLAP is intentional now (a second engine re-covers the freshness window; SS-first dedup collapses duplicates and enriches arXiv records with externalIds) — the old non-overlapping rule is retired. All caps are overridable per job via `IDEASPARK_POOL` (see § Configuration). Pass `--named-papers` whenever the query name-drops a paper or system without a link: the URL/ID regex only sees links, so a bare name is otherwise invisible to the U fetch tier and never becomes an anchor the candidate is differentiated against (add one later with `add_user_ref`). Each name is resolved against the connectors by CONTAINMENT — the paper's title must lead with the name, since a user writes the nickname and not the title — and a name two papers lead with is reported AMBIGUOUS and left unresolved rather than guessed, because a wrong U-tier entry is uncapped, always deep-read and becomes an anchor. Pass that one as a URL instead. It writes the user's question verbatim to `phase0/user_query.txt` — the 0.4 partition, the 0.5 coverage check, Phase 1 and Phase 2.1 are all pointed at that FILE rather than at a string the host retypes, so a paraphrase cannot silently propagate and a compacted run can still recover what was asked. It also extracts URL/ID user-refs from the query into `phase0/user_refs.json`; emits `.pattern_summary_pending` for the host.

**Phase 0.4 relevance partition (host precision gate; DEFAULT ON, disable with `IDEASPARK_RELEVANCE_PARTITION=off`):** because retrieval now over-fetches, the host — in ONE compact pass over every record's title+abstract — labels each `core | adjacent | off_topic`, writing `phase0/relevance_partition.json` `[{paper_id, relevance, reason}]`. Use YOUR OWN model (open-ended relevance judgment — do NOT downgrade); be CONSERVATIVE — when unsure between core and adjacent pick core, hard-label off_topic ONLY for clear cross-domain false positives (broad "memory-augmented" matches from wireless/recommendation/NLP), pure surveys, or unrelated fields (a wrong off_topic is an unrecoverable recall loss). Then `apply_partition --out phase0/ --partition <file>` (deterministic) stamps `relevance` on each record, **archives off_topic to `off_topic.md` and drops it from `lit_results.json`**, and touches `.partition_applied`, and writes **`query_yield.md`** — the Phase 0.4 labels joined back onto the `from_query` provenance the connectors stamp, i.e. each query's core/adjacent/off_topic yield. That report is how the query-set design question stops being a judgement call: a query whose share is mostly off_topic is spending guaranteed round-robin slots on noise and should be dropped or rephrased next run (see the VOCABULARY-OWNERSHIP TEST in references/intent-recognition.md). Papers reachable from several queries are credited to each. This runs BEFORE tagging so the per-paper pattern-tag pass only sees survivors, and it **replaces the weak Haiku `outside_taxonomy` tag** as the precision gate. `core` alone feeds the deep-read pool; `adjacent` stays as a citeable baseline/backbone but never consumes a fetch slot. `next` drives the sub-flow; the marker guards re-runs.

Retrieval takes 3-10 min (the openreview connector alone budgets 600s) — set your Bash timeout ≥ 600s or run it in the background.

**Pattern tagging (host step):** classify each `lit_results.json` paper per `references/pattern-summary-rubric.md` into 1-3 of the 15 patterns → write `lit_table.md` with columns `paper_id | year_month | venue | title | ideation pattern tags | bottleneck this paper targets | open issue / unresolved gap | resolves_problem | retrieved_via`. Pure classification — it does not need the large reasoning model: route it to a cheaper/faster model tier or a lower reasoning effort BY DEFAULT (the same tier `NOVELTY_LLM_CLASSIFY_FAST_CMD` names in § Configuration; spending the large model here is pure waste); only when no cheaper tier exists, run it isolated on the host model. Rows are per-paper independent, so for 40+ papers the tagging MAY be sharded across 2-3 parallel fast-tier sub-agents (contiguous slices; assemble with the deterministic `lit_table_merge --out $RUN_DIR/phase0/ --shards <row-file>...` — it validates 9-column shape, row count == paper count, and paper_id coverage against lit_results.json, replacing hand-checks) — wall-clock win, no quality delta.

**Phase 0.5 coverage check (host-recall channel, host step; DEFAULT ON, disable with `IDEASPARK_COVERAGE_CHECK=off`):** after tagging, before fulltext, the host — having read the whole table — names up to 8 clearly load-bearing works the retrieval pool MISSED, writing `phase0/host_refs_nominations.json` `[{title, id_hint?, why, source: parametric|websearch}]`. It PRIORITIZES the last ~12 months (recent/frontier work the dated retrieval windows under-sampled — the target that keeps the diagnosed gap current); older foundational papers (canonical base policies, >12-month landmarks) are mostly Phase 1 lineage's job and should be nominated only as a small minority, and only when a load-bearing backbone/baseline the candidate builds on or is measured against. Use YOUR OWN model here (open-ended intent judgment — do NOT downgrade to a cheap tier); WebSearch is allowed ONLY in this step and ONLY to find titles, never to fabricate a record; an empty `[]` is a valid honest output. Then `add_host_refs --out phase0/ --refs <noms>` (deterministic) VERIFIES each nomination via the SS/arXiv connectors (>=0.9 title match) — verified records merge into `lit_results.json` with `retrieved_via=host_recall|host_web`; unresolvable titles (likely hallucinations) are NOT admitted, they land in `host_refs_unresolved.md`; admitted ids are recorded in `host_refs.json` for provenance. Newly-admitted rows get tagged (fast tier) and `lit_table_merge`d in; `.coverage_check_done` guards against re-running. This keeps `lit_grounding_mode` honestly `real` (every record is connector-verified) while letting host knowledge repair a thin or stale pool. `next` drives the whole sub-flow. Admitted refs feed the fulltext H tier (below).

**Title-named user refs:** if the user query names anchor papers by TITLE ("based on the LoRA paper" — anything the URL/ID regex can't catch), register each BEFORE `phase0_fulltext` via `add_user_ref` (entry-point table). It does a deterministic dedup-merge into `user_refs.json` — do NOT hand-edit that file (some harnesses' file-write tools refuse to overwrite a file that was never read, and a malformed edit silently drops the U fetch tier).

**Phase 0+ full-text fetch — MANDATORY.** The instant `lit_table.md` lands (and after the coverage check), run `phase0_fulltext` (entry-point table) before touching Phase 1. Pool = U (user refs, never capped) + **H (host-recall refs, retrieved_via host_*, cap 6)** + T2 (published on-topic, cap 10) + T3 (arxiv recent on-topic, cap 15; also absorbs unused H/T2 slots), ceiling **25** excluding U, method-first ordering, concurrent fetch (HTML path first, pymupdf PDF fallback; per-paper budget so one slow PDF can't stall the step). **Only `core`-tagged papers are eligible** (Phase 0.4 relevance stamp; U bypasses, H needs its nomination tagged core) — `adjacent` papers stay citeable at abstract level but never consume a deep-read slot. Any pooled paper that fetches to an **empty method section is backfilled** — swapped for the next-ranked core reserve that has one (one-out-one-in, ceiling held; `--backfill` reserve size, default 6). Output `fulltext_cache.json` keyed by paper_id (`{tier, intro, method, source_used, warning}`); fetch failures degrade to abstract + warning. Phase 1 **hard-gates** on this file (`error: fulltext_not_fetched`). Alongside the blob it writes a derived per-paper read view (`phase0/fulltext/index.json` + one `.md` per paper, same content, index carries tier/source_used/warning) — Phase 1 reads the index then only the papers it needs; the blob stays canonical and `phase1_fulltext_topup` refreshes both. Fetches hit a cross-run content cache (`~/.cache/ideaspark/fulltext/`, successful fetches only, 30-day TTL; disable with `IDEASPARK_FETCH_CACHE=off`), so papers recurring across runs on adjacent topics cost zero network time.

### Phase 1 — Bottleneck identification

One isolated LLM call. Prompt: [references/system-prompts/bottleneck_identify.txt](references/system-prompts/bottleneck_identify.txt). Inputs: user query + intake, `phase0/lit_table.md`, `phase0/fulltext_cache.json` (all-failed cache → continue with `fulltext_degraded: true`, abstract-level residue confidence), `phase0/lit_results.json`. Output `phase1/phase1_output.json`: `intake` (+`_inferred_fields[]` — missing fields are inferred, never asked), `bottleneck_statement` (≥2 paper_id cited inline), `closest_adjacent[]` (`{paper_id, summary_and_residue}`), `what_phase_0_did_not_address[]` (each gap ends with an inline stakes clause — practitioner costs and intellectual costs are coequal; gaps are framed as structural properties of a problem class when honestly true, with the anchor as primary instance, else declared class-of-one), `state ∈ {proceed, do_not_generate}`.

Routing: **proceed** (literature-groundable, no OOD trigger) or **do_not_generate** (too-broad / no-anchor OOD, <5 truly-relevant papers, genuinely blank space, or benchmark/system construction) → write `do_not_generate.md` with concrete remedial steps — terminal.

### Phase 2 — Selection + generation (ONE isolated context, TWO outputs)

Run 2.1 and 2.2 back-to-back in one isolated context, writing BOTH output files (they are both generation-side; only adversarial pairs need separate calls):

**2.1** — prompt [references/system-prompts/ideate_select.txt](references/system-prompts/ideate_select.txt); inputs `phase1_output.json`, `references/ideation-patterns/overview.md` (all 15 patterns' Definition / Operational signature / When to apply — selection at WHAT/WHEN level), `references/ideation-patterns/companion-combos.md`, `lit_table.md`. Pick the anchor gap (type-bound to `intake.contribution_type`); commit the PATTERN COMPOSITION — ≥2 distinct patterns by default, realized preferentially as a CHAIN on the anchor (attested second pattern from companion-combos, with a named intermediate object) and/or via a sibling that EARNS its seat through the removal test ("the anchor's story is incomplete without it") — anchor-only is legitimate and common (1-3 gaps total, ≤2 siblings); a single-pattern selection requires a `composition_note` defense (validator presence-checks it; audit weighs it). Record saturation (transparency, not a filter). Output `phase2_select/phase2_select_output.json`: `selected_gaps[]` (index 0 = anchor) + `coherence_thread_type` (or `n_a` when anchor-only) + `composition_note` + `pattern_saturation` + `deferred_gaps[]`. **Retry mode:** when `$RUN_DIR/attempt_1/` exists, the prompt's OPTIONAL retry input applies — the archived audit + selection become negative constraints; anchor-only is a valid retry outcome. **Cross-run dedup:** `next` scans sibling run dirs and appends a soft-negative-anchor input line (titles + signature terms of their canonical candidates, 5 most recent) so adjacent-direction runs don't silently re-invent the same mechanism family; soft by design (see ideate_select.txt's OPTIONAL cross-run input), disable with `IDEASPARK_CROSS_RUN_DEDUP=off`.

**2.2** — prompt [references/system-prompts/ideate_generate.txt](references/system-prompts/ideate_generate.txt); inputs 2.1 output, `phase1_output.json`, `lit_results.json` (closest_adjacent entries ONLY — filter to those paper_ids before reading; the prompt forbids pulling the full dump into context), plus for each gap ONE picked sub-pattern card from `references/ideation-sub-patterns/` (compare `when_to_pick_this_one` + `differentiation_within_parent` via its overview.md; then read the picked card's `tactical_pattern` + Step-by-Step). Output `phase2_generate/phase2_generate_output.json` — ONE candidate, 12 flat fields: `title` / `hook` / `core_mechanism` / `core_mechanism_reasoning` / `core_mechanism_steps`; `gap_closure[]` (`{gap, main_pattern, sub_pattern: "C## (parent pattern name)", how_closed}`, mirrors selected_gaps one-for-one); `falsification_prediction` (single paragraph: minimal experiment + metric-with-direction + ONE named load-bearing variable + negative control on that variable predicting the DOWNSTREAM outcome metric returns to baseline — non-tautological); `compute_budget` (user-relative, GPU-day line + API-dollar line when the campaign calls paid APIs; default intake envelope = 80GB-class GPUs, ≤8 concurrent, ≈150 GPU-days / 5 months, ~$10k API — overridable per user via `IDEASPARK_DEFAULT_COMPUTE`, see § Configuration); `differentiation_from_lit[]` (substantive deltas, not "different pattern"); `almost_prior_paper_id` + `what_step_was_missed`; `signature_terms[]` (own vocabulary — recent collision channel); `alias_terms[]` (other communities' names for the same mechanism, from parametric knowledge — multi-year alias collision channel); `composition_note` (echoed verbatim from 2.1). Reality constraints at writing time: `core_mechanism_reasoning` carries three mandatory blocks — a PREMISES ledger (load-bearing premises + domain justification; bets tagged `untested — falsification target`; WHEN the mechanism consumes sampled/observed data, at least one OBSERVATION-MODEL premise — how are the inputs sampled, is that unbiased here — else the literal `observation-model: n/a`; WHEN the mechanism estimates any quantity, declare the ESTIMAND precisely), a NAIVE-BASELINE AUDIT (state the naive version; why doesn't it already work — branch (i) naive relies on a false premise → confronting it IS the contribution, with a STANDARD-TOOL FOLLOW-UP: if the confrontation is a textbook tool from another field, name the domain-specific structure that makes this instance unsolved or declare the contribution application-grade, (ii) naive suffices → incremental signal surfaced honestly, (iii) naive works but the field disbelieves → minimalism with evidence), then design rationale — plus every invoked dataset / model-access level / annotation / tool must be a nameable EXISTING artifact (hard rule 3; modest self-built resources allowed with cost counted in compute_budget) and every claim stated at its defensible strength — guarantee-grade wording only with assumptions stated where it appears (hard rule 4; the coherence gate grades this, and a strong claim with honest assumptions beats a hedged weak one) — and every NUMBER carrying provenance (hard rule 5): method parameters are named symbols with a default + selection rule (a bare `>= 95%` / `top 5` / `every 2e3` is forbidden — an unnamed quantity cannot be swept or graded), while `falsification_prediction` states metric + direction + control and may carry a numeric bar ONLY when tagged `derived:` or `measured in <paper_id>`; invented bars are fabrication. Artifact versions, algebraic constants, definitional settings and experiment-scale counts are not parameters and are unaffected. Both kill-switch fields (`falsification_prediction`, `compute_budget`) are locked from here on — see Phase 3 for the single audited exception.

**Citation gate (deterministic, MANDATORY before Phase 3):**

```bash
python3 "$SKILL_DIR/scripts/run.py" validate --phase2 $RUN_DIR/phase2_generate/phase2_generate_output.json
```

Any `fail` = a `sub_pattern` citation was guessed from the parent's gist, not read from `overview.md`. Fix against `references/ideation-sub-patterns/overview.md` (or regenerate 2.2 with the card open) and re-run until clean — the gate proves parent-consistency only; whether core_mechanism performs the cluster's actual tactic is Phase 3.2's `recipe_application_check`. (`next` runs this gate automatically.)

**Coherence gate (2.3 — one isolated LLM call, MANDATORY after the citation gate, before the 3.2 audit; 3.1 collision may run CONCURRENTLY with it):** prompt [references/system-prompts/coherence_trace.txt](references/system-prompts/coherence_trace.txt); inputs: the 2.2 candidate + the 2.1 spec; MUST be a FRESH context, never the 2.1+2.2 agent (the context that wrote a logic bug rubber-stamps it). It verifies internal procedural validity by EXECUTION, not review — five trace actions: formalize the dataflow (undefined symbols, missing producers, circular deps, and UNBOUND PARAMETERS — a number the procedure reads as a parameter but that carries no name/default/selection rule, the form a magic constant takes to evade every symbol-keyed check; method fields only, `falsification_prediction` numbers belong to the audit), numeric dry-run on one small concrete instance (magnitude/probability absurdities — logic bugs read fluently and only surface when computed; when a code-execution tool exists the gate WRITES AND RUNS a stdlib Python script and pastes script+output, else hand-computes marked `unexecuted`), degenerate probes (empty/k=0/ties), claim→step mapping AND grading (every asserted property mapped to the step that establishes it AND graded `established` / `conditional` — assumptions listed / `overclaim` — wording downgraded / `empirical`; statistical claims settled by an executed Monte Carlo with the measured number, theorem-shaped claims recorded as proof obligations — at best conditional; grading never rewards vagueness: all-hedged claims are themselves a weakness finding), and a NAIVE-BASELINE COMPARISON (independently construct the naive version — never the candidate's own stated one — run it on the same instance, and judge against the candidate's declared branch: `confronts_obstacle` / `equivalent_to_naive` / `n_a`). OBSTACLE HOLES — findings whose honest fix is a redesign, or which coincide with the obstacle the candidate declares it exists to solve (incl. `equivalent_to_naive`) — must NOT be patched around with avoidance-style repairs (abstain/clamp/skip); they go to `unrepaired[]` as blocking, verbatim. Verdict `pass` | `patched`; repairs are patch-only via the SAME merger (`--out-name refined_candidate.json`), scoped to making the written procedure sound (core_mechanism*, how_closed narrative, signature/alias terms when the repair changed what the mechanism is) — novelty surface, pattern bindings, and kill-switch fields are out of scope; unfixable-without-redesign findings go to `unrepaired[]` for the audit to weigh. Single pass, never abandons. The 3.2 audit stays blind to this report's trace/verdict/patches — with ONE exception: blocking `unrepaired[]` entries are executed evidence, so the gate ALSO writes them to `phase2_coherence/blocking_findings.json` (self-contained: verbatim step quote + script excerpt + measured numbers + reading_dependence + `structural_requirement` per entry — what it would TAKE to pass this obstacle: the identifying condition any mechanism here needs, which sources of it are available vs excluded and why, and what is worth salvaging; diagnosis, not the redesign the repair contract forbids, and honestly empty when the evidence supports nothing concrete — `next` passes it to the retry as a directive), which `next` lists as a 3.2 input; the audit must disposition each (uphold, or refute by naming a concrete modeling/arithmetic flaw — `next` deterministically bounces an audit report that skipped dispositions or advanced over an upheld finding). Formalization ambiguity is controlled by the gate's dual-reading protocol (every defensible reading of an ambiguous operative term is executed; findings are tagged reading_robust vs reading_dependent). When `refined_candidate.json` exists it is the canonical candidate for every later phase (`next` wires this automatically). It validates that the algorithm survives on paper — NOT that it works empirically (falsification experiment) or is novel (audit).

### Phase 3 — Quality gauntlet

**3.1 collision (orchestrator, no LLM):** entry-point table. May be LAUNCHED IN PARALLEL with the 2.3 gate (on the 2.2 output — `next` emits the background launch alongside the 2.3 step): collision reads only `signature_terms[]`/`alias_terms[]`, a `.collision_terms.json` sidecar records the terms actually used, and `next` re-issues collision if a coherence patch changed those terms (rare — term repairs are scoped to mechanism-changing patches). TWO retrieval channels over all 4 connectors, merged into `collision_hits.json` with a per-hit `collision_channel` tag: **signature** — the candidate's `signature_terms[]` over a 10-month window (contemporaneous scoop risk); **alias** — the candidate's `alias_terms[]` (other communities' names for the same mechanism, produced from parametric knowledge at 2.2) over a 48-month window (renamed-ancestor risk — the "goal-conditioned success detector vs goal-image conditioned scorer" blind spot is lexical, not temporal, so widening the signature window alone cannot catch it). Missing `signature_terms[]` → rc=11 sentinel: produce BOTH term sets per intent-recognition.md Collision mode (terms 3-7 words each — long sentences break URL encoding), edit the candidate JSON, re-invoke. Missing only `alias_terms[]` → loud warning, alias channel skipped (add the field and re-run to close the blind spot). The audit-facing pool is relevance-truncated per channel (≤120 hits/channel by lexical overlap with the channel's own terms; zero-relevance BM25 noise dropped unconditionally; drops printed; untruncated pool preserved as `collision_hits.full.json`), so the audit can consume `collision_hits.json` in a few sequential Read chunks — no jq two-pass triage needed.

**3.2 audit (one isolated LLM call):** prompt [references/system-prompts/critique.txt](references/system-prompts/critique.txt); inputs: candidate, 2.1 spec, `lit_table.md`, `collision_hits.json`, `references/anti-patterns.md`, `phase2_coherence/blocking_findings.json` when it exists (the 2.3 gate's executed blocking evidence — the report must disposition each entry, and advance is forbidden while one is upheld; a `refuted` disposition additionally triggers a bounded refutation re-check (`refutation_recheck.txt`, fresh call) that `next` requires before trusting the verdict — an invalid refutation counts as upheld and bounces the audit), and each cited sub-pattern card `references/ideation-sub-patterns/<C##>.md` (strip the leading code from `sub_pattern`; typically 1-3 cards, others NOT loaded). Five corpus-anchored checks:

| Check | Question |
|---|---|
| gap_closure_reject_check | does the candidate match a documented Reject lesson in each cited sub-pattern card (`## Tactical failure mode` + ALL `### Reject lessons` bullets)? |
| recipe_application_check | does `core_mechanism` actually perform the cited C## cluster's `## Tactical pattern` signature move, or only the parent's generic idea (`bypassed` — the leading cause of incremental output)? |
| anti_pattern_check | if the SET of `gap_closure[].main_pattern` matches a reject-favored composition, is the required mitigation substantively delivered (artifact, not keyword)? |
| paper_pointed_threat | most specific subsuming/competing paper in `lit_table ∪ collision_hits` (both channels; alias-channel threats are NOT discounted for age); `no_threat_found` is valid — fabricating a generic threat is forbidden. Side output `parametric_family_concern`: a named un-retrieved mechanism family from parametric knowledge (family name + query vocabulary, never specific paper cites) — soft signal only, flows to Phase 4 reviewer_concerns as a "scoop-check X first" flag |
| falsification_structure_check | does `falsification_prediction` name the minimal experiment, the outcome metric + direction, ONE load-bearing variable, and a NON-tautological negative control targeting the downstream metric — and is every numeric outcome bar sourced (`numeric_bar_provenance` ∈ none / derived / measured / asserted / invented — cost bars count, qualitative magnitude claims and design/scale/conditioning numbers do not; `invented` is a deficiency repaired by STRIKING the bar, which the contract states is a fabrication repair rather than a claim weakening, while `asserted` — a derivation claimed but left uncheckable — only routes to borderline, so a judgment call never forces a kill-switch edit)? |

Verdict is two-layer. **Hard floor** (LLM cannot override) → `abandon`: triggered Reject lesson / unmitigatable anti-pattern / exact-mechanism collision. **Soft judgment** otherwise → `advance` (only trivial borderlines; concerns surface in Phase 4's reviewer_concerns) or `revise` with concrete `revision_targets[]` (scopes: `tactical` / `sub_pattern` / `falsification`). `verdict_rationale` must cite specific check findings. The audit judges only — it never modifies the candidate.

**Routing on verdict:**

- **advance** → Phase 4 reads the 2.2 candidate directly.
- **revise** → **3.3** (one isolated LLM call, prompt [references/system-prompts/revise.txt](references/system-prompts/revise.txt)): reads candidate + 2.1 spec + the revision brief (`phase3_revise_brief` — `next` materializes `revise_brief.json`, the audit minus the bulky reject-lesson quotations; full report stays on disk for lesson-specific lookups); emits patch-only `applied_revisions[]` — one entry per revision_target, ops `replace` / `append_sentence` / `append_items` / `swap_sub_pattern` / `rewrite_falsification`, never echoes the candidate, never re-judges the verdict. Then run the **merger** (entry-point table, WITH `--critique`) → writes `phase3_revise/final_candidate.json` + back-injects it into the patch file. Kill-switch fields are merger-refused with ONE audited exception: a `scope=falsification` target from `falsification_structure_check` is applied via the dedicated `rewrite_falsification` op (authorization verified against the audit report via `--critique`; same experiment/metric/claim, structure repaired; max one per run). When the merger prints `falsification_rewritten`, run the **falsification re-audit** (self-contained prompt [references/system-prompts/falsification_reaudit.txt](references/system-prompts/falsification_reaudit.txt), reading the `phase3_falsification_view` slice of `final_candidate.json` → `phase3_critique/falsification_reaudit.json`): `advance` → Phase 4; `abandon` → `phase_3_failed.md`. `compute_budget` has no revision route under any scope. No `composition` scope — gap-level changes route through the abandon-retry below, never through patches.
- **abandon** → **information-gain retry rule** (ONE rule, no death-type taxonomy; the one-shot guarantee bars asking the user, not internal regeneration). Every failed attempt's audit is reduced to a LESSON SET — upheld executed blocking findings (mechanism-level positive directives), unaddressable subsuming papers (mechanism-family negative anchors), triggered reject/anti-pattern/recipe lessons (generation-quality constraints). A retry must carry NEW lessons the previous generation did not have: the first abandon always retries (generation ran with zero audit information); a later abandon retries only if its lesson set adds entries beyond the union of all archived attempts' lessons, with the new lessons injected as directives. A REPEATED unaddressable-subsumption lesson across attempts binds at the FRAMING level instead → one bottleneck re-diagnosis (Phase 1 retry mode; `do_not_generate` remains a legitimate exit), whose new framing gets exactly ONE candidate attempt. Termination: no new lessons, or the candidate-cycle cap (3 per framing; worst case 4 gauntlet cycles per run). `next` computes all of this deterministically from the archived critique reports. On the first abandon, archive the attempt and regenerate —

exec
"C:\\Users\\Raymond\\.cache\\codex-runtimes\\codex-primary-runtime\\dependencies\\native\\powershell\\pwsh.exe" -Command '$skill='"'C:\\Users\\Raymond\\.codex\\skills\\idea-spark\\SKILL.md'; "'$lines=Get-Content $skill; $lines[200..299]' in D:\Downloads\Final Year Project
 succeeded in 175ms:

  ```bash
  mkdir -p "$RUN_DIR/attempt_1" && \
  mv "$RUN_DIR/phase2_select" "$RUN_DIR/phase2_generate" "$RUN_DIR/phase2_coherence" "$RUN_DIR/phase3_collision" \
     "$RUN_DIR/phase3_critique" "$RUN_DIR/phase3_revise" "$RUN_DIR/attempt_1/" 2>/dev/null; \
  touch "$RUN_DIR/.retry_used"
  ```

  then re-run Phase 2 in retry mode (archived audit + selection = negative constraints; blocking obstacle findings = POSITIVE directives the new mechanism must confront), citation gate, 3.1, 3.2. Phase 0/1 artifacts are reused as-is.

  **Later `abandon`s → the same rule, deterministically** (`next` compares this attempt's lesson set against the union over `attempt_N/` archives): new mechanism-level lessons → directed retry to the next `attempt_N` (all archived audits + selections ride along as constraints; the fresh lessons are named in the emit); repeated subsumption lesson → bottleneck re-diagnosis (archive incl. `phase1/`, touch `.bottleneck_retry_used`; the re-diagnosed framing gets ONE attempt); no new lessons / cap reached / post-bottleneck failure → write `phase_3_failed.md` citing EVERY attempt's verdict_rationale + triggering checks + user-side options — terminal.

### Phase 4 — Expansion + packaging

Six steps in order (`next` emits each with the correct flags for the advance vs revise path — on the revise path `--candidate` is `final_candidate.json` and `--phase3-revise` is passed; on advance it's the CANONICAL candidate (refined_candidate.json when 2.3 patched, else the 2.2 output) and the flag is omitted):

1. **skeleton** (orchestrator): populates every mechanical field — kill-switch echoes (byte-identical from the candidate), `differentiation_from_lit` venue_years, `almost_prior_venue_year`, `why_prior_stopped[].paper_id/venue_year`, `domain_landscape` (pattern_distribution + candidate_uses), `literature_breakdown`, `reviewer_concerns_and_responses[].attack/severity/fields_changed_to_address` (lifted from audit + patch), `feasibility_validation.compute` (bucketed against `intake.compute`) — and marks every prose field `<TODO[path]: hint>`.
2. **fill** (one isolated LLM call, prompt [references/system-prompts/expand.txt](references/system-prompts/expand.txt)): author the TECHNICAL TODO paths as one flat `{path: value}` map → `phase4/fill_map.json`. The derive-owned paths (`title_zh` + all `plain_*`) are explicitly EXCLUDED — the derive step owns them. No calendar projections; no experiment matrix / ablation plan / baseline table — the skill produces IDEA + falsifiability + feasibility judgment, not experimental engineering.
3. **assemble (partial) → derive → assemble (final)**: the orchestrator assembles the technical map first (`phase4_expansion.json`, derive-owned placeholders remain — the WARN is expected); then **derive** (one isolated LLM call, prompt [references/system-prompts/derive_plain.txt](references/system-prompts/derive_plain.txt)) mechanically rewrites the finished technical fields into the plain register (`title_zh` + `plain_*` → `phase4/derive_map.json`) — register transformation + translation with NO new facts, so it runs on the CLASSIFY_FAST tier by default (fallback ladder: `NOVELTY_LLM_CLASSIFY_FAST_CMD` → host cheap model → host model isolated; even the last rung keeps it cheap since the input is only the finished prose); then the final assemble merges both maps (`phase4_assemble --fill-map <tech> --fill-map <derive>` — overlapping paths are a hard error) and extracts `phase4/method_view.json` (`phase4_method_view` — the method-only slice the 4.1.5 audit reads: method_flow + plain steps + key_equations + claims). The assembler validates every path resolves to a real TODO and refuses kill-switch roots per map.
4. **implementability audit (4.1.5, one isolated LLM call, default on):** prompt [references/system-prompts/implementability_audit.txt](references/system-prompts/implementability_audit.txt) — fresh skeptical-engineer persona (separate from the 4.fill author) reads `method_view.json` (fallback: the full expansion) and rewrites each method step into a buildable spec: `enriched_steps[]` (one per step, same ids/order, `what_changes` + `what_to_do_en` + `what_to_do_zh`) + `underspecified_points[]` (`{step_id, hole, fill, severity: filled|open}` — unfillable holes stay honest as `open`). Compute-agnostic by design (resource feasibility is 4.1's job); never adds/removes/renames steps; never carries kill-switch fields. Output `phase4/phase4_implementability.json`.
5. **validate + render:** run the validators (below), then `phase4_render` — templating only, no model call; auto-detects the sibling implementability file and merges `enriched_steps` by step_id into the rendered Method (deterministic; no-op when absent). Writes `idea.std.zh.md` (plain Chinese, domain-newcomer register) + `idea.std.en.md` (plain English) + `idea.detail.en.md` (rigorous English — the novelty + validity defense) + `idea.std.{en,zh}.tex` (auto-compiled to PDF when xelatex/tectonic is on PATH; skipped with a hint otherwise).

**Final response:** read all three markdown cards and return them inline under headings 中文版 / English / Reviewer version. Other phase outputs stay on disk for inspection, not echoed.

## Validators

```bash
# advance path: --phase3 = phase3_critique_output.json; revise path: --phase3 = phase3_revise_output.json
# --phase2 = the CANONICAL candidate (refined_candidate.json when 2.3 patched, else the 2.2 output)
python3 "$SKILL_DIR/scripts/run.py" validate \
  --phase1 $RUN_DIR/phase1/phase1_output.json \
  --phase2-select $RUN_DIR/phase2_select/phase2_select_output.json \
  --phase2 <canonical candidate file> \
  --phase3 <see comment> \
  --phase4 $RUN_DIR/phase4/phase4_expansion.json \
  --phase4-impl $RUN_DIR/phase4/phase4_implementability.json   # optional; enables implementability checks
```

| Validator | Check | Severity |
|---|---|---|
| **subpattern_citation_consistency** | each `gap_closure[].sub_pattern` resolves to a real C## cluster in overview.md whose true parent == the cited `main_pattern` and whose parenthetical == that cluster's parent display name. Primary use: the Phase 2.2 citation gate; re-runs harmlessly here. | fail (hard) |
| **alias_collateral_coverage** | `alias_terms[]` actually queries the cross-community families Phase 1 pinned as `is_collateral` nodes in `method_lineage`. Needs BOTH phase1 and phase2 paths. Runs in the Phase 2.2 citation gate — i.e. BEFORE 3.1 collision, which consumes `alias_terms[]` verbatim, so a miss caught later is a wasted retrieval budget. Zero coverage = fail; partial = warn naming the unqueried families (a family can be genuinely unreachable, and a forced fabricated term would evict real ones from a channel that truncates by lexical relevance — `composition_note` carries the skip defense, the 3.2 audit weighs it). | fail (zero) / warn (partial) |
| **kill_switch_integrity** | `falsification_prediction` + `compute_budget` byte-identical along Phase 2.2 → [3.3 final_candidate →] 4. After an audited falsification rewrite (`falsification_rewritten` marker + matching applied `rewrite_falsification` entry — disagreement fails), the anchor for `falsification_prediction` re-bases at the 3.3 final_candidate (3.3 → 4 must match); `compute_budget` stays full-chain always. | fail (hard) |
| **expansion_completeness** | motivation (≥2 `why_prior_stopped`), `method_flow.steps[]` (each with `linked_component` + `linked_falsification`), `feasibility_validation` (5 sub-verdicts + `overall`), non-empty `abstract_draft` + `core_claim` + `sub_claims[]` — missing sections would render as silent blanks. | fail (hard) |
| **implementability_completeness** | `enriched_steps[]` one-per-step (same ids/order, EN+ZH), `underspecified_points[]` present (`[]` allowed), NO kill-switch field in the file. | fail (hard) |
| **user_direction** | when `intake.user_direction` is set, `phase2_select` must carry a `user_direction_disposition` (adopted/departed, `why_departed` required on departed), and both quoted spans must appear in `phase0/user_query.txt`. Hard rule 10 keeps a user-named solution OUT of gap selection on purpose; this only forbids dropping it silently. | fail (hard) |
| **chinese_word_order** | no `_zh` field puts more than 18 characters in front of 的 without a break (an English relative clause left in pre-nominal position — the reader cannot tell what is being described until the end), and no known calque (`保留任务` for held-out, `两个位` for the two bits). | warn |
| **implementability_readability** | std-register fields: no `占位`/`placeholder` leak, no bare English jargon dropped into Chinese prose. | warn |

**Retry budget on `fail` (cap = 2).** Fix only the named contract, re-validate; still failing after the 2nd retry → stop revising, render as-is, and append a short note listing the failing validators (a flagged-imperfect card beats a watchdog-killed run with zero output). Never "fix" `kill_switch_integrity` or `subpattern_citation_consistency` by editing a guarded field — surface them as the headline caveat instead.

## Configuration

By default every model-driven phase runs on the host LLM. To route phases to a different backend (Gemini, open-weights, custom):

- `NOVELTY_LLM_REASONING_LARGE_CMD` — Phase 1 / 2.1 / 2.2 / 3.2 / 3.3 / 4.fill (needs ≥ 200k context, JSON output)
- `NOVELTY_LLM_CLASSIFY_FAST_CMD` — Phase 0 intent extraction + per-paper pattern tagging (smaller context, JSON output)

**Which tier a step tolerates — the split is by TASK KIND, not by cost** (both directions measured; see design-notes):

- **Mechanical classification against a written rubric** — per-item independent, "which of these N named categories", criteria already in the rubric. *Pattern tagging is the whole of this class.* **Cheapest tier is correct here**, and shardable across parallel sub-agents.
- **Open-ended judgement with no enumerated answer set** — "is this paper on-topic", "what load-bearing work is MISSING", "is this candidate subsumed". *Phase 0.4 partition, Phase 0.5 coverage check, and every gauntlet phase are this class.* **Do NOT downgrade these**, which is why their emits say so explicitly — a cheap tier's over-strict drop is an unrecoverable recall loss, while an over-inclusion costs one row the next stage can still catch.

With no separate cheap model, lower the REASONING EFFORT for the mechanical class rather than reaching for the largest configuration everywhere; reserve full effort for the open-ended class.

Each is a CLI taking a stdin prompt (`<<SYSTEM>>...<<USER>>...`) and emitting JSON on stdout. When unset (the default when running inside any host LLM), the orchestrator emits sentinel files and the host LLM handles those steps natively.

- `IDEASPARK_POOL` — per-job Phase 0 retrieval caps, `job=N,...` (jobs: arxiv, ss_recent, oa_recent, openalex, semanticscholar, openreview). A job at 0 is skipped (this is how oa_recent stays off; set `oa_recent=6` for journal-heavy fields). Malformed values fail-fast.
- `IDEASPARK_RETRIEVAL_CACHE` — cross-run Phase 0 retrieval cache (default `~/.cache/ideaspark/retrieval`, 24h TTL via `IDEASPARK_RETRIEVAL_CACHE_TTL_S`); set to `off` to bypass, or to a path to relocate. Keyed on connector + queries + window + caps + `--as-of`, so any real change to the request misses; successful non-empty results only. Exists because re-running Phase 0 otherwise re-hammers every API — three runs in ~15 min rate-limited arXiv and Semantic Scholar into returning zero records.
- `IDEASPARK_RETRY_PAUSE_S` — pause before the single bounded per-job retry (default 45s). A failed job also hands its cap to the surviving job covering the same window (≤2x).
- `IDEASPARK_RELEVANCE_PARTITION` — set to `off` to disable the Phase 0.4 host relevance-partition (default: on). When off, retrieval's wide net flows straight to tagging with no core/adjacent/off_topic gate (the old `outside_taxonomy`-only behavior); deep-read then falls back to on-topic (non-`outside_taxonomy`) rather than `core`-gated.
- `IDEASPARK_COVERAGE_CHECK` — set to `off` to disable the Phase 0.5 host-recall coverage check (default: on).
- `IDEASPARK_CROSS_RUN_DEDUP` — set to `off` to disable the sibling-run soft-negative-anchor scan in the Phase 2 emit (default: on).
- `IDEASPARK_DEFAULT_COMPUTE` — optional standing compute profile for the user (free text, e.g. `"8×H100 node, ~300 GPU-days, $50k API budget"`). Put it in `.env` (auto-loaded); `next` surfaces it to Phase 1 as intake context. Precedence: compute stated in the user's query > this value > the factory default (80GB-class GPUs, ≤8 concurrent, ≈150 GPU-days / 5 months, ~$10k API campaign). Use this instead of editing the factory default — the default is the feasibility yardstick for users who state nothing.

codex
已找到与用户指定内容同名的有效 prompt：实际安装源为 `...\work\ResearchStudio\ResearchStudio-Idea\skills\idea_spark\references\system-prompts\revise.txt`，而用户给出的少了 `skills\idea_spark` 这一层。该 prompt 已完整读取。现在开始读取本次 run 的 revision brief、audit、candidate、selection、supplemental constraints 与 host review；只提取用于 patch 的字段与约束，不会把整份 candidate 回写或回显。
exec
"C:\\Users\\Raymond\\.cache\\codex-runtimes\\codex-primary-runtime\\dependencies\\native\\powershell\\pwsh.exe" -Command '$paths = @(
'"'D:\\Downloads\\Final Year Project\\ideaspark_run\\world-action-model-quantization\\phase2_coherence\\refined_candidate.json',
'D:\\Downloads\\Final Year Project\\ideaspark_run\\world-action-model-quantization\\phase2_select\\phase2_select_output.json',
'D:\\Downloads\\Final Year Project\\ideaspark_run\\world-action-model-quantization\\phase3_revise\\revise_brief.json',
'D:\\Downloads\\Final Year Project\\ideaspark_run\\world-action-model-quantization\\phase3_critique\\phase3_critique_output.json',
'D:\\Downloads\\Final Year Project\\ideaspark_run\\world-action-model-quantization\\host_supplemental_constraints.md',
'D:\\Downloads\\Final Year Project\\ideaspark_run\\INITIAL_CANDIDATE_REVIEW.md'
)
Get-Item -LiteralPath "'$paths | Select-Object FullName,Length,LastWriteTime | Format-Table -AutoSize
Write-Output '"'--- candidate top-level keys/id and guarded fields ---'
"'$c=Get-Content -Raw -LiteralPath $paths[0] | ConvertFrom-Json
[pscustomobject]@{candidate_id=$c.candidate_id; keys=($c.psobject.Properties.Name -join '"', '); falsification_prediction="'$c.falsification_prediction; compute_budget=$c.compute_budget} | ConvertTo-Json -Depth 10
Write-Output '"'--- selection ---'
Get-Content -Raw -LiteralPath "'$paths[1]
Write-Output '"'--- revise brief ---'
Get-Content -Raw -LiteralPath "'$paths[2]
Write-Output '"'--- critique target summary ---'
"'$cr=Get-Content -Raw -LiteralPath $paths[3] | ConvertFrom-Json
$cr.psobject.Properties | Where-Object Name -in @('"'verdict','verdict_rationale','revision_targets','falsification_structure_check','paper_pointed_threat','parametric_family_concern','gap_closure_reject_check','recipe_application_check','anti_pattern_check') | ForEach-Object { \"["'$($_.Name)]"; $_.Value | ConvertTo-Json -Depth 20 }
Write-Output '"'--- host supplemental constraints ---'
Get-Content -Raw -LiteralPath "'$paths[4]
Write-Output '"'--- host review ---'
Get-Content -Raw -LiteralPath "'$paths[5]' in D:\Downloads\Final Year Project
 succeeded in 184ms:

FullName
--------                                                                                                               
D:\Downloads\Final Year Project\ideaspark_run\world-action-model-quantization\phase2_coherence\refined_candidate.json  
D:\Downloads\Final Year Project\ideaspark_run\world-action-model-quantization\phase2_select\phase2_select_output.json  
D:\Downloads\Final Year Project\ideaspark_run\world-action-model-quantization\phase3_revise\revise_brief.json          
D:\Downloads\Final Year Project\ideaspark_run\world-action-model-quantization\phase3_critique\phase3_critique_output.j…
D:\Downloads\Final Year Project\ideaspark_run\world-action-model-quantization\host_supplemental_constraints.md         
D:\Downloads\Final Year Project\ideaspark_run\INITIAL_CANDIDATE_REVIEW.md                                              

--- candidate top-level keys/id and guarded fields ---
{
  "candidate_id": null,
  "keys": "title, hook, hook_shape_rationale, gap_closure, core_mechanism, core_mechanism_reasoning, core_mechanism_steps, falsification_prediction, compute_budget, differentiation_from_lit, almost_prior_paper_id, what_step_was_missed, signature_terms, alias_terms, composition_note",
  "falsification_prediction": "Run the minimal experiment on the exact Fast-WAM checkpoint_FW in idm mode with sigma_shift_FW=1.0 and infer_action, using a small held-out RoboTwin 2.0/LIBERO manipulation split, the same low-bit setting b_lo, and the same high-precision budget B for local-score and C_r allocations. If the candidate works, the contact-transition subset will show a larger local-score-versus-C_r ranking divergence and the C_r schedule will reduce held-out contact-sensitive task failures and next-observation deviation relative to the matched local-score schedule, without changing the architecture or precision budget. The single load-bearing variable is C_r, the paired downstream consequence index; permute C_r across the site registry before selecting top-B sites, and the predicted effect is that the downstream contact-sensitive success/failure advantage disappears even though the quantizer and evaluation remain unchanged. A positive control is the oracle simulator-state branch that exposes the independent transition outcome during calibration; it should preserve more of the C_r allocation advantage than a model-only local discrepancy if the mechanism, rather than calibration noise, is carrying the result.",
  "compute_budget": "1.2 GPU-day on an 80GB-class equivalent for a staged Fast-WAM-only validation (approximately two short single-GPU A100-40GB stages, no large-model training) + $0 paid API; LingBot-VA replication, if later added, requires a separate calibration-memory and inference budget."
}
--- selection ---
{
  "selected_gaps": [
    {
      "gap": "additive: No retrieved WAM-quantization leaf establishes the downstream consequence of a numerical perturbation at a module or denoising step for the final action chunk and the next closed-loop observation in contact-critical manipulation; this is the unresolved residue of QuantWAMs (arxiv:2607.28405v1), not a request for a particular remedy. — stakes: practitioners may deploy a memory- and latency-efficient WAM that preserves local calibration metrics but fails on contact-sensitive actions, while researchers may mistake local fidelity for transferable closed-loop reliability.",
      "chosen_pattern_id": "reframe_as_solvable_object",
      "anchor_alternates": [],
      "companion_pattern": "architectural_operator_substitution",
      "companion_rationale": "reframe_as_solvable_object turns each module or denoising-step numerical perturbation into the named intermediate object O, a counterfactual closed-loop consequence functional over the final action chunk and next observation; architectural_operator_substitution consumes O to replace the layer-local fidelity score with a runnable precision-scoring operator.",
      "selection_rationale": "This is the load-bearing construction gap because the committed contribution_type is method and the bottleneck is the missing action-level and next-observation consequence signal that a deployable precision decision must produce. The reframe makes that consequence a computable object, while the chained operator substitution turns it into the runnable method artifact without reverting to a local PTQ score, latent rollout/planning surrogate, or QuantWAMs fixed-count schedule repair."
    },
    {
      "gap": "subtractive: No retrieved WAM-quantization leaf tests whether the local numerical-fidelity surrogate inherited from layer-local PTQ is actually sufficient for precision selection in the coupled video/action setting, rather than an assumed proxy for downstream task impact; this questions the inherited assumption and leaves the replacement open. — stakes: precision-allocation conclusions can rest on an unexamined surrogate and invalidate comparisons across WAMs or tasks, while deployment teams cannot tell whether an apparent speed or memory gain preserves the action behavior they require.",
      "chosen_pattern_id": "controlled_diagnostic_design",
      "anchor_alternates": [],
      "companion_pattern": null,
      "companion_rationale": "",
      "selection_rationale": "This sibling shares the anchor's central downstream-consequence object and tests the same precision decision against the local-fidelity surrogate rather than introducing a separate architecture or data source. It passes the removal test because without the controlled comparison the anchor's operator has no isolating evidence that local fidelity is insufficient, leaving an obvious reviewer objection about why the replacement score is needed."
    }
  ],
  "coherence_thread_type": "shared_object",
  "deliverable_coverage": "method — anchor gap → reframe_as_solvable_object defines the downstream-consequence object and its architectural_operator_substitution companion ships the runnable precision-scoring and selection mechanism.",
  "user_direction_disposition": {
    "verdict": "n_a"
  },
  "composition_note": "",
  "pattern_saturation": {
    "reframe_as_solvable_object": {
      "share": 0.15,
      "saturation_band": "mid_frequency",
      "selection_note": ""
    },
    "architectural_operator_substitution": {
      "share": 0.19,
      "saturation_band": "mid_frequency",
      "selection_note": ""
    },
    "controlled_diagnostic_design": {
      "share": 0.07,
      "saturation_band": "mid_frequency",
      "selection_note": ""
    }
  },
  "deferred_gaps": []
}

--- revise brief ---
{
  "recipe_application_check": {
    "entries": [
      {
        "gap": "additive: No retrieved WAM-quantization leaf establishes the downstream consequence of a numerical perturbation at a module or denoising step for the final action chunk and the next closed-loop observation in contact-critical manipulation; this is the unresolved residue of QuantWAMs (arxiv:2607.28405v1), not a request for a particular remedy. — stakes: practitioners may deploy a memory- and latency-efficient WAM that preserves local calibration metrics but fails on contact-sensitive actions, while researchers may mistake local fidelity for transferable closed-loop reliability.",
        "sub_pattern": "C00 (Reframe as a Solvable Object)",
        "tactical_pattern_quoted": "This cluster recasts an opaque transparency question about an already-trained model or dataset — which feature/datum/internal component is responsible, how uncertain a prediction is, what a hidden representation encodes — as a concrete measurement over a well-studied object: a manifold-constrained counterfactual optimization, a submodular subset selection, an expectation/estimator over marginal statistics, an additive decomposition into a (often sparse) basis, or an influence/Shapley score accumulated along the optimization trajectory.",
        "instantiation_in_core_mechanism": "The candidate defines the precise per-site object `C_r`, fixes r to an actual WAM branch/module/denoising-step/axis registry, constrains `F_0` versus `F_r` to matched history, seeds, simulator state, and cache, and validates the resulting action-to-observation counterfactual through an external simulator and held-out schedule test. It performs the C00 signature operation rather than merely renaming a local score.",
        "verdict": "applied"
      },
      {
        "gap": "subtractive: No retrieved WAM-quantization leaf tests whether the local numerical-fidelity surrogate inherited from layer-local PTQ is actually sufficient for precision selection in the coupled video/action setting, rather than an assumed proxy for downstream task impact; this questions the inherited assumption and leaves the replacement open. — stakes: precision-allocation conclusions can rest on an unexamined surrogate and invalidate comparisons across WAMs or tasks, while deployment teams cannot tell whether an apparent speed or memory gain preserves the action behavior they require.",
        "sub_pattern": "C02 (Design a Confound-Isolating Diagnostic)",
        "tactical_pattern_quoted": "The shared move is to construct a controlled set of evaluation instances in which exactly one named confound is either structurally blocked or independently varied, then report the divergence between apparent and de-confounded performance as the primary result rather than an aggregate leaderboard score.",
        "instantiation_in_core_mechanism": "The candidate holds site, history, instruction, denoising/model RNG, simulator state, controller state, history buffers, cache, and precision budget fixed while comparing `L_r` with `C_r`, stratifying native contact/task-transition states against non-contact states, and using a score-permuted control plus held-out task outcomes. The simulator transition is independent of the WAM's own score, satisfying the required external-oracle role.",
        "verdict": "applied"
      }
    ],
    "verdict": "applied",
    "reasoning": "Both cluster-specific signature moves are present: C00 supplies a formal native-site counterfactual object with a faithfulness intervention, and C02 supplies controlled same-context confound isolation with an external transition oracle and divergence outcome."
  },
  "anti_pattern_check": {
    "composition_set": [
      "reframe_as_solvable_object",
      "architectural_operator_substitution",
      "controlled_diagnostic_design"
    ],
    "matched_pattern_id": null,
    "required_mitigation_quoted": null,
    "mitigation_substantively_delivered": "n/a",
    "reasoning": "No gap_closure entry uses heterogeneous_decomposition, so none of the three documented reject-favored compositions matches, including the operator-plus-decomposition pair."
  },
  "paper_pointed_threat": {
    "threat_paper_id": "arxiv:2607.28405v1",
    "threat_source": "lit_table",
    "threat_channel": null,
    "subsumption_argument": "QuantWAMs is the closest corpus threat: it already covers joint video/action empirical-Fisher cross terms over block and denoising-step granularity and fixed-count closed-loop protection-schedule repair. That subsumes any claim that joint coupling, denoising-step sensitivity, closed-loop protection, or contact-critical precision allocation is novel. The candidate survives only on a narrower concrete intervention and estimand: single-site numerical W/A PTQ with all other target sites at FP16, paired full action-chunk plus first-action-to-next-observation counterfactuals over `D_cal`, and `C_r` ranking; it explicitly disclaims the jointly low-bit marginal, joint optimality, task-success-loss identity, and full replan consequence. QuantWM is a distinct weight/activation PTQ and long-horizon latent rollout/planning branch, not this coupled manipulation-WAM estimator. VAML, Model Advantage / Value-Aware Model Learning, and Deep Task-Based Quantization already establish the broad task-consequence principle for choosing a quantization target, so that principle is not novel. SQIL already protects mission-critical states through saliency-weighted QAT and action distillation, so critical-state or contact-state protection is not novel by itself; SQIL is not identical to a single-site WAM simulator PTQ intervention, but the candidate must compare their intervention, estimand, and numerical mechanism explicitly. The branch remains distinct from latent rollout/planning, latent/action vector quantization, and VLA-only/tokenizer quantization only if it keeps the claim at this concrete W/A, one-control-transition operator.",
    "addressable_via": "Extend `differentiation_from_lit` with explicit concrete intervention, estimand, and numerical-mechanism comparisons for QuantWM, QuantWAMs, VAML, Model Advantage / Value-Aware Model Learning, Deep Task-Based Quantization, and SQIL, while stating that broad downstream-consequence targeting and critical-state protection are prior art.",
    "parametric_family_concern": null
  },
  "falsification_structure_check": {
    "minimal_experiment_named": "yes",
    "outcome_metric_named": "yes",
    "load_bearing_variable": "C_r",
    "negative_control_target": "outcome_metric",
    "numeric_bar_provenance": "none",
    "verdict": "sound",
    "reasoning": "The prediction names the Fast-WAM setup, matched local-score and `C_r` schedules, contact-sensitive task failures and next-observation deviation, and a registry permutation whose predicted effect is on those downstream outcomes. No unsupported numeric outcome bar is asserted in `falsification_prediction`."
  },
  "blocking_findings_disposition": [
    {
      "finding_ref": "coherence note: `C_r` is still a single-site FP16-background score, so its value is not the marginal consequence of the jointly low-bit deployment",
      "status": "upheld",
      "basis": "The synthetic T2 construction is a valid non-blocking limitation: `C_r` selects r2 while the independent joint residual-loss table favors r3. The candidate explicitly disclaims joint marginal and joint-optimality claims, so the held-out schedule comparison must test transfer rather than silently treating `C_r` as a joint optimizer."
    },
    {
      "finding_ref": "coherence note: the action-chunk MSE plus next-observation pixel L1 remains a one-transition proxy, not task-success loss or full-chunk/replan consequence",
      "status": "upheld",
      "basis": "The T2 arithmetic genuinely contains only `d_a`, `d_o`, and `C_r`; full action chunks are scored but only the first executable action changes the simulator. This is a non-blocking estimand boundary because the candidate says so explicitly and reserves task validation for S9; claiming a true full-horizon task consequence would require a different mechanism."
    },
    {
      "finding_ref": "coherence note: the primary `B_site` site-count match does not establish equal bytes, peak memory, or latency across schedules",
      "status": "upheld",
      "basis": "The 9:1 same-count byte stress is arithmetically valid and no actual tensor, scale, live-range, layout, or kernel measurement is supplied. The candidate limits `B_site` to the experimental matching unit and requests a separate real-kernel cost ledger, so this is a non-blocking cost boundary rather than evidence of equal deployment cost."
    },
    {
      "finding_ref": "coherence note: the exact Optional IDM `checkpoint_FW` identity is not present in the candidate",
      "status": "upheld",
      "basis": "The supplied files contain only the `checkpoint_FW` placeholder and no filename, repository revision, or digest, so the finding cannot be refuted without inventing evidence. The manifest-and-halt gate is the correct pre-execution treatment; results remain unverifiable until that identity is resolved."
    },
    {
      "finding_ref": "coherence note: the 1.2 GPU-day 80GB-class estimate is not a measured duration conversion for A100 40GB stages",
      "status": "upheld",
      "basis": "The executed arithmetic supports only the nominal 28.8-hour convention; it does not establish memory equivalence, stage walltime, or calibration fit on A100-40GB. This remains an unverified `compute_budget` claim with no Phase 3.2 repair route, and must not be presented as a measured A100 conversion."
    }
  ],
  "verdict": "revise",
  "verdict_layer": "soft_judgment",
  "verdict_rationale": "`gap_closure_reject_check` is borderline rather than triggered: the C00 porting/faithfulness lessons and C02 confirmatory-transfer risks remain, but the causal paired intervention, external simulator, negative control, and held-out comparison prevent a yes-level reject lesson; `recipe_application_check` is applied and `falsification_structure_check` is sound. `anti_pattern_check.mitigation_substantively_delivered` is n/a, while `paper_pointed_threat.subsumption_argument` shows that QuantWAMs, QuantWM, the earlier task-consequence lineage, and SQIL own the broad claims, so `differentiation_from_lit` and the executable state-restore semantics need revision. The five coherence notes are upheld as explicit non-blocking limits; an honest full-chunk/replan task estimand would change the mechanism and cannot be silently claimed.",
  "revision_targets": [
    {
      "scope": "tactical",
      "field": "differentiation_from_lit",
      "issue": "`paper_pointed_threat` and the C00 porting lesson: the current field does not explicitly compare the candidate's concrete intervention, estimand, and numerical mechanism against the full required prior set.",
      "fix_direction": "Add explicit, claim-bounded comparisons for QuantWM, QuantWAMs, VAML, Model Advantage / Value-Aware Model Learning, Deep Task-Based Quantization, and SQIL. State that broad downstream-consequence targeting and critical-state protection are prior art, and preserve novelty only for the coupled WAM numerical W/A single-site FP16-background intervention, one-control-transition estimand, and full-chunk/next-observation numerical operator."
    },
    {
      "scope": "tactical",
      "field": "core_mechanism_steps",
      "issue": "INITIAL_CANDIDATE_REVIEW item 4 and the coherence restoration concern: controller, RNG, history, and cache are named, but the proposal does not yet give an executable branch-cloning and cache-validity protocol.",
      "fix_direction": "Specify the concrete save, clone, restore, and equality-check order for controller state, simulator RNG, model/generator RNG, observation/history buffers, persistent cache, and the action transformation; require fresh per-branch restoration and rebuild or invalidation of cache entries touched by r before F_0 and F_r. Keep the same first-action environment estimand and full-chunk d_a definition unchanged."
    },
    {
      "scope": "tactical",
      "field": "hook",
      "issue": "The proxy boundary is explicit in `core_mechanism`, but the hook still places a task-failure prediction next to a one-transition `C_r` score without stating that transfer to task outcome is an empirical hypothesis.",
      "fix_direction": "Add a sentence that C_r is not task loss, full-chunk physical consequence, or replan-horizon marginal gain; task-failure reduction is a held-out predictive test. If the intended contribution is instead an actual full-horizon task-consequence estimator, route to abandon or redesign rather than relabeling this one-transition mechanism."
    }
  ],
  "_brief_note": "gap_closure_reject_check details omitted for context economy — full report at D:\\Downloads\\Final Year Project\\ideaspark_run\\world-action-model-quantization\\phase3_critique\\phase3_critique_output.json; read it ONLY if a revision_target's `issue` cites a specific reject lesson you need verbatim."
}
--- critique target summary ---
[gap_closure_reject_check]
{
  "entries": [
    {
      "gap": "additive: No retrieved WAM-quantization leaf establishes the downstream consequence of a numerical perturbation at a module or denoising step for the final action chunk and the next closed-loop observation in contact-critical manipulation; this is the unresolved residue of QuantWAMs (arxiv:2607.28405v1), not a request for a particular remedy. — stakes: practitioners may deploy a memory- and latency-efficient WAM that preserves local calibration metrics but fails on contact-sensitive actions, while researchers may mistake local fidelity for transferable closed-loop reliability.",
      "main_pattern": "reframe_as_solvable_object",
      "sub_pattern": "C00 (Reframe as a Solvable Object)",
      "tactical_failure_mode_quoted": "The signature failure is treating the reframing as a mechanical wrapper: taking an established attribution, decomposition, or estimation tool and pointing it at a new input granularity, a new architecture, or a new domain without re-deriving why the resulting scores are faithful to the model.",
      "reject_lessons_evaluated": [
        {
          "lesson_quoted": "Pointing an off-the-shelf decomposition at a coarser input granularity without showing the coarser units preserve the signal reads as a wrapper, not a contribution.",
          "candidate_match": "no",
          "reasoning": "The candidate does not use an off-the-shelf decomposition; it defines a site registry and a paired simulator counterfactual at native numerical intervention sites."
        },
        {
          "lesson_quoted": "Porting a known attribution technique to a new architecture or domain while ignoring that technique's documented failure modes — and omitting comparisons to alternatives — leaves reviewers unable to see any unique benefit.",
          "candidate_match": "borderline",
          "reasoning": "The WAM transfer is accompanied by local-score, QuantWM, and QuantWAMs comparisons and causal intervention, but the explicit VAML, Model Advantage / Value-Aware Model Learning, Deep Task-Based Quantization, and SQIL comparisons are absent from `differentiation_from_lit`."
        },
        {
          "lesson_quoted": "Proposing a new attribution definition without an axiomatic or causal argument for why it should count as ground-truth collapses the moment a reviewer asks why this is the real importance.",
          "candidate_match": "borderline",
          "reasoning": "Matched state restoration, an external simulator transition, a permutation control, and held-out task testing provide a causal validation path, but `C_r` is an explicitly chosen action/observation proxy rather than an axiomatic task-importance ground truth."
        },
        {
          "lesson_quoted": "Conflating a methodological artifact — a computation-granularity difference, a degenerate zero baseline — with genuine signal undermines the entire premise once the confound is identified.",
          "candidate_match": "no",
          "reasoning": "The candidate separates local `L_r` from `C_r`, specifies epsilon floors and population normalizers, guards constant vectors, and includes a score-permuted negative control."
        },
        {
          "lesson_quoted": "Building a suppression or privacy method without formalizing the threat model leaves the central guarantee unverifiable regardless of strong empirical numbers.",
          "candidate_match": "no",
          "reasoning": "The candidate is a numerical precision-allocation method, not a suppression or privacy method with a threat-model guarantee."
        },
        {
          "lesson_quoted": "Surveying or recombining fragmented methods without a unifying axis or a decisive controlled experiment yields a contribution reviewers dismiss as anecdotal.",
          "candidate_match": "no",
          "reasoning": "The shared `C_r` object, native WAM intervention registry, paired transition experiment, and held-out schedule comparison provide a unifying axis and decisive experiment."
        }
      ],
      "verdict": "borderline"
    },
    {
      "gap": "subtractive: No retrieved WAM-quantization leaf tests whether the local numerical-fidelity surrogate inherited from layer-local PTQ is actually sufficient for precision selection in the coupled video/action setting, rather than an assumed proxy for downstream task impact; this questions the inherited assumption and leaves the replacement open. — stakes: precision-allocation conclusions can rest on an unexamined surrogate and invalidate comparisons across WAMs or tasks, while deployment teams cannot tell whether an apparent speed or memory gain preserves the action behavior they require.",
      "main_pattern": "controlled_diagnostic_design",
      "sub_pattern": "C02 (Design a Confound-Isolating Diagnostic)",
      "tactical_failure_mode_quoted": "Papers attempting this tactic fail most often when the constructed instrument isolates the confound only inside an artificial regime and never demonstrates that the isolated signal transfers to real deployment conditions — the synthetic-to-real gap is asserted rather than shown.",
      "reject_lessons_evaluated": [
        {
          "lesson_quoted": "A diagnostic that only re-confirms a shortcut already known in advance, surfacing no previously unmeasurable failure, is read as confirmatory rather than as a contribution.",
          "candidate_match": "borderline",
          "reasoning": "QuantWAMs already motivates closed-loop calibration concerns, while the executed toy divergence is synthetic; the proposed contact-regime held-out WAM result must show a new action-mediated failure rather than merely restating that local metrics can disagree."
        },
        {
          "lesson_quoted": "Validating a synthetic-ground-truth instrument only on synthetic data, without demonstrating that the controlled finding transfers to real-world instances, leaves ecological validity unestablished and undermines the central claim.",
          "candidate_match": "borderline",
          "reasoning": "RoboTwin 2.0 and LIBERO held-out episodes plus task metrics provide an external simulator regime beyond the two-episode toy, but the candidate does not establish transfer to physical-robot deployment and must keep its scope to the evaluated simulator WAM setting."
        },
        {
          "lesson_quoted": "Building the instrument around one contested definition of the target property means it cannot independently confirm that the definition is correct, and reviewers flag the resulting circularity.",
          "candidate_match": "no",
          "reasoning": "The transition is produced by the simulator and its native contact/task flag, while held-out task failure is an outcome separate from the `C_r` definition."
        },
        {
          "lesson_quoted": "Resting an unsupervised evaluation on the unverified premise that the systems under test can reliably judge one another stakes the entire method on an assumption that collapses under scrutiny.",
          "candidate_match": "no",
          "reasoning": "The method uses an external simulator and paired numerical traces rather than one evaluated system judging another."
        },
        {
          "lesson_quoted": "Pairing a single-task or small-sample instrument with broad capability claims triggers a scope-mismatch rejection, and assembling retrieval or engineering blocks without an isolating scientific insight reads as application rather than research.",
          "candidate_match": "borderline",
          "reasoning": "The two benchmark families, contact/non-contact stratification, and negative control support an isolating insight, but `N_cal=32` and one-transition measurement cannot support broad capability or real-world claims."
        }
      ],
      "verdict": "borderline"
    }
  ],
  "verdict": "borderline",
  "reasoning": "The aggregate is driven by the C00 porting/faithfulness concerns and the C02 risk that the synthetic toy divergence becomes merely confirmatory. The candidate has a concrete causal paired intervention, an external simulator oracle, and a held-out outcome test, so no reject lesson is a yes match; the remaining concern is explicit claim and prior-art scoping."
}
[recipe_application_check]
{
  "entries": [
    {
      "gap": "additive: No retrieved WAM-quantization leaf establishes the downstream consequence of a numerical perturbation at a module or denoising step for the final action chunk and the next closed-loop observation in contact-critical manipulation; this is the unresolved residue of QuantWAMs (arxiv:2607.28405v1), not a request for a particular remedy. — stakes: practitioners may deploy a memory- and latency-efficient WAM that preserves local calibration metrics but fails on contact-sensitive actions, while researchers may mistake local fidelity for transferable closed-loop reliability.",
      "sub_pattern": "C00 (Reframe as a Solvable Object)",
      "tactical_pattern_quoted": "This cluster recasts an opaque transparency question about an already-trained model or dataset — which feature/datum/internal component is responsible, how uncertain a prediction is, what a hidden representation encodes — as a concrete measurement over a well-studied object: a manifold-constrained counterfactual optimization, a submodular subset selection, an expectation/estimator over marginal statistics, an additive decomposition into a (often sparse) basis, or an influence/Shapley score accumulated along the optimization trajectory.",
      "instantiation_in_core_mechanism": "The candidate defines the precise per-site object `C_r`, fixes r to an actual WAM branch/module/denoising-step/axis registry, constrains `F_0` versus `F_r` to matched history, seeds, simulator state, and cache, and validates the resulting action-to-observation counterfactual through an external simulator and held-out schedule test. It performs the C00 signature operation rather than merely renaming a local score.",
      "verdict": "applied"
    },
    {
      "gap": "subtractive: No retrieved WAM-quantization leaf tests whether the local numerical-fidelity surrogate inherited from layer-local PTQ is actually sufficient for precision selection in the coupled video/action setting, rather than an assumed proxy for downstream task impact; this questions the inherited assumption and leaves the replacement open. — stakes: precision-allocation conclusions can rest on an unexamined surrogate and invalidate comparisons across WAMs or tasks, while deployment teams cannot tell whether an apparent speed or memory gain preserves the action behavior they require.",
      "sub_pattern": "C02 (Design a Confound-Isolating Diagnostic)",
      "tactical_pattern_quoted": "The shared move is to construct a controlled set of evaluation instances in which exactly one named confound is either structurally blocked or independently varied, then report the divergence between apparent and de-confounded performance as the primary result rather than an aggregate leaderboard score.",
      "instantiation_in_core_mechanism": "The candidate holds site, history, instruction, denoising/model RNG, simulator state, controller state, history buffers, cache, and precision budget fixed while comparing `L_r` with `C_r`, stratifying native contact/task-transition states against non-contact states, and using a score-permuted control plus held-out task outcomes. The simulator transition is independent of the WAM's own score, satisfying the required external-oracle role.",
      "verdict": "applied"
    }
  ],
  "verdict": "applied",
  "reasoning": "Both cluster-specific signature moves are present: C00 supplies a formal native-site counterfactual object with a faithfulness intervention, and C02 supplies controlled same-context confound isolation with an external transition oracle and divergence outcome."
}
[anti_pattern_check]
{
  "composition_set": [
    "reframe_as_solvable_object",
    "architectural_operator_substitution",
    "controlled_diagnostic_design"
  ],
  "matched_pattern_id": null,
  "required_mitigation_quoted": null,
  "mitigation_substantively_delivered": "n/a",
  "reasoning": "No gap_closure entry uses heterogeneous_decomposition, so none of the three documented reject-favored compositions matches, including the operator-plus-decomposition pair."
}
[paper_pointed_threat]
{
  "threat_paper_id": "arxiv:2607.28405v1",
  "threat_source": "lit_table",
  "threat_channel": null,
  "subsumption_argument": "QuantWAMs is the closest corpus threat: it already covers joint video/action empirical-Fisher cross terms over block and denoising-step granularity and fixed-count closed-loop protection-schedule repair. That subsumes any claim that joint coupling, denoising-step sensitivity, closed-loop protection, or contact-critical precision allocation is novel. The candidate survives only on a narrower concrete intervention and estimand: single-site numerical W/A PTQ with all other target sites at FP16, paired full action-chunk plus first-action-to-next-observation counterfactuals over `D_cal`, and `C_r` ranking; it explicitly disclaims the jointly low-bit marginal, joint optimality, task-success-loss identity, and full replan consequence. QuantWM is a distinct weight/activation PTQ and long-horizon latent rollout/planning branch, not this coupled manipulation-WAM estimator. VAML, Model Advantage / Value-Aware Model Learning, and Deep Task-Based Quantization already establish the broad task-consequence principle for choosing a quantization target, so that principle is not novel. SQIL already protects mission-critical states through saliency-weighted QAT and action distillation, so critical-state or contact-state protection is not novel by itself; SQIL is not identical to a single-site WAM simulator PTQ intervention, but the candidate must compare their intervention, estimand, and numerical mechanism explicitly. The branch remains distinct from latent rollout/planning, latent/action vector quantization, and VLA-only/tokenizer quantization only if it keeps the claim at this concrete W/A, one-control-transition operator.",
  "addressable_via": "Extend `differentiation_from_lit` with explicit concrete intervention, estimand, and numerical-mechanism comparisons for QuantWM, QuantWAMs, VAML, Model Advantage / Value-Aware Model Learning, Deep Task-Based Quantization, and SQIL, while stating that broad downstream-consequence targeting and critical-state protection are prior art.",
  "parametric_family_concern": null
}
[falsification_structure_check]
{
  "minimal_experiment_named": "yes",
  "outcome_metric_named": "yes",
  "load_bearing_variable": "C_r",
  "negative_control_target": "outcome_metric",
  "numeric_bar_provenance": "none",
  "verdict": "sound",
  "reasoning": "The prediction names the Fast-WAM setup, matched local-score and `C_r` schedules, contact-sensitive task failures and next-observation deviation, and a registry permutation whose predicted effect is on those downstream outcomes. No unsupported numeric outcome bar is asserted in `falsification_prediction`."
}
[verdict]
"revise"
[verdict_rationale]
"`gap_closure_reject_check` is borderline rather than triggered: the C00 porting/faithfulness lessons and C02 confirmatory-transfer risks remain, but the causal paired intervention, external simulator, negative control, and held-out comparison prevent a yes-level reject lesson; `recipe_application_check` is applied and `falsification_structure_check` is sound. `anti_pattern_check.mitigation_substantively_delivered` is n/a, while `paper_pointed_threat.subsumption_argument` shows that QuantWAMs, QuantWM, the earlier task-consequence lineage, and SQIL own the broad claims, so `differentiation_from_lit` and the executable state-restore semantics need revision. The five coherence notes are upheld as explicit non-blocking limits; an honest full-chunk/replan task estimand would change the mechanism and cannot be silently claimed."
[revision_targets]
[
  {
    "scope": "tactical",
    "field": "differentiation_from_lit",
    "issue": "`paper_pointed_threat` and the C00 porting lesson: the current field does not explicitly compare the candidate's concrete intervention, estimand, and numerical mechanism against the full required prior set.",
    "fix_direction": "Add explicit, claim-bounded comparisons for QuantWM, QuantWAMs, VAML, Model Advantage / Value-Aware Model Learning, Deep Task-Based Quantization, and SQIL. State that broad downstream-consequence targeting and critical-state protection are prior art, and preserve novelty only for the coupled WAM numerical W/A single-site FP16-background intervention, one-control-transition estimand, and full-chunk/next-observation numerical operator."
  },
  {
    "scope": "tactical",
    "field": "core_mechanism_steps",
    "issue": "INITIAL_CANDIDATE_REVIEW item 4 and the coherence restoration concern: controller, RNG, history, and cache are named, but the proposal does not yet give an executable branch-cloning and cache-validity protocol.",
    "fix_direction": "Specify the concrete save, clone, restore, and equality-check order for controller state, simulator RNG, model/generator RNG, observation/history buffers, persistent cache, and the action transformation; require fresh per-branch restoration and rebuild or invalidation of cache entries touched by r before F_0 and F_r. Keep the same first-action environment estimand and full-chunk d_a definition unchanged."
  },
  {
    "scope": "tactical",
    "field": "hook",
    "issue": "The proxy boundary is explicit in `core_mechanism`, but the hook still places a task-failure prediction next to a one-transition `C_r` score without stating that transfer to task outcome is an empirical hypothesis.",
    "fix_direction": "Add a sentence that C_r is not task loss, full-chunk physical consequence, or replan-horizon marginal gain; task-failure reduction is a held-out predictive test. If the intended contribution is instead an actual full-horizon task-consequence estimator, route to abandon or redesign rather than relabeling this one-transition mechanism."
  }
]
--- host supplemental constraints ---
# Host supplemental constraints

This file is a host-side constraint for the Idea Spark run. It is not a
pipeline retrieval record, citation, full-text gate, collision result, or
novelty certificate. Claims about papers must still be supported by this run's
connector records and full-text artifacts.

## Direct prior-art boundaries

- QuantWM, *An Empirical Study of World Model Quantization*, arXiv:2602.02110v1,
  already covers encoder/predictor asymmetric sensitivity, rollout/planning
  mismatch, and the fact that more planning optimization need not repair
  aggressive low-bit failures.
- *Where Bits Matter in World-Model Planning*, arXiv:2602.11882v1, already
  covers paired mixed-bit/planner-budget comparisons and module-aware allocation
  questions for latent world-model planning.
- QuantWAMs, *QuantWAMs: Calibrating at the Right Granularity for World Action
  Models*, arXiv:2607.28405v1, already contains the joint video/action
  empirical-Fisher cross term in Eq. 12--16 and fixed-count closed-loop
  protection-schedule repair in Eq. 22. Renaming those mechanisms is not novel.

## Fast-WAM feasibility boundary

- The official FastWAM README documents an Optional IDM mode on the same
  checkpoint, with `idm` versus `first_frame` modes. The original checkpoint
  uses `sigma_shift=5.0`; the Optional IDM setting uses `sigma_shift=1.0`.
- `infer_action` no longer defaults to VAE decoding in that README.
- If Fast-WAM is used as a feasibility baseline, the candidate must lock the
  exact checkpoint, mode, sigma-shift, and inference path. A `first_frame` run
  that completely skips the future branch cannot be treated as a baseline that
  tests protection of future-conditioning signals.

## LingBot-VA feasibility boundary

- The current official LingBot-VA README reports roughly 24 GB for RoboTwin
  single-GPU evaluation with VAE/text-encoder CPU offload, and roughly 18 GB
  for i2av with the same offload.
- These figures establish only the reported inference scope. They do not imply
  that backward-based calibration or empirical-Fisher profiling fits in 40 GB.
  Any candidate using LingBot-VA must budget calibration separately from
  inference and state the offload/checkpoint/inference configuration.

## Interpretation boundaries

- Keep this branch focused on manipulation WAMs that couple future prediction
  with action generation and on numerical weight/activation quantization.
- Keep it distinct from the main branch's latent rollout/planning route, from
  ordinary VLA-only quantization, and from latent/action vector quantization.
- CPU traces can test implementation or arithmetic consistency only; they do not
  establish robot success, GPU memory, or speedup. Fake-quant/dequantized GEMM
  must remain separate from real low-bit kernels.

## Earlier task-consequence quantization lineage

- Host review also identifies VAML (AISTATS 2017), Model Advantage / Value-Aware
  Model Learning (arXiv:2106.14080), and Deep Task-Based Quantization
  (arXiv:1908.06845) in the pre-48-month mechanism lineage.
- These records do not automatically subsume a manipulation WAM method, but the
  broad principle of defining a quantization target by downstream task
  consequence is already present in the lineage. It cannot be presented as
  novelty. Later audits must compare the concrete intervention, estimand, and
  numerical mechanism against these records.

## Direct adjacent robot-control prior

- SQIL, *Saliency-Aware Quantized Imitation Learning for Efficient Robotic
  Control*, ICCV 2025, arXiv:2505.15304, with the official project page
  `https://aiha-lab.github.io/sqil/` and arXiv page
  `https://arxiv.org/abs/2505.15304`, is a direct adjacent prior identified by
  host review.
- Its mission-critical-state protection and saliency-weighted QAT/action
  distillation mean that protecting contact or otherwise critical states is not
  by itself novel. The later audit must explicitly compare SQIL's concrete
  intervention, estimand, and numerical mechanism with any WAM paired-simulator
  site intervention; it must not treat SQIL as automatically identical to a
  coupled future-video/action WAM method.

--- host review ---
# 初版候选：主代理独立检查

2026-09-08。对象为两条分支初次 `phase2_generate_output.json`；不是 pipeline 审稿结论，也不修改 guarded fields。以下问题应由 coherence / critique / implementability 各自按证据处理。

## RankCal

1. **单点与联合配置的差异。** I_g 在其余位置均为 FP16 时估计，但最终是许多位置同时量化。累加 I_g 的分配目标可能被交互项破坏。应执行至少一个两位置协同/抵消反例，明确这一分数只是启发式，或说明可识别部署影响所需条件；不可声称为联合最优。
2. **预算和负对照可实现性。** 各位置的 peak-byte cost 通常不能直接相加为整体峰值；activation 生存期、共享 tensor、weight copies、scales 和实际 kernel layout 都影响结果。任意置换 bit assignments 不一定同时保持 bit histogram 和 bytes；需要实际可执行的匹配规则。
3. **quantizer 支持边界。** `torch.ao.quantization` 不是所有 W4A4/W8A4 组合在 A100 上已有可用执行路径的证明。fake quant 和实际 kernel 支持应分开。
4. **验证规模。** 六轴 full nuisance grid 的组合数量很大，M0 又由预算决定。12 GPU-days 是未实测估计；不能把大网格称为已经可在单卡轻松完成的最小试验。此处仅报告风险，不擅自改写锁定的 compute_budget/falsification。
5. **novelty。** 需要对照 VAML / VaGraM 等旧谱系，以及普通 task-aware PTQ。teacher-relative rank agreement 不直接等于真实环境任务质量。

## Causal Consequence Calibration

1. **同样存在干预背景不一致。** C_r 在 all-other-FP16 背景估计，却在联合 low-bit 配置中用作 top-B protection；它并不天然等于量化部署背景中的边际收益。
2. **被估计量与任务后果。** action-chunk MSE + 下一帧 pixel L1 是 paired downstream discrepancy，不等于任务成功、接触风险或 marginal task gain。一步观测还不能识别后续 chunk/replan 的全部后果；需明确假设与可证伪范围。
3. **保护单位与真实代价。** branch/module/denoising-step/axis tuple 的数量预算不自动匹配真实 bytes 或 latency。动态 weight precision 的多个副本与切换成本、activation schedule 必须纳入部署边界。
4. **可复现干预。** 相同 simulator state 必须包含必要的 controller/RNG/history/cache 状态；仅重置可见位置不必然产生有效配对。最终实现须给出可执行的恢复语义。
5. **模型与预算身份。** 要锁定真正有 future/action 耦合的 Optional IDM checkpoint 与 idm 模式，而非普通 first_frame checkpoint。1.2 GPU-day 的 80GB-class equivalent 没有测量转换时不能等同于 A100 40GB 的固定时长；inference 能放下不证明全部校准能放下。

## 两条 idea 的区分

两者目前都采用单点干预分数→precision allocation。具体 estimand 和系统结构有所不同，但最终应说明为什么它们需要不同机制，而非仅将 ranking error 换成 action/pixel discrepancy。不能因为分别叫 WM 和 WAM 就视为两个充分不同的算法贡献。


exec
"C:\\Users\\Raymond\\.cache\\codex-runtimes\\codex-primary-runtime\\dependencies\\native\\powershell\\pwsh.exe" -Command '$p='"'D:\\Downloads\\Final Year Project\\ideaspark_run\\world-action-model-quantization\\phase2_coherence\\refined_candidate.json'; "'$c=Get-Content -Raw -LiteralPath $p | ConvertFrom-Json
Write-Output '"'--- hook ---'; "'$c.hook
Write-Output '"'--- core_mechanism ---'; "'$c.core_mechanism
Write-Output '"'--- core_mechanism_steps ---'; "'$c.core_mechanism_steps | ConvertTo-Json -Depth 20
Write-Output '"'--- core_mechanism_reasoning ---'; "'$c.core_mechanism_reasoning
Write-Output '"'--- differentiation_from_lit ---'; "'$c.differentiation_from_lit | ConvertTo-Json -Depth 20
Write-Output '"'--- almost_prior ---'; [pscustomobject]@{almost_prior_paper_id="'$c.almost_prior_paper_id; what_step_was_missed=$c.what_step_was_missed}
Write-Output '"'--- gap_closure ---'; "'$c.gap_closure | ConvertTo-Json -Depth 20
Write-Output '"'--- terms/composition ---'; [pscustomobject]@{signature_terms="'$c.signature_terms; alias_terms=$c.alias_terms; composition_note=$c.composition_note} | ConvertTo-Json -Depth 20' in D:\Downloads\Final Year Project
 succeeded in 176ms:
--- hook ---
At a matched high-precision-unit budget, the candidate predicts that the rank correlation between local PTQ discrepancy and paired one-step closed-loop consequence will drop specifically on contact-transition states, while consequence-ranked protection will reduce contact-sensitive task failures relative to local-score ranking. The surprising result is the regime split, not a claim that local numerical error is useless everywhere.
--- core_mechanism ---
The shared object is a per-site counterfactual consequence index C_r for a numerical quantization site r, where r is a tuple of WAM branch, module, inner denoising step, and weight-or-activation axis; the site registry is enumerated from the public coupled WAM's actual PTQ hooks, so no latent rollout/planning or action-tokenization site is introduced. For calibration episode j, the inputs are the recorded observation history h_j, language instruction l_j, simulator state z_j, denoising seed xi_j, and simulator seed eta_j from an existing RoboTwin 2.0 or LIBERO episode, while F_0 is the full-precision WAM and F_r is the same model with only site r using the matched low-bit numerical quantizer and every other target site left at FP16. Running F_0 and F_r with the same history, seed, cache, and coupled future-video/action inference produces full action chunks a_j0 and a_jr; the first executable action from each chunk is then applied from the identical simulator state z_j to obtain next observations o_(j+1)0 and o_(j+1)r. Define d_a(j,r) as the mean squared difference between the two full action chunks divided by max(sigma_a^2, epsilon_a^2), where sigma_a^2 is the mean coordinate variance of FP16 full action chunks over the calibration episodes and epsilon_a is a named normalized-scale floor; define d_o(j,r) as the mean per-pixel absolute next-observation difference divided by max(sigma_o, epsilon_o), where sigma_o is the calibration mean per-pixel standard deviation. With lambda_a=lambda_o=1 as the default dimensionless weights, selected before held-out evaluation and sensitivity-checked over a named ratio sweep, C_r is the episode mean of lambda_a d_a plus lambda_o d_o, and its top-B sites replace the local PTQ ranking under the same high-precision budget B as the matched baseline. The C00 reframe supplies a computable responsibility-like object and the operator substitution makes it actionable for numerical weight/activation precision selection, while the C02 diagnostic compares C_r with the same-context local discrepancy L_r and uses the simulator's native contact or task-transition flag to report the divergence without treating the evaluated WAM as its own oracle. This closes the additive gap because a single-site perturbation is followed through the coupled action output and an externally simulated next observation, and it closes the subtractive gap because the same paired intervention tests whether L_r is sufficient rather than assuming it. The candidate remains a coupled future-prediction/action-generation WAM method: the minimal Fast-WAM path uses the official released checkpoint in idm mode with sigma_shift_FW=1.0 and the current infer_action path, not first_frame, and numerical W/A quantization only; an optional LingBot-VA replication must use the README's VAE/text-encoder CPU offload and budget backward calibration separately from its reported inference memory. Fake-quant or dequantized GEMM is used only for profiling and must be reported separately from final measurements with the real low-bit kernel. For coherence purposes, C_r is explicitly a single-site FP16-background ranking proxy for one control transition—not the marginal consequence of the jointly low-bit schedule, not a jointly optimality guarantee, not task-success loss, and not a consequence of executing the full chunk or subsequent replanning; the primary allocation budget is B_site, the matched baseline's pre-registered site count, while persistent weight bytes, peak live activation workspace, scale/copy overhead, and latency are separate measured deployment quantities; the concrete method scope is numerical weight or activation intervention inside the coupled future-video/action denoiser with the one-transition estimand and normalized full-chunk/next-observation score, and the broad downstream-task-consequence quantization principle is treated as prior art.
--- core_mechanism_steps ---
"1. Freeze the public artifacts and inference contract. Require a pre-existing run-manifest entry checkpoint_FW containing the exact official Fast-WAM released checkpoint filename, repository revision, and immutable digest when available; if the entry is missing or ambiguous, halt before calibration. Set mode_FW=idm, sigma_shift_FW=1.0, and inference_path_FW=infer_action with the README's current no-default-VAE-decoding behavior; do not substitute first_frame because it removes the future branch. Bind b_lo to an exact per-axis numerical weight/activation bit tuple and real low-bit kernel identifier in the same manifest, and halt if the selected real kernel is unavailable; fake-quant/dequantized GEMM is not evidence of kernel support. Keep the action/video coupling unchanged and output a versioned configuration manifest with the checkpoint filename, repository revision, mode, sigma shift, inference path, bit tuple, and kernel identifiers. The minimal validation uses Fast-WAM only; if LingBot-VA is added, use its official VAE/text-encoder CPU-offload inference configuration and report backward-calibration memory as a separate measurement rather than reusing the reported inference-memory figure.\n2. Build an episode-level calibration manifest from existing RoboTwin 2.0 and LIBERO simulator episodes. Set N_cal=32 as the default to match the fixed-trajectory scale reported for QuantWAMs, with a pre-registered 16/32/64 episode sensitivity check; preserve observation histories, instructions, simulator states, action chunks, denoising seeds xi_j, simulator seeds eta_j, and native contact/task-transition flags. For each item, capture a complete paired-state snapshot S_j containing h_j, l_j, simulator state z_j, controller state, simulator RNG state eta_j, model/denoising RNG state xi_j, observation/history buffers, and persistent model-cache state. Output D_cal and a held-out episode/task split without mixing adjacent frames across splits. Reject an empty calibration or held-out split before scoring.\n3. Enumerate the intervention registry R from the public WAM implementation. Each r=(branch, module, denoising_step, axis) identifies one actual quantization hook in the coupled video/action denoiser, with axis in {weight, activation}; exclude VAE-only, text-only, latent/action tokenizer, and planner-only sites. Output a deterministic registry with a lexicographic tie-break order and a mapping from each site to its real kernel and exact bit setting. Reject an empty registry before ranking.\n4. Measure single-site model interventions with matched stochastic state. For every r and calibration item j, restore an independent clone of S_j before each branch, including controller state, simulator RNG, model RNG/generator, observation/history buffers, and persistent model cache; then run F_0 and F_r with the same h_j, l_j, xi_j, and inference path. Record the full action chunks a_j0 and a_jr and the hook outputs y_jr0 and y_jrr. Define the open-loop local surrogate L_jr as the mean squared difference over all elements of y_jr0 and y_jrr, and L_r as the episode mean of L_jr. Use fake-quant/dequantized GEMM only as a profiling implementation; record whether it was used and keep it separate from the real low-bit-kernel evaluation. Output paired model traces indexed by (j,r).\n5. Execute paired environment counterfactuals. Restore independent clones of S_j, including z_j, controller state, simulator RNG eta_j, observation/history buffers, model RNG/generator state, and persistent cache, for the two traces. Apply the first executable action after the restored controller's identical action transformation to each full chunk, and record o_(j+1)0, o_(j+1)r, and the simulator's independent contact/task-transition state. The downstream output of this step is the actual next observation and state transition caused by the altered first action, not merely a changed intermediate discrepancy; C_r therefore does not estimate effects of executing later chunk actions or subsequent replanning.\n6. Compute the consequence object. Define sigma_a^2 as the population variance over all scalar coordinates of all FP16 full action chunks in D_cal, and sigma_o as the mean of the population per-pixel standard deviations of FP16 next observations across D_cal. Use named floors epsilon_a=epsilon_o=1e-3 in normalized units, with a pre-registered 1e-4/1e-3/1e-2 sensitivity check. Compute d_a and d_o as the normalized full-action-chunk MSE and next-observation per-pixel L1 differences, set lambda_a=lambda_o=1 by default with a held-out sensitivity sweep over lambda_a/lambda_o in {0.5,1,2}, and output C_r as their episode mean with bootstrap intervals over complete episodes. C_r is a one-control-transition action/observation proxy, not task-success loss.\n7. Replace the local allocation operator without retraining. Define B_site, with B equivalent to B_site everywhere in this method, as the nonnegative integer high-precision-site count recorded by the matched local-fidelity baseline in the pre-registered run manifest; halt if that entry is missing or B_site>|R|. Rank sites by C_r and upgrade exactly the top-B_site sites while leaving all other sites at b_lo; resolve ties by the registry order. This primary comparison is a site-count budget only, not a claim of equal bytes or latency. For every schedule, record a separate deployment-cost ledger covering persistent weight bytes including copies and scales, peak live activation workspace under the real kernel, and measured latency; do not substitute that ledger for B_site or call the quantities equivalent.\n8. Run the confound-isolating diagnostic. On the same episodes, compare L_r and C_r using Spearman rank correlation separately on simulator-identified contact/task-transition and non-contact subsets, then compare top-B_site C_r allocation against top-B_site L_r allocation and a score-permuted negative control. If fewer than two sites exist, or either score vector is constant in a subset, record Spearman as undefined and skip that correlation rather than fabricate a value. The reported object is the divergence in downstream action/observation and task metrics, not a claim that a correlation itself is causal.\n9. Validate the selected schedule on held-out RoboTwin 2.0 and LIBERO episodes with the real low-bit kernel. Compare FP16, the matched local-fidelity baseline, the C_r allocation, and uniform precision at the same B_site site-count budget; measure task success, contact-sensitive failure rate, full action-chunk deviation, next-observation deviation, peak memory, and latency, with the same Fast-WAM configuration and multiple episode seeds.\n10. Keep CPU arithmetic checks and deployment claims separate. CPU runs may verify site indexing, deterministic paired seeds, scale normalization, and C_r arithmetic only; they do not establish robot success, GPU memory, or speedup. Report any LingBot-VA replication as a separate calibration/inference experiment with its offload and checkpoint configuration, and do not combine fake-quant traces with real-kernel performance."
--- core_mechanism_reasoning ---
BLOCK 1 — PREMISES ledger
P1 — Coupled closed-loop premise: each denoising call produces an action chunk and later calls consume observations affected by earlier actions; this is supported by the WAM formulation and the QuantWAMs pipeline record, but the size of a quantization-induced downstream effect is untested — falsification target.
P2 — Single-site intervention premise: the public WAM/PTQ implementation can execute one named weight or activation site at the matched low-bit setting while the remaining target sites stay at FP16 and the same stochastic state is restored; this is an implementation bet — untested — falsification target.
P3 — External transition premise: RoboTwin 2.0 and LIBERO provide existing simulator states, action execution, next observations, and native task/contact state needed to compare the two branches without inventing a new large dataset; this is believed true because both are the stated public robot-manipulation evaluation resources.
P4 — Consequence relevance premise: a perturbation that changes the full action chunk and the next simulator observation is more deployment-relevant than a one-call local discrepancy; this follows directly from the Phase 1 bottleneck, but the ranking advantage is untested — falsification target.
P5 — Contact-regime premise: local discrepancy and downstream consequence can rank sites differently around contact or task-state transitions; this is a domain bet motivated by contact-sensitive manipulation, and is untested — falsification target.
observation-model: calibration inputs are sampled as complete episodes from the available RoboTwin 2.0 and LIBERO splits, not as iid frames; histories are temporally dependent, task and embodiment proportions are inherited from the manifest, and simulator states are selected by the recorded trajectory, so C_r estimates the calibration episode distribution rather than an unbiased deployment distribution. Episode-level resampling and a held-out task/episode split are required.
ESTIMAND: for each site r, C_r estimates the mean dimensionless one-control-transition consequence of intervening on r over the calibration episode distribution D_cal, conditional on the same h_j, l_j, denoising seed xi_j, simulator state z_j, and simulator seed eta_j; it is not an unbiased estimate of global task-success loss or of an arbitrary real-robot deployment population.
lineage constraint: the load-bearing artifact is the paired action-to-simulator-observation counterfactual, not n1 layer-local PTQ, n2 static sensitivity allocation, n3 QuantWM latent rollout/planning, or n4 QuantWAMs joint empirical-Fisher scoring and fixed-count schedule repair.
BLOCK 2 — NAIVE-BASELINE AUDIT (branch (i))
The naive version is runnable-in-principle local PTQ: compute each site's FP16-versus-low-bit local output discrepancy on open-loop calibration inputs and upgrade the top-B sites. It does not already work because it silently assumes that local discrepancy is a sufficient proxy for downstream consequence, and that premise is false in this setting: a changed action chunk changes the next observation and therefore the inputs to later coupled predictions and contact dynamics. The mechanism confronts that failure head-on with paired simulator counterfactuals; ordinary local sensitivity or one-step PTQ does not resolve the WAM-specific combination of coupled video/action denoising, action-mediated state transition, and contact-critical feedback, so this is not a textbook tool application with the structure removed.
BLOCK 3 — design rationale
Both gaps share one intervention registry and one C_r object: the anchor uses it to allocate precision, while the sibling uses the same contexts to isolate whether L_r agrees with it, so no second architecture or dataset is needed. A replay-only local profile in the style of QuantWAMs was rejected because it never executes the changed action to obtain the next observation, and a full Jacobian-product or latent-rollout planner was rejected because it would add unobserved assumptions and move the branch into the excluded latent planning route. The chosen design therefore preserves the WAM's coupled future/action path, adds only a one-site causal diagnostic and matched allocation operator, and explicitly avoids the occupied ancestral nodes and the already-covered QuantWAMs mechanisms. The phrase more deployment-relevant is a conditional design hypothesis rather than an established equality to task loss; no joint-low-bit additivity or single-site-to-joint marginal assumption is made, and the broad downstream-task-consequence principle is prior art.
--- differentiation_from_lit ---
[
  {
    "paper_id": "arxiv:2607.28405v1",
    "delta": "QuantWAMs scores local block and denoising-step discrepancies on replayed full-precision states and repairs a fixed-count protection schedule, whereas this candidate constructs a paired single-site counterfactual that executes the altered action from the same simulator state and uses the resulting next observation as the precision-selection signal."
  },
  {
    "paper_id": "arxiv:2602.02110v1",
    "delta": "QuantWM studies weight/activation PTQ and long-horizon error in a DINO-WM latent planning setting, whereas this candidate measures the action-chunk and next-observation consequence of numerical perturbations inside a coupled video/action manipulation WAM."
  },
  {
    "paper_id": "semanticscholar:b5be12ccbb037be57a1dbc03fbd43d379170055f",
    "delta": "Fast-WAM isolates video co-training from explicit future imagination and does not provide a numerical PTQ consequence signal, whereas this candidate keeps the future/action-coupled idm feasibility path and adds single-site weight/activation intervention with consequence-based allocation."
  }
]
--- almost_prior ---

almost_prior_paper_id what_step_was_missed
--------------------- --------------------
arxiv:2607.28405v1    The closest paper would have needed to construct and execute a paired simulator counterfactual i…
--- gap_closure ---
[
  {
    "gap": "additive: No retrieved WAM-quantization leaf establishes the downstream consequence of a numerical perturbation at a module or denoising step for the final action chunk and the next closed-loop observation in contact-critical manipulation; this is the unresolved residue of QuantWAMs (arxiv:2607.28405v1), not a request for a particular remedy. — stakes: practitioners may deploy a memory- and latency-efficient WAM that preserves local calibration metrics but fails on contact-sensitive actions, while researchers may mistake local fidelity for transferable closed-loop reliability.",
    "main_pattern": "reframe_as_solvable_object",
    "sub_pattern": "C00 (Reframe as a Solvable Object)",
    "companion_pattern": "architectural_operator_substitution",
    "how_closed": "Closed by recasting each numerical intervention as a per-site counterfactual action-and-next-observation consequence functional, then using the companion at the main-pattern level to replace the layer-local fidelity score with a runnable precision-scoring and allocation operator."
  },
  {
    "gap": "subtractive: No retrieved WAM-quantization leaf tests whether the local numerical-fidelity surrogate inherited from layer-local PTQ is actually sufficient for precision selection in the coupled video/action setting, rather than an assumed proxy for downstream task impact; this questions the inherited assumption and leaves the replacement open. — stakes: precision-allocation conclusions can rest on an unexamined surrogate and invalidate comparisons across WAMs or tasks, while deployment teams cannot tell whether an apparent speed or memory gain preserves the action behavior they require.",
    "main_pattern": "controlled_diagnostic_design",
    "sub_pattern": "C02 (Design a Confound-Isolating Diagnostic)",
    "companion_pattern": null,
    "how_closed": "Closed by evaluating local fidelity and the counterfactual consequence on the same site, history, denoising seed, simulator state, and precision budget, then reporting their divergence separately for contact and non-contact transitions."
  }
]
--- terms/composition ---
{
  "signature_terms": [
    "single-site closed-loop consequence score",
    "action and next-observation counterfactuals",
    "contact-critical mixed-precision selection",
    "coupled video-action denoising"
  ],
  "alias_terms": [
    "task-loss-aware quantization calibration",
    "model-error-aware robust MPC",
    "teacher-student behavior matching",
    "off-policy marginal effect estimation",
    "interventional policy sensitivity analysis"
  ],
  "composition_note": ""
}


codex
审计明确给出 3 个 `tactical` targets，顺序为：`differentiation_from_lit`、`core_mechanism_steps`、`hook`。我将各生成一个 patch entry（总数严格为 3）：第一项只扩展 claim-bounded prior-art comparison；第二项补上 branch snapshot/clone/restore/cache invalidation/equality-check 的执行顺序；第三项只强化 `C_r` 的 proxy 与 held-out predictive-test 边界。不会触碰 `falsification_prediction` 或 `compute_budget`。
exec
"C:\\Users\\Raymond\\.cache\\codex-runtimes\\codex-primary-runtime\\dependencies\\native\\powershell\\pwsh.exe" -Command '$out='"'D:\\Downloads\\Final Year Project\\ideaspark_run\\world-action-model-quantization\\phase3_revise\\phase3_revise_output.json'; if (Test-Path -LiteralPath "'$out) { Get-Item -LiteralPath $out | Select-Object FullName,Length,LastWriteTime; Get-Content -Raw -LiteralPath $out } else { '"'MISSING' }" in D:\Downloads\Final Year Project
 succeeded in 103ms:
MISSING

apply patch
patch: completed
D:\Downloads\Final Year Project\ideaspark_run\world-action-model-quantization\phase3_revise\phase3_revise_output.json
diff --git a/ideaspark_run/world-action-model-quantization/phase3_revise/phase3_revise_output.json b/ideaspark_run/world-action-model-quantization/phase3_revise/phase3_revise_output.json
new file mode 100644
index 0000000000000000000000000000000000000000..db526281f10aff3a81d2a41ef66744fb9542355e
--- /dev/null
+++ b/ideaspark_run/world-action-model-quantization/phase3_revise/phase3_revise_output.json
@@ -0,0 +1,58 @@
+{
+  "candidate_id": null,
+  "applied_revisions": [
+    {
+      "scope": "tactical",
+      "op": "replace",
+      "field": "differentiation_from_lit",
+      "value": [
+        {
+          "paper_id": "arxiv:2607.28405v1",
+          "delta": "QuantWAMs already covers joint video/action empirical-Fisher cross terms over block and denoising-step granularity and fixed-count closed-loop protection-schedule repair, so joint coupling, denoising-step sensitivity, closed-loop protection, and contact-critical allocation are prior art; this candidate instead intervenes on one numerical weight-or-activation site r with every other target site at FP16, estimates C_r from paired full action-chunk discrepancy plus the next observation after only the first action is executed, and ranks sites under B_site, without claiming a jointly-low-bit marginal, joint optimality, task-success-loss identity, full-horizon/replan consequence, bytes objective, or latency objective."
+        },
+        {
+          "paper_id": "arxiv:2602.02110v1",
+          "delta": "QuantWM already covers weight/activation PTQ sensitivity and long-horizon error in a DINO-WM latent planning setting, so latent rollout/planning mismatch and that sensitivity branch are not claimed as novel here; this candidate is a manipulation WAM coupled future-video/action denoiser with a single-site FP16-background numerical intervention, a one-control-transition C_r estimand, and paired full-chunk/next-observation discrepancies rather than a latent rollout/planning marginal or a bytes/latency objective."
+        },
+        {
+          "paper_id": "VAML (AISTATS 2017)",
+          "delta": "VAML is part of the earlier task-consequence/value-aware lineage supplied for this run, so defining a quantization target by downstream consequence is prior art and not this candidate's novelty; the bounded difference here is a single numerical W/A site intervention in a coupled future-video/action WAM, with C_r estimating a one-control-transition paired action/observation proxy and the numerical operator combining normalized full action-chunk MSE with next-observation pixel L1, not a claim of identical objective, task-loss identity, or full-horizon marginal."
+        },
+        {
+          "paper_id": "arxiv:2106.14080",
+          "delta": "Model Advantage / Value-Aware Model Learning is included in the earlier task-consequence lineage, so broad downstream-consequence targeting is prior art; this candidate makes no equivalence claim about objectives or estimands and retains only the narrower concrete intervention of one-site W/A quantization with an FP16 background, the one-control-transition C_r proxy, and top-B_site allocation in a coupled WAM, without joint-low-bit optimality or task-loss claims."
+        },
+        {
+          "paper_id": "arxiv:1908.06845",
+          "delta": "Deep Task-Based Quantization establishes the prior-art principle that downstream task consequence can guide quantization, which is not claimed as novel here; the candidate's distinct bounded object is a single-site W/A PTQ intervention with all other target sites at FP16, C_r over one simulator control transition, and a normalized full-action-chunk plus next-observation numerical score, while held-out task-failure reduction is only a predictive validation of transfer and not C_r itself."
+        },
+        {
+          "paper_id": "arxiv:2505.15304",
+          "delta": "SQIL already treats mission-critical-state protection as prior art through saliency-weighted QAT and action distillation, so protecting contact or other critical states alone is not novel; this candidate uses a different bounded intervention and estimand—single-site numerical W/A PTQ in a coupled future-video/action WAM, paired simulator branches with only the first action executed, and C_r from full-chunk/next-observation discrepancy—and does not claim that SQIL is identical or that critical-state protection alone is its contribution."
+        },
+        {
+          "paper_id": "semanticscholar:b5be12ccbb037be57a1dbc03fbd43d379170055f",
+          "delta": "Fast-WAM isolates video co-training from explicit future imagination and does not provide a numerical PTQ consequence signal, whereas this candidate keeps the future/action-coupled idm feasibility path and adds single-site weight/activation intervention with consequence-based allocation."
+        }
+      ],
+      "outcome": "applied",
+      "delta_summary": "Expanded the literature differentiation to all six required prior-art comparisons while preserving novelty only for the one-site FP16-background WAM numerical operator and one-control-transition estimand."
+    },
+    {
+      "scope": "tactical",
+      "op": "append_sentence",
+      "field": "core_mechanism_steps",
+      "value": "Executable branch protocol: the exact Optional IDM checkpoint identity (the checkpoint_FW filename, repository revision, and immutable digest when available) must be present and unambiguous in the pre-run manifest; otherwise halt before calibration. Before either branch, serialize one immutable versioned snapshot S_j in the fixed order controller state, simulator RNG state eta_j, model/generator RNG state xi_j, observation/history buffers, persistent model-cache state, simulator state z_j, and action-transformation state; deep-clone S_j into separate F_0 and F_r copies, restore the complete copy immediately before its branch, and rebuild or invalidate every cache entry whose key or dependency is touched by r before executing that branch. Canonical-serialize and equality-check h_j, l_j, z_j, controller/RNG/history/action-transformation state, and the cache-validity manifest against S_j after each restore; allow only the explicitly recorded r-touched entries to differ after branch-local invalidation/rebuild, abort on any other mismatch, and never share mutable state or cache entries. Equality-check the action-transformation inputs and the first-action mapping before applying each branch's first executable action to its restored simulator, while retaining d_a over the full action chunks, so the first-action environment estimand and full-chunk d_a definition remain unchanged."
+      ,"outcome": "applied",
+      "delta_summary": "Added an executable immutable-snapshot, branch-clone, cache-validity, state-equality, and first-action restoration protocol without changing the one-transition estimand."
+    },
+    {
+      "scope": "tactical",
+      "op": "append_sentence",
+      "field": "hook",
+      "value": "C_r is not task loss, the physical consequence of executing the full action chunk or a replan horizon, a marginal effect in the jointly low-bit deployment, or a bytes/latency objective; any reduction in held-out task failures is only an empirical predictive test of transfer from this one-control-transition proxy."
+      ,"outcome": "applied",
+      "delta_summary": "Made the proxy boundary explicit and labeled held-out task-failure reduction as an empirical predictive test rather than an identity claim."
+    }
+  ]
+}

diff --git a/ideaspark_run/world-action-model-quantization/phase3_revise/phase3_revise_output.json b/ideaspark_run/world-action-model-quantization/phase3_revise/phase3_revise_output.json
new file mode 100644
index 0000000000000000000000000000000000000000..db526281f10aff3a81d2a41ef66744fb9542355e
--- /dev/null
+++ b/ideaspark_run/world-action-model-quantization/phase3_revise/phase3_revise_output.json
@@ -0,0 +1,58 @@
+{
+  "candidate_id": null,
+  "applied_revisions": [
+    {
+      "scope": "tactical",
+      "op": "replace",
+      "field": "differentiation_from_lit",
+      "value": [
+        {
+          "paper_id": "arxiv:2607.28405v1",
+          "delta": "QuantWAMs already covers joint video/action empirical-Fisher cross terms over block and denoising-step granularity and fixed-count closed-loop protection-schedule repair, so joint coupling, denoising-step sensitivity, closed-loop protection, and contact-critical allocation are prior art; this candidate instead intervenes on one numerical weight-or-activation site r with every other target site at FP16, estimates C_r from paired full action-chunk discrepancy plus the next observation after only the first action is executed, and ranks sites under B_site, without claiming a jointly-low-bit marginal, joint optimality, task-success-loss identity, full-horizon/replan consequence, bytes objective, or latency objective."
+        },
+        {
+          "paper_id": "arxiv:2602.02110v1",
+          "delta": "QuantWM already covers weight/activation PTQ sensitivity and long-horizon error in a DINO-WM latent planning setting, so latent rollout/planning mismatch and that sensitivity branch are not claimed as novel here; this candidate is a manipulation WAM coupled future-video/action denoiser with a single-site FP16-background numerical intervention, a one-control-transition C_r estimand, and paired full-chunk/next-observation discrepancies rather than a latent rollout/planning marginal or a bytes/latency objective."
+        },
+        {
+          "paper_id": "VAML (AISTATS 2017)",
+          "delta": "VAML is part of the earlier task-consequence/value-aware lineage supplied for this run, so defining a quantization target by downstream consequence is prior art and not this candidate's novelty; the bounded difference here is a single numerical W/A site intervention in a coupled future-video/action WAM, with C_r estimating a one-control-transition paired action/observation proxy and the numerical operator combining normalized full action-chunk MSE with next-observation pixel L1, not a claim of identical objective, task-loss identity, or full-horizon marginal."
+        },
+        {
+          "paper_id": "arxiv:2106.14080",
+          "delta": "Model Advantage / Value-Aware Model Learning is included in the earlier task-consequence lineage, so broad downstream-consequence targeting is prior art; this candidate makes no equivalence claim about objectives or estimands and retains only the narrower concrete intervention of one-site W/A quantization with an FP16 background, the one-control-transition C_r proxy, and top-B_site allocation in a coupled WAM, without joint-low-bit optimality or task-loss claims."
+        },
+        {
+          "paper_id": "arxiv:1908.06845",
+          "delta": "Deep Task-Based Quantization establishes the prior-art principle that downstream task consequence can guide quantization, which is not claimed as novel here; the candidate's distinct bounded object is a single-site W/A PTQ intervention with all other target sites at FP16, C_r over one simulator control transition, and a normalized full-action-chunk plus next-observation numerical score, while held-out task-failure reduction is only a predictive validation of transfer and not C_r itself."
+        },
+        {
+          "paper_id": "arxiv:2505.15304",
+          "delta": "SQIL already treats mission-critical-state protection as prior art through saliency-weighted QAT and action distillation, so protecting contact or other critical states alone is not novel; this candidate uses a different bounded intervention and estimand—single-site numerical W/A PTQ in a coupled future-video/action WAM, paired simulator branches with only the first action executed, and C_r from full-chunk/next-observation discrepancy—and does not claim that SQIL is identical or that critical-state protection alone is its contribution."
+        },
+        {
+          "paper_id": "semanticscholar:b5be12ccbb037be57a1dbc03fbd43d379170055f",
+          "delta": "Fast-WAM isolates video co-training from explicit future imagination and does not provide a numerical PTQ consequence signal, whereas this candidate keeps the future/action-coupled idm feasibility path and adds single-site weight/activation intervention with consequence-based allocation."
+        }
+      ],
+      "outcome": "applied",
+      "delta_summary": "Expanded the literature differentiation to all six required prior-art comparisons while preserving novelty only for the one-site FP16-background WAM numerical operator and one-control-transition estimand."
+    },
+    {
+      "scope": "tactical",
+      "op": "append_sentence",
+      "field": "core_mechanism_steps",
+      "value": "Executable branch protocol: the exact Optional IDM checkpoint identity (the checkpoint_FW filename, repository revision, and immutable digest when available) must be present and unambiguous in the pre-run manifest; otherwise halt before calibration. Before either branch, serialize one immutable versioned snapshot S_j in the fixed order controller state, simulator RNG state eta_j, model/generator RNG state xi_j, observation/history buffers, persistent model-cache state, simulator state z_j, and action-transformation state; deep-clone S_j into separate F_0 and F_r copies, restore the complete copy immediately before its branch, and rebuild or invalidate every cache entry whose key or dependency is touched by r before executing that branch. Canonical-serialize and equality-check h_j, l_j, z_j, controller/RNG/history/action-transformation state, and the cache-validity manifest against S_j after each restore; allow only the explicitly recorded r-touched entries to differ after branch-local invalidation/rebuild, abort on any other mismatch, and never share mutable state or cache entries. Equality-check the action-transformation inputs and the first-action mapping before applying each branch's first executable action to its restored simulator, while retaining d_a over the full action chunks, so the first-action environment estimand and full-chunk d_a definition remain unchanged."
+      ,"outcome": "applied",
+      "delta_summary": "Added an executable immutable-snapshot, branch-clone, cache-validity, state-equality, and first-action restoration protocol without changing the one-transition estimand."
+    },
+    {
+      "scope": "tactical",
+      "op": "append_sentence",
+      "field": "hook",
+      "value": "C_r is not task loss, the physical consequence of executing the full action chunk or a replan horizon, a marginal effect in the jointly low-bit deployment, or a bytes/latency objective; any reduction in held-out task failures is only an empirical predictive test of transfer from this one-control-transition proxy."
+      ,"outcome": "applied",
+      "delta_summary": "Made the proxy boundary explicit and labeled held-out task-failure reduction as an empirical predictive test rather than an identity claim."
+    }
+  ]
+}

codex
patch 已写入指定位置，且只包含 3 个 `applied_revisions[]` 条目。接下来做只读 QA：验证 JSON 可解析、条目数/字段与 audit targets 一一对应、没有 `final_candidate` 或 guarded-field generic rewrite，并确认六个 prior-art 名称、Optional IDM manifest/halt 语义及 proxy 边界均已落在 patch 中。不会运行 merger，以保持该输出严格为 patch-only。
exec
"C:\\Users\\Raymond\\.cache\\codex-runtimes\\codex-primary-runtime\\dependencies\\native\\powershell\\pwsh.exe" -Command '$out='"'D:\\Downloads\\Final Year Project\\ideaspark_run\\world-action-model-quantization\\phase3_revise\\phase3_revise_output.json'; "'$raw=Get-Content -Raw -LiteralPath $out; $p=$raw | ConvertFrom-Json
$targetFields=@('"'differentiation_from_lit','core_mechanism_steps','hook')"'$checks=[ordered]@{}
$checks.json_parse=$true
$checks.candidate_id_is_null=($null -eq $p.candidate_id)
$checks.revision_count=($p.applied_revisions.Count)
$checks.target_count_matches=($p.applied_revisions.Count -eq $targetFields.Count)
$checks.target_order_matches=(($(for($i=0;$i -lt $targetFields.Count;$i++){ $p.applied_revisions[$i].field -eq $targetFields[$i] }) | Where-Object {$_ -eq $false}).Count -eq 0)
$checks.no_final_candidate=($null -eq $p.final_candidate)
$checks.no_guarded_generic_ops=(@($p.applied_revisions | Where-Object { $_.field -match '"'"'^(falsification_prediction|compute_budget)('"\\.|"'$)'"' -and "'$_.op -ne '"'rewrite_falsification' }).Count -eq 0)
"'$diff=($p.applied_revisions[0].value | ForEach-Object { $_.delta }) -join "`n"
$checks.six_prior_names=(@('"'QuantWAMs','QuantWM','VAML','Model Advantage / Value-Aware Model Learning','Deep Task-Based Quantization','SQIL') | ForEach-Object { "'$diff.Contains($_) } | Where-Object {$_ -eq $false}).Count -eq 0
$steps=$p.applied_revisions[1].value
$checks.executable_state_semantics=(@('"'serialize one immutable versioned snapshot','deep-clone','restore the complete copy','rebuild or invalidate','equality-check','first executable action') | ForEach-Object { "'$steps.Contains($_) } | Where-Object {$_ -eq $false}).Count -eq 0
$hook=$p.applied_revisions[2].value
$checks.proxy_boundary=(@('"'not task loss','jointly low-bit deployment','bytes/latency objective','empirical predictive test') | ForEach-Object { "'$hook.Contains($_) } | Where-Object {$_ -eq $false}).Count -eq 0
$checks.optional_idm_manifest_halt=$steps.Contains('"'exact Optional IDM checkpoint identity') -and "'$steps.Contains('"'otherwise halt before calibration')
"'$checks.outcome_all_applied=(@($p.applied_revisions | Where-Object {$_.outcome -ne '"'applied'}).Count -eq 0)
"'$checks | ConvertTo-Json
Write-Output '"'--- compact patch inventory ---'
"'$p.applied_revisions | Select-Object scope,op,field,outcome,delta_summary | ConvertTo-Json -Depth 5
Write-Output '"'--- byte/file info ---'
Get-Item -LiteralPath "'$out | Select-Object FullName,Length,LastWriteTime' in D:\Downloads\Final Year Project
 exited 1 in 108ms:
ParserError: 
Line |
   3 |  �� fferentiation_from_lit','core_mechanism_steps','hook')$checks=[ordere ��
     |                                                          ~~~~~~~
     | Unexpected token '$checks' in expression or statement.

diff --git a/ideaspark_run/world-action-model-quantization/phase3_revise/phase3_revise_output.json b/ideaspark_run/world-action-model-quantization/phase3_revise/phase3_revise_output.json
new file mode 100644
index 0000000000000000000000000000000000000000..db526281f10aff3a81d2a41ef66744fb9542355e
--- /dev/null
+++ b/ideaspark_run/world-action-model-quantization/phase3_revise/phase3_revise_output.json
@@ -0,0 +1,58 @@
+{
+  "candidate_id": null,
+  "applied_revisions": [
+    {
+      "scope": "tactical",
+      "op": "replace",
+      "field": "differentiation_from_lit",
+      "value": [
+        {
+          "paper_id": "arxiv:2607.28405v1",
+          "delta": "QuantWAMs already covers joint video/action empirical-Fisher cross terms over block and denoising-step granularity and fixed-count closed-loop protection-schedule repair, so joint coupling, denoising-step sensitivity, closed-loop protection, and contact-critical allocation are prior art; this candidate instead intervenes on one numerical weight-or-activation site r with every other target site at FP16, estimates C_r from paired full action-chunk discrepancy plus the next observation after only the first action is executed, and ranks sites under B_site, without claiming a jointly-low-bit marginal, joint optimality, task-success-loss identity, full-horizon/replan consequence, bytes objective, or latency objective."
+        },
+        {
+          "paper_id": "arxiv:2602.02110v1",
+          "delta": "QuantWM already covers weight/activation PTQ sensitivity and long-horizon error in a DINO-WM latent planning setting, so latent rollout/planning mismatch and that sensitivity branch are not claimed as novel here; this candidate is a manipulation WAM coupled future-video/action denoiser with a single-site FP16-background numerical intervention, a one-control-transition C_r estimand, and paired full-chunk/next-observation discrepancies rather than a latent rollout/planning marginal or a bytes/latency objective."
+        },
+        {
+          "paper_id": "VAML (AISTATS 2017)",
+          "delta": "VAML is part of the earlier task-consequence/value-aware lineage supplied for this run, so defining a quantization target by downstream consequence is prior art and not this candidate's novelty; the bounded difference here is a single numerical W/A site intervention in a coupled future-video/action WAM, with C_r estimating a one-control-transition paired action/observation proxy and the numerical operator combining normalized full action-chunk MSE with next-observation pixel L1, not a claim of identical objective, task-loss identity, or full-horizon marginal."
+        },
+        {
+          "paper_id": "arxiv:2106.14080",
+          "delta": "Model Advantage / Value-Aware Model Learning is included in the earlier task-consequence lineage, so broad downstream-consequence targeting is prior art; this candidate makes no equivalence claim about objectives or estimands and retains only the narrower concrete intervention of one-site W/A quantization with an FP16 background, the one-control-transition C_r proxy, and top-B_site allocation in a coupled WAM, without joint-low-bit optimality or task-loss claims."
+        },
+        {
+          "paper_id": "arxiv:1908.06845",
+          "delta": "Deep Task-Based Quantization establishes the prior-art principle that downstream task consequence can guide quantization, which is not claimed as novel here; the candidate's distinct bounded object is a single-site W/A PTQ intervention with all other target sites at FP16, C_r over one simulator control transition, and a normalized full-action-chunk plus next-observation numerical score, while held-out task-failure reduction is only a predictive validation of transfer and not C_r itself."
+        },
+        {
+          "paper_id": "arxiv:2505.15304",
+          "delta": "SQIL already treats mission-critical-state protection as prior art through saliency-weighted QAT and action distillation, so protecting contact or other critical states alone is not novel; this candidate uses a different bounded intervention and estimand—single-site numerical W/A PTQ in a coupled future-video/action WAM, paired simulator branches with only the first action executed, and C_r from full-chunk/next-observation discrepancy—and does not claim that SQIL is identical or that critical-state protection alone is its contribution."
+        },
+        {
+          "paper_id": "semanticscholar:b5be12ccbb037be57a1dbc03fbd43d379170055f",
+          "delta": "Fast-WAM isolates video co-training from explicit future imagination and does not provide a numerical PTQ consequence signal, whereas this candidate keeps the future/action-coupled idm feasibility path and adds single-site weight/activation intervention with consequence-based allocation."
+        }
+      ],
+      "outcome": "applied",
+      "delta_summary": "Expanded the literature differentiation to all six required prior-art comparisons while preserving novelty only for the one-site FP16-background WAM numerical operator and one-control-transition estimand."
+    },
+    {
+      "scope": "tactical",
+      "op": "append_sentence",
+      "field": "core_mechanism_steps",
+      "value": "Executable branch protocol: the exact Optional IDM checkpoint identity (the checkpoint_FW filename, repository revision, and immutable digest when available) must be present and unambiguous in the pre-run manifest; otherwise halt before calibration. Before either branch, serialize one immutable versioned snapshot S_j in the fixed order controller state, simulator RNG state eta_j, model/generator RNG state xi_j, observation/history buffers, persistent model-cache state, simulator state z_j, and action-transformation state; deep-clone S_j into separate F_0 and F_r copies, restore the complete copy immediately before its branch, and rebuild or invalidate every cache entry whose key or dependency is touched by r before executing that branch. Canonical-serialize and equality-check h_j, l_j, z_j, controller/RNG/history/action-transformation state, and the cache-validity manifest against S_j after each restore; allow only the explicitly recorded r-touched entries to differ after branch-local invalidation/rebuild, abort on any other mismatch, and never share mutable state or cache entries. Equality-check the action-transformation inputs and the first-action mapping before applying each branch's first executable action to its restored simulator, while retaining d_a over the full action chunks, so the first-action environment estimand and full-chunk d_a definition remain unchanged."
+      ,"outcome": "applied",
+      "delta_summary": "Added an executable immutable-snapshot, branch-clone, cache-validity, state-equality, and first-action restoration protocol without changing the one-transition estimand."
+    },
+    {
+      "scope": "tactical",
+      "op": "append_sentence",
+      "field": "hook",
+      "value": "C_r is not task loss, the physical consequence of executing the full action chunk or a replan horizon, a marginal effect in the jointly low-bit deployment, or a bytes/latency objective; any reduction in held-out task failures is only an empirical predictive test of transfer from this one-control-transition proxy."
+      ,"outcome": "applied",
+      "delta_summary": "Made the proxy boundary explicit and labeled held-out task-failure reduction as an empirical predictive test rather than an identity claim."
+    }
+  ]
+}

exec
"C:\\Users\\Raymond\\.cache\\codex-runtimes\\codex-primary-runtime\\dependencies\\native\\powershell\\pwsh.exe" -Command '$out='"'D:\\Downloads\\Final Year Project\\ideaspark_run\\world-action-model-quantization\\phase3_revise\\phase3_revise_output.json'; "'$raw=Get-Content -Raw -LiteralPath $out; $p=$raw | ConvertFrom-Json
$targetFields=@('"'differentiation_from_lit','core_mechanism_steps','hook')
"'$checks=[ordered]@{}
$checks.json_parse=$true
$checks.candidate_id_is_null=($null -eq $p.candidate_id)
$checks.revision_count=$p.applied_revisions.Count
$checks.target_count_matches=($p.applied_revisions.Count -eq $targetFields.Count)
$checks.target_order_matches=(($(for($i=0;$i -lt $targetFields.Count;$i++){ $p.applied_revisions[$i].field -eq $targetFields[$i] }) | Where-Object {$_ -eq $false}).Count -eq 0)
$checks.no_final_candidate=($null -eq $p.final_candidate)
$checks.no_guarded_generic_ops=(@($p.applied_revisions | Where-Object { $_.field -match '"'"'^(falsification_prediction|compute_budget)('"\\.|"'$)'"' -and "'$_.op -ne '"'rewrite_falsification' }).Count -eq 0)
"'$diff=($p.applied_revisions[0].value | ForEach-Object { $_.delta }) -join "`n"
$checks.six_prior_names=(@('"'QuantWAMs','QuantWM','VAML','Model Advantage / Value-Aware Model Learning','Deep Task-Based Quantization','SQIL') | ForEach-Object { "'$diff.Contains($_) } | Where-Object {$_ -eq $false}).Count -eq 0
$steps=$p.applied_revisions[1].value
$checks.executable_state_semantics=(@('"'serialize one immutable versioned snapshot','deep-clone','restore the complete copy','rebuild or invalidate','equality-check','first executable action') | ForEach-Object { "'$steps.Contains($_) } | Where-Object {$_ -eq $false}).Count -eq 0
$hook=$p.applied_revisions[2].value
$checks.proxy_boundary=(@('"'not task loss','jointly low-bit deployment','bytes/latency objective','empirical predictive test') | ForEach-Object { "'$hook.Contains($_) } | Where-Object {$_ -eq $false}).Count -eq 0
$checks.optional_idm_manifest_halt=$steps.Contains('"'exact Optional IDM checkpoint identity') -and "'$steps.Contains('"'otherwise halt before calibration')
"'$checks.outcome_all_applied=(@($p.applied_revisions | Where-Object {$_.outcome -ne '"'applied'}).Count -eq 0)
"'$checks | ConvertTo-Json
Write-Output '"'--- compact patch inventory ---'
"'$p.applied_revisions | Select-Object scope,op,field,outcome,delta_summary | ConvertTo-Json -Depth 5
Write-Output '"'--- byte/file info ---'
Get-Item -LiteralPath "'$out | Select-Object FullName,Length,LastWriteTime' in D:\Downloads\Final Year Project
 succeeded in 196ms:
{
  "json_parse": true,
  "candidate_id_is_null": true,
  "revision_count": 3,
  "target_count_matches": true,
  "target_order_matches": true,
  "no_final_candidate": true,
  "no_guarded_generic_ops": true,
  "six_prior_names": true,
  "executable_state_semantics": true,
  "proxy_boundary": true,
  "optional_idm_manifest_halt": true,
  "outcome_all_applied": true
}
--- compact patch inventory ---
[
  {
    "scope": "tactical",
    "op": "replace",
    "field": "differentiation_from_lit",
    "outcome": "applied",
    "delta_summary": "Expanded the literature differentiation to all six required prior-art comparisons while preserving novelty only for the one-site FP16-background WAM numerical operator and one-control-transition estimand."
  },
  {
    "scope": "tactical",
    "op": "append_sentence",
    "field": "core_mechanism_steps",
    "outcome": "applied",
    "delta_summary": "Added an executable immutable-snapshot, branch-clone, cache-validity, state-equality, and first-action restoration protocol without changing the one-transition estimand."
  },
  {
    "scope": "tactical",
    "op": "append_sentence",
    "field": "hook",
    "outcome": "applied",
    "delta_summary": "Made the proxy boundary explicit and labeled held-out task-failure reduction as an empirical predictive test rather than an identity claim."
  }
]
--- byte/file info ---

FullName                                                                                                              L
                                                                                                                      e
                                                                                                                      n
                                                                                                                      g
                                                                                                                      t
                                                                                                                      h
--------                                                                                                              -
D:\Downloads\Final Year Project\ideaspark_run\world-action-model-quantization\phase3_revise\phase3_revise_output.json 4


diff --git a/ideaspark_run/world-action-model-quantization/phase3_revise/phase3_revise_output.json b/ideaspark_run/world-action-model-quantization/phase3_revise/phase3_revise_output.json
new file mode 100644
index 0000000000000000000000000000000000000000..db526281f10aff3a81d2a41ef66744fb9542355e
--- /dev/null
+++ b/ideaspark_run/world-action-model-quantization/phase3_revise/phase3_revise_output.json
@@ -0,0 +1,58 @@
+{
+  "candidate_id": null,
+  "applied_revisions": [
+    {
+      "scope": "tactical",
+      "op": "replace",
+      "field": "differentiation_from_lit",
+      "value": [
+        {
+          "paper_id": "arxiv:2607.28405v1",
+          "delta": "QuantWAMs already covers joint video/action empirical-Fisher cross terms over block and denoising-step granularity and fixed-count closed-loop protection-schedule repair, so joint coupling, denoising-step sensitivity, closed-loop protection, and contact-critical allocation are prior art; this candidate instead intervenes on one numerical weight-or-activation site r with every other target site at FP16, estimates C_r from paired full action-chunk discrepancy plus the next observation after only the first action is executed, and ranks sites under B_site, without claiming a jointly-low-bit marginal, joint optimality, task-success-loss identity, full-horizon/replan consequence, bytes objective, or latency objective."
+        },
+        {
+          "paper_id": "arxiv:2602.02110v1",
+          "delta": "QuantWM already covers weight/activation PTQ sensitivity and long-horizon error in a DINO-WM latent planning setting, so latent rollout/planning mismatch and that sensitivity branch are not claimed as novel here; this candidate is a manipulation WAM coupled future-video/action denoiser with a single-site FP16-background numerical intervention, a one-control-transition C_r estimand, and paired full-chunk/next-observation discrepancies rather than a latent rollout/planning marginal or a bytes/latency objective."
+        },
+        {
+          "paper_id": "VAML (AISTATS 2017)",
+          "delta": "VAML is part of the earlier task-consequence/value-aware lineage supplied for this run, so defining a quantization target by downstream consequence is prior art and not this candidate's novelty; the bounded difference here is a single numerical W/A site intervention in a coupled future-video/action WAM, with C_r estimating a one-control-transition paired action/observation proxy and the numerical operator combining normalized full action-chunk MSE with next-observation pixel L1, not a claim of identical objective, task-loss identity, or full-horizon marginal."
+        },
+        {
+          "paper_id": "arxiv:2106.14080",
+          "delta": "Model Advantage / Value-Aware Model Learning is included in the earlier task-consequence lineage, so broad downstream-consequence targeting is prior art; this candidate makes no equivalence claim about objectives or estimands and retains only the narrower concrete intervention of one-site W/A quantization with an FP16 background, the one-control-transition C_r proxy, and top-B_site allocation in a coupled WAM, without joint-low-bit optimality or task-loss claims."
+        },
+        {
+          "paper_id": "arxiv:1908.06845",
+          "delta": "Deep Task-Based Quantization establishes the prior-art principle that downstream task consequence can guide quantization, which is not claimed as novel here; the candidate's distinct bounded object is a single-site W/A PTQ intervention with all other target sites at FP16, C_r over one simulator control transition, and a normalized full-action-chunk plus next-observation numerical score, while held-out task-failure reduction is only a predictive validation of transfer and not C_r itself."
+        },
+        {
+          "paper_id": "arxiv:2505.15304",
+          "delta": "SQIL already treats mission-critical-state protection as prior art through saliency-weighted QAT and action distillation, so protecting contact or other critical states alone is not novel; this candidate uses a different bounded intervention and estimand—single-site numerical W/A PTQ in a coupled future-video/action WAM, paired simulator branches with only the first action executed, and C_r from full-chunk/next-observation discrepancy—and does not claim that SQIL is identical or that critical-state protection alone is its contribution."
+        },
+        {
+          "paper_id": "semanticscholar:b5be12ccbb037be57a1dbc03fbd43d379170055f",
+          "delta": "Fast-WAM isolates video co-training from explicit future imagination and does not provide a numerical PTQ consequence signal, whereas this candidate keeps the future/action-coupled idm feasibility path and adds single-site weight/activation intervention with consequence-based allocation."
+        }
+      ],
+      "outcome": "applied",
+      "delta_summary": "Expanded the literature differentiation to all six required prior-art comparisons while preserving novelty only for the one-site FP16-background WAM numerical operator and one-control-transition estimand."
+    },
+    {
+      "scope": "tactical",
+      "op": "append_sentence",
+      "field": "core_mechanism_steps",
+      "value": "Executable branch protocol: the exact Optional IDM checkpoint identity (the checkpoint_FW filename, repository revision, and immutable digest when available) must be present and unambiguous in the pre-run manifest; otherwise halt before calibration. Before either branch, serialize one immutable versioned snapshot S_j in the fixed order controller state, simulator RNG state eta_j, model/generator RNG state xi_j, observation/history buffers, persistent model-cache state, simulator state z_j, and action-transformation state; deep-clone S_j into separate F_0 and F_r copies, restore the complete copy immediately before its branch, and rebuild or invalidate every cache entry whose key or dependency is touched by r before executing that branch. Canonical-serialize and equality-check h_j, l_j, z_j, controller/RNG/history/action-transformation state, and the cache-validity manifest against S_j after each restore; allow only the explicitly recorded r-touched entries to differ after branch-local invalidation/rebuild, abort on any other mismatch, and never share mutable state or cache entries. Equality-check the action-transformation inputs and the first-action mapping before applying each branch's first executable action to its restored simulator, while retaining d_a over the full action chunks, so the first-action environment estimand and full-chunk d_a definition remain unchanged."
+      ,"outcome": "applied",
+      "delta_summary": "Added an executable immutable-snapshot, branch-clone, cache-validity, state-equality, and first-action restoration protocol without changing the one-transition estimand."
+    },
+    {
+      "scope": "tactical",
+      "op": "append_sentence",
+      "field": "hook",
+      "value": "C_r is not task loss, the physical consequence of executing the full action chunk or a replan horizon, a marginal effect in the jointly low-bit deployment, or a bytes/latency objective; any reduction in held-out task failures is only an empirical predictive test of transfer from this one-control-transition proxy."
+      ,"outcome": "applied",
+      "delta_summary": "Made the proxy boundary explicit and labeled held-out task-failure reduction as an empirical predictive test rather than an identity claim."
+    }
+  ]
+}

exec
"C:\\Users\\Raymond\\.cache\\codex-runtimes\\codex-primary-runtime\\dependencies\\native\\powershell\\pwsh.exe" -Command '$out='"'D:\\Downloads\\Final Year Project\\ideaspark_run\\world-action-model-quantization\\phase3_revise\\phase3_revise_output.json'; "'$candidate='"'D:\\Downloads\\Final Year Project\\ideaspark_run\\world-action-model-quantization\\phase2_coherence\\refined_candidate.json'; "'$p=Get-Content -Raw -LiteralPath $out | ConvertFrom-Json; $c=Get-Content -Raw -LiteralPath $candidate | ConvertFrom-Json
$checks=[ordered]@{
  output_bytes=(Get-Item -LiteralPath $out).Length
  patch_only=($null -eq $p.final_candidate -and $null -eq $p.final_candidate_json)
  all_scopes_tactical=(@($p.applied_revisions | Where-Object {$_.scope -ne '"'tactical'}).Count -eq 0)
  fields_exact=(("'$p.applied_revisions.field -join '"'|') -eq 'differentiation_from_lit|core_mechanism_steps|hook')
  guard_source_has_80GB_A100=(("'$c.compute_budget -match '"'80GB-class') -and ("'$c.compute_budget -match '"'A100-40GB'))
  guard_source_untouched_by_patch=(@("'$p.applied_revisions | Where-Object {$_.field -match '"'"'^(falsification_prediction|compute_budget)('"\\.|"'$)'"'}).Count -eq 0)
  no_mechanism_relabeling=("'$p.applied_revisions[2].value -match '"'not task loss' -and "'$p.applied_revisions[2].value -match '"'jointly low-bit deployment' -and "'$p.applied_revisions[2].value -match '"'bytes/latency objective')
}
"'$checks | ConvertTo-Json
Write-Output ('"'output_path=' + "'$out)
Write-Output ('"'output_bytes=' + ((Get-Item -LiteralPath "'$out).Length))' in D:\Downloads\Final Year Project
 succeeded in 167ms:
{
  "output_bytes": 7044,
  "patch_only": true,
  "all_scopes_tactical": true,
  "fields_exact": true,
  "guard_source_has_80GB_A100": true,
  "guard_source_untouched_by_patch": true,
  "no_mechanism_relabeling": true
}
output_path=D:\Downloads\Final Year Project\ideaspark_run\world-action-model-quantization\phase3_revise\phase3_revise_output.json
output_bytes=7044

diff --git a/ideaspark_run/world-action-model-quantization/phase3_revise/phase3_revise_output.json b/ideaspark_run/world-action-model-quantization/phase3_revise/phase3_revise_output.json
new file mode 100644
index 0000000000000000000000000000000000000000..db526281f10aff3a81d2a41ef66744fb9542355e
--- /dev/null
+++ b/ideaspark_run/world-action-model-quantization/phase3_revise/phase3_revise_output.json
@@ -0,0 +1,58 @@
+{
+  "candidate_id": null,
+  "applied_revisions": [
+    {
+      "scope": "tactical",
+      "op": "replace",
+      "field": "differentiation_from_lit",
+      "value": [
+        {
+          "paper_id": "arxiv:2607.28405v1",
+          "delta": "QuantWAMs already covers joint video/action empirical-Fisher cross terms over block and denoising-step granularity and fixed-count closed-loop protection-schedule repair, so joint coupling, denoising-step sensitivity, closed-loop protection, and contact-critical allocation are prior art; this candidate instead intervenes on one numerical weight-or-activation site r with every other target site at FP16, estimates C_r from paired full action-chunk discrepancy plus the next observation after only the first action is executed, and ranks sites under B_site, without claiming a jointly-low-bit marginal, joint optimality, task-success-loss identity, full-horizon/replan consequence, bytes objective, or latency objective."
+        },
+        {
+          "paper_id": "arxiv:2602.02110v1",
+          "delta": "QuantWM already covers weight/activation PTQ sensitivity and long-horizon error in a DINO-WM latent planning setting, so latent rollout/planning mismatch and that sensitivity branch are not claimed as novel here; this candidate is a manipulation WAM coupled future-video/action denoiser with a single-site FP16-background numerical intervention, a one-control-transition C_r estimand, and paired full-chunk/next-observation discrepancies rather than a latent rollout/planning marginal or a bytes/latency objective."
+        },
+        {
+          "paper_id": "VAML (AISTATS 2017)",
+          "delta": "VAML is part of the earlier task-consequence/value-aware lineage supplied for this run, so defining a quantization target by downstream consequence is prior art and not this candidate's novelty; the bounded difference here is a single numerical W/A site intervention in a coupled future-video/action WAM, with C_r estimating a one-control-transition paired action/observation proxy and the numerical operator combining normalized full action-chunk MSE with next-observation pixel L1, not a claim of identical objective, task-loss identity, or full-horizon marginal."
+        },
+        {
+          "paper_id": "arxiv:2106.14080",
+          "delta": "Model Advantage / Value-Aware Model Learning is included in the earlier task-consequence lineage, so broad downstream-consequence targeting is prior art; this candidate makes no equivalence claim about objectives or estimands and retains only the narrower concrete intervention of one-site W/A quantization with an FP16 background, the one-control-transition C_r proxy, and top-B_site allocation in a coupled WAM, without joint-low-bit optimality or task-loss claims."
+        },
+        {
+          "paper_id": "arxiv:1908.06845",
+          "delta": "Deep Task-Based Quantization establishes the prior-art principle that downstream task consequence can guide quantization, which is not claimed as novel here; the candidate's distinct bounded object is a single-site W/A PTQ intervention with all other target sites at FP16, C_r over one simulator control transition, and a normalized full-action-chunk plus next-observation numerical score, while held-out task-failure reduction is only a predictive validation of transfer and not C_r itself."
+        },
+        {
+          "paper_id": "arxiv:2505.15304",
+          "delta": "SQIL already treats mission-critical-state protection as prior art through saliency-weighted QAT and action distillation, so protecting contact or other critical states alone is not novel; this candidate uses a different bounded intervention and estimand—single-site numerical W/A PTQ in a coupled future-video/action WAM, paired simulator branches with only the first action executed, and C_r from full-chunk/next-observation discrepancy—and does not claim that SQIL is identical or that critical-state protection alone is its contribution."
+        },
+        {
+          "paper_id": "semanticscholar:b5be12ccbb037be57a1dbc03fbd43d379170055f",
+          "delta": "Fast-WAM isolates video co-training from explicit future imagination and does not provide a numerical PTQ consequence signal, whereas this candidate keeps the future/action-coupled idm feasibility path and adds single-site weight/activation intervention with consequence-based allocation."
+        }
+      ],
+      "outcome": "applied",
+      "delta_summary": "Expanded the literature differentiation to all six required prior-art comparisons while preserving novelty only for the one-site FP16-background WAM numerical operator and one-control-transition estimand."
+    },
+    {
+      "scope": "tactical",
+      "op": "append_sentence",
+      "field": "core_mechanism_steps",
+      "value": "Executable branch protocol: the exact Optional IDM checkpoint identity (the checkpoint_FW filename, repository revision, and immutable digest when available) must be present and unambiguous in the pre-run manifest; otherwise halt before calibration. Before either branch, serialize one immutable versioned snapshot S_j in the fixed order controller state, simulator RNG state eta_j, model/generator RNG state xi_j, observation/history buffers, persistent model-cache state, simulator state z_j, and action-transformation state; deep-clone S_j into separate F_0 and F_r copies, restore the complete copy immediately before its branch, and rebuild or invalidate every cache entry whose key or dependency is touched by r before executing that branch. Canonical-serialize and equality-check h_j, l_j, z_j, controller/RNG/history/action-transformation state, and the cache-validity manifest against S_j after each restore; allow only the explicitly recorded r-touched entries to differ after branch-local invalidation/rebuild, abort on any other mismatch, and never share mutable state or cache entries. Equality-check the action-transformation inputs and the first-action mapping before applying each branch's first executable action to its restored simulator, while retaining d_a over the full action chunks, so the first-action environment estimand and full-chunk d_a definition remain unchanged."
+      ,"outcome": "applied",
+      "delta_summary": "Added an executable immutable-snapshot, branch-clone, cache-validity, state-equality, and first-action restoration protocol without changing the one-transition estimand."
+    },
+    {
+      "scope": "tactical",
+      "op": "append_sentence",
+      "field": "hook",
+      "value": "C_r is not task loss, the physical consequence of executing the full action chunk or a replan horizon, a marginal effect in the jointly low-bit deployment, or a bytes/latency objective; any reduction in held-out task failures is only an empirical predictive test of transfer from this one-control-transition proxy."
+      ,"outcome": "applied",
+      "delta_summary": "Made the proxy boundary explicit and labeled held-out task-failure reduction as an empirical predictive test rather than an identity claim."
+    }
+  ]
+}

codex
已完成，patch-only JSON 已写入：

[phase3_revise_output.json](<D:\Downloads\Final Year Project\ideaspark_run\world-action-model-quantization\phase3_revise\phase3_revise_output.json>)

QA 通过：3 个 targets、JSON 有效、未修改 guarded fields、保留 80GB-class/A100-40GB 边界。未运行 merger。

完成信号：`PATCH_WRITTEN`


diff --git a/ideaspark_run/world-action-model-quantization/phase3_revise/phase3_revise_output.json b/ideaspark_run/world-action-model-quantization/phase3_revise/phase3_revise_output.json
new file mode 100644
index 0000000000000000000000000000000000000000..db526281f10aff3a81d2a41ef66744fb9542355e
--- /dev/null
+++ b/ideaspark_run/world-action-model-quantization/phase3_revise/phase3_revise_output.json
@@ -0,0 +1,58 @@
+{
+  "candidate_id": null,
+  "applied_revisions": [
+    {
+      "scope": "tactical",
+      "op": "replace",
+      "field": "differentiation_from_lit",
+      "value": [
+        {
+          "paper_id": "arxiv:2607.28405v1",
+          "delta": "QuantWAMs already covers joint video/action empirical-Fisher cross terms over block and denoising-step granularity and fixed-count closed-loop protection-schedule repair, so joint coupling, denoising-step sensitivity, closed-loop protection, and contact-critical allocation are prior art; this candidate instead intervenes on one numerical weight-or-activation site r with every other target site at FP16, estimates C_r from paired full action-chunk discrepancy plus the next observation after only the first action is executed, and ranks sites under B_site, without claiming a jointly-low-bit marginal, joint optimality, task-success-loss identity, full-horizon/replan consequence, bytes objective, or latency objective."
+        },
+        {
+          "paper_id": "arxiv:2602.02110v1",
+          "delta": "QuantWM already covers weight/activation PTQ sensitivity and long-horizon error in a DINO-WM latent planning setting, so latent rollout/planning mismatch and that sensitivity branch are not claimed as novel here; this candidate is a manipulation WAM coupled future-video/action denoiser with a single-site FP16-background numerical intervention, a one-control-transition C_r estimand, and paired full-chunk/next-observation discrepancies rather than a latent rollout/planning marginal or a bytes/latency objective."
+        },
+        {
+          "paper_id": "VAML (AISTATS 2017)",
+          "delta": "VAML is part of the earlier task-consequence/value-aware lineage supplied for this run, so defining a quantization target by downstream consequence is prior art and not this candidate's novelty; the bounded difference here is a single numerical W/A site intervention in a coupled future-video/action WAM, with C_r estimating a one-control-transition paired action/observation proxy and the numerical operator combining normalized full action-chunk MSE with next-observation pixel L1, not a claim of identical objective, task-loss identity, or full-horizon marginal."
+        },
+        {
+          "paper_id": "arxiv:2106.14080",
+          "delta": "Model Advantage / Value-Aware Model Learning is included in the earlier task-consequence lineage, so broad downstream-consequence targeting is prior art; this candidate makes no equivalence claim about objectives or estimands and retains only the narrower concrete intervention of one-site W/A quantization with an FP16 background, the one-control-transition C_r proxy, and top-B_site allocation in a coupled WAM, without joint-low-bit optimality or task-loss claims."
+        },
+        {
+          "paper_id": "arxiv:1908.06845",
+          "delta": "Deep Task-Based Quantization establishes the prior-art principle that downstream task consequence can guide quantization, which is not claimed as novel here; the candidate's distinct bounded object is a single-site W/A PTQ intervention with all other target sites at FP16, C_r over one simulator control transition, and a normalized full-action-chunk plus next-observation numerical score, while held-out task-failure reduction is only a predictive validation of transfer and not C_r itself."
+        },
+        {
+          "paper_id": "arxiv:2505.15304",
+          "delta": "SQIL already treats mission-critical-state protection as prior art through saliency-weighted QAT and action distillation, so protecting contact or other critical states alone is not novel; this candidate uses a different bounded intervention and estimand—single-site numerical W/A PTQ in a coupled future-video/action WAM, paired simulator branches with only the first action executed, and C_r from full-chunk/next-observation discrepancy—and does not claim that SQIL is identical or that critical-state protection alone is its contribution."
+        },
+        {
+          "paper_id": "semanticscholar:b5be12ccbb037be57a1dbc03fbd43d379170055f",
+          "delta": "Fast-WAM isolates video co-training from explicit future imagination and does not provide a numerical PTQ consequence signal, whereas this candidate keeps the future/action-coupled idm feasibility path and adds single-site weight/activation intervention with consequence-based allocation."
+        }
+      ],
+      "outcome": "applied",
+      "delta_summary": "Expanded the literature differentiation to all six required prior-art comparisons while preserving novelty only for the one-site FP16-background WAM numerical operator and one-control-transition estimand."
+    },
+    {
+      "scope": "tactical",
+      "op": "append_sentence",
+      "field": "core_mechanism_steps",
+      "value": "Executable branch protocol: the exact Optional IDM checkpoint identity (the checkpoint_FW filename, repository revision, and immutable digest when available) must be present and unambiguous in the pre-run manifest; otherwise halt before calibration. Before either branch, serialize one immutable versioned snapshot S_j in the fixed order controller state, simulator RNG state eta_j, model/generator RNG state xi_j, observation/history buffers, persistent model-cache state, simulator state z_j, and action-transformation state; deep-clone S_j into separate F_0 and F_r copies, restore the complete copy immediately before its branch, and rebuild or invalidate every cache entry whose key or dependency is touched by r before executing that branch. Canonical-serialize and equality-check h_j, l_j, z_j, controller/RNG/history/action-transformation state, and the cache-validity manifest against S_j after each restore; allow only the explicitly recorded r-touched entries to differ after branch-local invalidation/rebuild, abort on any other mismatch, and never share mutable state or cache entries. Equality-check the action-transformation inputs and the first-action mapping before applying each branch's first executable action to its restored simulator, while retaining d_a over the full action chunks, so the first-action environment estimand and full-chunk d_a definition remain unchanged."
+      ,"outcome": "applied",
+      "delta_summary": "Added an executable immutable-snapshot, branch-clone, cache-validity, state-equality, and first-action restoration protocol without changing the one-transition estimand."
+    },
+    {
+      "scope": "tactical",
+      "op": "append_sentence",
+      "field": "hook",
+      "value": "C_r is not task loss, the physical consequence of executing the full action chunk or a replan horizon, a marginal effect in the jointly low-bit deployment, or a bytes/latency objective; any reduction in held-out task failures is only an empirical predictive test of transfer from this one-control-transition proxy."
+      ,"outcome": "applied",
+      "delta_summary": "Made the proxy boundary explicit and labeled held-out task-failure reduction as an empirical predictive test rather than an identity claim."
+    }
+  ]
+}

2026-09-08T13:38:42.065789Z  WARN codex_mcp::rmcp_client: failed to initialize MCP client during shutdown: MCP startup failed: handshaking with MCP server failed: Send message error Transport [codex_rmcp_client::event_notification_transport::EventNotificationTransport<rmcp::transport::worker::WorkerTransport<rmcp::transport::streamable_http_client::StreamableHttpClientWorker<codex_rmcp_client::http_client_adapter::StreamableHttpClientAdapter>>>] error: Auth required, when send initialize request
2026-09-08T13:38:42.066187Z  WARN codex_mcp::rmcp_client: failed to initialize MCP client during shutdown: MCP startup failed: handshaking with MCP server failed: Send message error Transport [codex_rmcp_client::event_notification_transport::EventNotificationTransport<rmcp::transport::worker::WorkerTransport<rmcp::transport::streamable_http_client::StreamableHttpClientWorker<codex_rmcp_client::http_client_adapter::StreamableHttpClientAdapter>>>] error: Auth required, when send initialize request
tokens used
124,480
已完成，patch-only JSON 已写入：

[phase3_revise_output.json](<D:\Downloads\Final Year Project\ideaspark_run\world-action-model-quantization\phase3_revise\phase3_revise_output.json>)

QA 通过：3 个 targets、JSON 有效、未修改 guarded fields、保留 80GB-class/A100-40GB 边界。未运行 merger。

完成信号：`PATCH_WRITTEN`


