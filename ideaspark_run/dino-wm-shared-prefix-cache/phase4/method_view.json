{
  "title": "RankSafe Prefix Cache for DINO-WM CEM Planning",
  "method_name": "RankSafe Exact-Prefix Cache (RankSafe-EPC)",
  "abstract_draft": "Counterfactual action-sequence evaluation is the main cost in DINO-WM's CEM planner: many candidates share the same observation and history computation, yet the planner repeatedly recomputes it before applying different action-conditioned suffixes. Recent reuse methods such as C³ache (arxiv:2606.08962v1) and X-Cache (arxiv:2604.20289v2) use residual similarity or action-aware fingerprints, but they do not make preservation of CEM candidate ordering the validity target. RankSafe-EPC instruments the forward graph, identifies an exact prefix whose outputs are independent of the candidate action, computes that prefix once, and reuses it while retaining every action-conditioned suffix and a mismatch fallback. The load-bearing prediction is that a correct action-dependency mask will preserve candidate costs, CEM first-action choices, and closed-loop Wall success while reducing native end-to-end planner latency; a randomized mask should instead induce rank inversions and lower action agreement. The expected outcome is a bounded planner-level cache with an explicit invariance contract, not a claim that approximate cache similarity is safe in general. Failure to reduce native latency or failure of the randomized-mask control to affect downstream decisions would falsify the proposed mechanism.",
  "core_claim": "Caching an exactly action-independent DINO-WM prefix across CEM candidates preserves candidate-cost ordering and the selected first action, whereas corrupting its action-dependency mask causes rank inversions.",
  "sub_claims": [
    {
      "id": "c1",
      "statement": "A graph-derived binary mask can identify a DINO-WM prefix whose outputs are identical for all counterfactual CEM candidates.",
      "supports_which_aspect": "Closes the bottleneck's missing action-independence contract for shared observation/history computation."
    },
    {
      "id": "c2",
      "statement": "Reusing that exact prefix while recomputing every action-conditioned suffix preserves candidate costs, their ordering, and the CEM first-action choice.",
      "supports_which_aspect": "Closes the gap between computation reuse and the planner-selection quantity that controls downstream behavior."
    },
    {
      "id": "c3",
      "statement": "A randomized action-dependency mask produces candidate-cost rank inversions and lower first-action agreement or closed-loop success under a matched cache opportunity.",
      "supports_which_aspect": "Distinguishes the causal invariance mechanism from a generic cache path or latency effect."
    }
  ],
  "key_equations": [
    {
      "id": "e1",
      "linked_step_id": "S1",
      "latex": "M_b = \\mathbf{1}\\{\\partial h_b / \\partial a \\neq 0\\}",
      "description": "The binary mask marks graph nodes at boundary b whose representation depends on the candidate action.",
      "description_zh": "二值 mask 标记边界 b 处依赖候选 action 的图节点。"
    },
    {
      "id": "e2",
      "linked_step_id": "S2",
      "latex": "M_b=0 \\Longrightarrow h_b^{(j)} = h_b^{\\mathrm{cache}} \\quad \\forall j \\in \\{1,\\ldots,K\\}",
      "description": "When the boundary is action-independent, one cached prefix representation is valid for every candidate j.",
      "description_zh": "当边界与 action 无关时，一个 cached prefix 表示可供全部候选 j 使用。"
    },
    {
      "id": "e3",
      "linked_step_id": "S3",
      "latex": "J_j = \\sum_{\\tau=1}^{H} \\ell(\\hat{o}_{j,\\tau}, o_{t+\\tau}), \\qquad \\pi = \\operatorname{argsort}(J_{1:K})",
      "description": "CEM ranks candidates by their rollout cost after the shared prefix and candidate-specific suffix are evaluated.",
      "description_zh": "CEM 根据共享 prefix 与候选专属 suffix 得到的 rollout cost 对候选排序。"
    },
    {
      "id": "e4",
      "linked_step_id": "S4",
      "latex": "\\Delta_{\\mathrm{rank}} = \\mathbf{1}[\\pi_{\\mathrm{cache}} \\neq \\pi_{\\mathrm{full}}], \\qquad a^*_{\\mathrm{cache}} = a^*_{\\mathrm{full}}",
      "description": "The mechanism check records rank disagreement and requires the selected first action to match the full recomputation.",
      "description_zh": "机制检查记录排序是否不一致，并要求 cached 与 full recomputation 的首 action 相同。"
    }
  ],
  "method_flow": {
    "high_level_pipeline": "Given one DINO-WM Wall observation and a CEM candidate population, instrument the forward graph to locate the boundary before action-dependent computation and record its binary mask. Compute the observation/history prefix once and reuse it only when the mask certifies candidate independence; evaluate every action-conditioned suffix normally. Compare the cached and full candidate costs, ordering, first action, closed-loop outcome, and native end-to-end latency, with a canary mismatch fallback for the valid cache path. A matched randomized-mask control tests whether any downstream change is caused by violating the proposed invariance rather than by caching alone.",
    "steps": [
      {
        "step_id": "S1",
        "title": "Trace the action boundary",
        "what_changes": "Instrument the DINO-WM CEM forward graph and construct `M_b`, marking which boundary outputs depend on candidate actions.",
        "why_this_step": "The bottleneck is repeated shared-prefix work, and an explicit dependency mask supplies the missing exact validity condition.",
        "linked_component": "engineering",
        "linked_falsification": "mechanism_distinguisher",
        "input": "DINO-WM Wall checkpoint, observation/history input, and CEM forward graph",
        "output": "Candidate-independent boundary and binary action-dependency mask `M_b`"
      },
      {
        "step_id": "S2",
        "title": "Cache the invariant prefix",
        "what_changes": "Compute the prefix through the certified boundary once per observation/history and feed the same cached representation to all candidate-specific suffixes.",
        "why_this_step": "Exact reuse removes duplicated computation without altering the action-conditioned part that determines counterfactual outcomes.",
        "linked_component": "engineering",
        "linked_falsification": "mechanism_distinguisher",
        "input": "Boundary mask `M_b` and current observation/history",
        "output": "Reusable prefix representation plus a cache-hit path"
      },
      {
        "step_id": "S3",
        "title": "Guard candidate evaluation",
        "what_changes": "Run each action-conditioned suffix, compare a deterministic canary against the full path, and fall back on mismatch while retaining a randomized-mask negative control with fallback disabled.",
        "why_this_step": "The provenance threat is that a self-contaminated gate can hide errors; an explicit canary and a matched corrupted-mask control expose that failure mode.",
        "linked_component": "engineering",
        "linked_falsification": "mechanism_distinguisher",
        "input": "Cached prefix and candidate-specific action suffixes",
        "output": "Guarded cached candidate costs and negative-control costs"
      },
      {
        "step_id": "S4",
        "title": "Test planner invariance",
        "what_changes": "Measure candidate-cost rank disagreement, first-action agreement, closed-loop success, and native end-to-end latency against matched full recomputation.",
        "why_this_step": "The proposal is about preserving the planner's selection quantity, so speed without ordering and action agreement is insufficient evidence.",
        "linked_component": "theory",
        "linked_falsification": "metric_specification",
        "input": "Full, RankSafe, and randomized-mask candidate evaluations under identical CEM settings",
        "output": "Planner-level invariance and latency verdict"
      }
    ],
    "design_reasoning_echo": "The design chooses exact action-independence over residual similarity because the downstream object that matters is CEM's candidate ordering, not a local feature distance. It removes only the prefix provably shared by counterfactual branches and leaves the action-conditioned suffix intact, limiting the mechanism claim to a graph boundary that can be inspected. The canary fallback addresses gate provenance, while the randomized-mask arm tests whether rank changes follow the proposed dependency violation; this separates a planner-preserving cache from generic acceleration or self-contaminated reuse."
  },
  "plain_method_steps_en": [
    {
      "step_id": "S1",
      "what_to_do": "Instrument the DINO-WM CEM forward graph using the Wall checkpoint, observation/history input, and CEM graph. Build `M_b` to mark which outputs at the chosen boundary depend on candidate actions. The result is a candidate-independent boundary and a binary action-dependency mask `M_b`.",
      "why_this_makes_sense": "The repeated work is the shared prefix, so an explicit dependency mask provides the exact condition for deciding whether that prefix can be reused."
    },
    {
      "step_id": "S2",
      "what_to_do": "For each observation/history, compute the prefix through the certified boundary once, then send the same cached representation to every candidate-specific action-conditioned suffix. Produce a reusable prefix representation and a cache-hit path.",
      "why_this_makes_sense": "Exact reuse removes duplicated computation while leaving the action-conditioned part that determines counterfactual outcomes unchanged."
    },
    {
      "step_id": "S3",
      "what_to_do": "Run every action-conditioned suffix. Compare a deterministic canary with the full path and fall back on a mismatch. In parallel, retain a randomized-mask negative control with fallback disabled; use the cached prefix and candidate-specific suffixes to obtain guarded cached costs and negative-control costs.",
      "why_this_makes_sense": "A gate could hide an error if it is contaminated by the result it is meant to protect. The canary and the matched corrupted-mask control expose that failure mode."
    },
    {
      "step_id": "S4",
      "what_to_do": "Under identical CEM settings, compare full, RankSafe, and randomized-mask candidate evaluations. Measure candidate-cost rank disagreement, first-action agreement, closed-loop success, and native end-to-end latency, and report a planner-level invariance and latency verdict.",
      "why_this_makes_sense": "The proposal concerns the planner's selection quantity, so speed alone is not enough; ordering and action agreement must also be preserved."
    }
  ],
  "plain_method_steps_zh": [
    {
      "step_id": "S1",
      "what_to_do": "使用 Wall checkpoint、observation/history input 和 CEM forward graph，对 DINO-WM CEM forward graph 做 instrumentation。在选定 boundary 处构造 `M_b`，标记哪些 output 依赖候选 action。结果是 candidate-independent boundary 和 binary action-dependency mask `M_b`。",
      "why_this_makes_sense": "重复发生的计算是 shared prefix，因此显式 dependency mask 能提供判断这个 prefix 是否可复用的精确条件。"
    },
    {
      "step_id": "S2",
      "what_to_do": "对于每个 observation/history，只计算一次通过已认证 boundary 的 prefix，然后把同一个 cached representation 送入每个 candidate-specific action-conditioned suffix。得到 reusable prefix representation 和 cache-hit path。",
      "why_this_makes_sense": "Exact reuse 去掉重复计算，同时保持决定 counterfactual outcome 的 action-conditioned 部分不变。"
    },
    {
      "step_id": "S3",
      "what_to_do": "运行每个 action-conditioned suffix。把 deterministic canary 与 full path 比较，出现 mismatch 时回退。与此同时保留一个关闭 fallback 的 randomized-mask negative control；使用 cached prefix 和 candidate-specific suffixes，得到 guarded cached costs 与 negative-control costs。",
      "why_this_makes_sense": "如果 gate 被它本应保护的结果污染，它可能掩盖错误。canary 和匹配的 corrupted-mask control 可以暴露这种 failure mode。"
    },
    {
      "step_id": "S4",
      "what_to_do": "在相同的 CEM settings 下比较 full、RankSafe 和 randomized-mask candidate evaluations。测量 candidate-cost rank disagreement、first-action agreement、closed-loop success 以及 native end-to-end latency，并给出 planner-level invariance 和 latency verdict。",
      "why_this_makes_sense": "这个 proposal 关注的是 planner 的 selection quantity，因此只有速度还不够；还必须保持排序和 action agreement。"
    }
  ]
}